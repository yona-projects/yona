---
id: p3-53
type: ticket
status: open
created: 2026-09-11
relates_to: [p3-46, p3-49]
source: P3-46 8단계(마크다운 에디터 EasyMDE 전환) 1~2단계(셸 교체·미리보기 서버 연동) 완료
  로그를 사용자 요청으로 재검토하며 세부 항목 단위로 재정리
---

# P3-53: P3-46 8-1/8-2단계(EasyMDE 셸 교체·미리보기 연동)에서 남겨진 죽은 코드·미정리 항목 묶음

## 배경

P3-46(클라이언트 위젯 라이브러리 현대화) 8단계 중 1단계(EasyMDE 셸 교체)와 2단계(미리보기
서버 연동)를 진행하며 "범위 밖 발견"으로 티켓 완료 로그에 기록만 해두고 별도 백로그로
승격하지 않았던 항목들이다. 사용자가 "8-1에서만 8개 정도 나왔던 것 같은데 왜 간추리냐"고
지적해, [[tickets/p3-46|P3-46]] 완료 로그에 흩어져 있던 항목을 하나도 누락 없이 재정리해
공식 백로그 항목으로 등록한다.

## 세부 항목 (총 8개)

1. **`yona.CommentAttachmentsUpdate.js`가 애초에 어느 템플릿·번들에서도 로드된 적 없음** —
   8-1단계 에이전트가 "드래그앤드롭 파일첨부가 깨질 수 있다"는 위험을 짚으며 근거로 든 파일.
   1차 결론은 "이 파일 자체가 죽은 코드라 EasyMDE와 무관"이었음. → 실제로는 [[tickets/p3-49]]에서
   "댓글 **수정** 폼 전용 파일이며 `<script src>`로 로드된 적이 없다"는 진짜 원인으로 재규명되어
   **해소됨(완료)**.
2. **`yona.SubComment.js`도 어느 템플릿·번들에서도 로드된 적 없음** — 1번과 같은 문맥에서 함께
   지목된 죽은 코드. 이 파일 자체의 기능(대댓글 관련 추정)이 실제로 필요한지, 완전히 죽은
   기능인지는 **별도로 확인된 적이 없다 — 미착수**. → **2026-09-11 1차 재검증(정적분석)**:
   P3-52가 추가한 `common/childComments.html`이 `.add-a-comment`/`.child-comment-input-form`
   클래스를 쓰고 `yobi.css`가 이를 기본 숨김 처리하는데 그 토글 JS(`yona.SubComment.js`)가
   미로드라 "버튼이 안 보이는 회귀"로 추정했음. → **2026-09-11 Playwright 실브라우저
   재검증 결과 가설이 틀렸음이 확인됨(정반대 방향)**: 실제로는 버튼/폼이 **호버·클릭 없이
   페이지 로드 시점부터 항상 열려서 보인다**(대댓글 작성 자체는 정상 동작). 근본 원인은
   `yobi.css`의 숨김 선택자(`.board-comment-wrap .comments .comment .add-a-comment` 등)가
   `.board-comment-wrap`과 `.comment` 사이에 `.comments` 조상 클래스를 요구하는데,
   `issue/view.html`/`board/view.html`이 쓰는 실제 마크업엔 그 조상이 없어(다른 구조,
   `#timeline`→`.timeline-list`→`.comment`) 선택자가 애초에 매치되지 않고 브라우저 기본값
   (`display:block`)이 그대로 노출되는 것 — **CSS 선택자 스코프 불일치가 진짜 원인이고,
   `yona.SubComment.js` 미로드는 부차적 요인**(로드해도 초기 숨김 자체는 해결 안 됨, 그
   JS는 "보여주기"만 담당). **기능 결손이 아니라 UI 회귀(댓글마다 대댓글 입력창이 상시
   펼쳐진 채로 렌더링돼 스레드가 번잡해짐)로 재분류.**
3. **`issue/view.html`의 `<script>` 안 `<th:block th:if>` 중첩으로 `yobi.issue.View` 모듈
   자체가 파싱 실패해 안 뜨는 버그** — 사용자가 "실제로 드롭까지 해봤냐"고 재확인시켜
   Playwright로 실제 재현해 드러난 진짜 원인 중 하나. → [[tickets/p3-49]]에서 **수정 완료**.
4. **`yona.CommentAttachmentsUpdate.js`가 포팅은 됐지만 `<script src>`로 로드된 적이 없는
   버그**(1번과 동일 파일, "죽은 코드"가 아니라 "로드 누락"이라는 별개 원인으로 재규명) —
   → [[tickets/p3-49]]/[[tickets/p3-50]]으로 분리. **완전히 해소됨(완료)** — P3-50(커밋
   `9eaa92498`, 2026-09-11)에서 `issue/view.html`/`board/view.html` 양쪽에 실제 로드
   완료, 게다가 그 과정에서 훨씬 심각한 자매 버그(`yobi.Files.js`/`yobi.Attachments.js`도
   동일하게 미로드돼 있어 첨부파일 링크 삽입이 저장 시점에 통째로 사라지는 데이터손실급
   결함)까지 발견·수정.
5. **`PullRequestListTemplateEquivalenceSpec`의 select2.js 스크립트 존재 단언(214번째 줄)이
   stale함** — 항목5(Select2→Tom Select 전환)에서 이미 제거된 리소스를 여전히 기대하는
   기존 단언. 발견 당시엔 `git stash` 대조로 확인만 하고 손대지 않았으나, **P3-51(커밋
   `4dc86ba74`, 2026-09-11) 재검토 중 함께 해소됨(완료)**.
6. **`.nav-tabs.small` CSS 규칙(`yobi.css`)이 구 탭 UI 제거로 고아가 됨** — 동작에는 영향
   없어 그대로 방치됨. **미착수**.
7. **`.markdown-preview` div의 `editorMode` 클래스 분기** — 8-1단계에서 실사용처를 못 찾아
   일단 제거했으나 100% 확신하지 못해 "2단계에서 재확인 필요"로 남겼던 항목. → 8-2단계에서
   `yobi.css`/전체 JS 재grep으로 재확인해 이 분기를 읽는 규칙이 정말 없음을 확정.
   **완전히 해소됨(완료)**.
8. **8-1단계의 "드래그앤드롭 문제 없음" 최초 결론 자체가 틀렸었다는 메타적 교훈** — 1차
   조사(소스 grep만)로는 "EasyMDE 탓 아니고 문제 없다"고 결론 냈으나, 사용자가 실제 브라우저
   재현을 요구해서야 진짜 회귀(3·4번)가 드러났다. 향후 유사한 "코드 안 읽었지만 문제
   없어 보인다"류 결론은 실제 브라우저 재현 없이는 확정하지 말 것 — **별도 코드 변경 대상이
   아니라 작업 방식에 대한 기록**.

## 현재 상태 요약

| # | 항목 | 상태 |
|---|---|---|
| 1 | `yona.CommentAttachmentsUpdate.js` 미로드(죽은 코드 오판) | 완료(P3-49) |
| 2 | `yona.SubComment.js` 미로드 → CSS 스코프 불일치로 대댓글 입력창 상시노출 | 완료(아래 완료 로그 참고) |
| 3 | `issue/view.html` `<th:block>` 중첩 파싱 실패 | 완료(P3-49) |
| 4 | `yona.CommentAttachmentsUpdate.js` 로드 누락 | 완료(P3-50) |
| 5 | `PullRequestListTemplateEquivalenceSpec` stale 단언 | 완료(P3-51) |
| 6 | `.nav-tabs.small` 고아 CSS | 완료(아래 완료 로그 참고) |
| 7 | `.markdown-preview`의 `editorMode` 분기 | 완료 |
| 8 | 실브라우저 재현 없이 "문제 없음" 결론 내린 것에 대한 교훈 | 기록용(코드 변경 아님) |

## 제안하는 다음 단계

**2번**: Playwright로 실재 확인된 UI 회귀(대댓글 입력창 상시노출)다. legacy v1.6 원본에서
`.comments` 조상 클래스가 어떤 마크업 구조였는지 대조해, (a) `issue/view.html`/
`board/view.html`의 댓글 목록 wrapper에 `.comments` 클래스를 추가해 기존 CSS 선택자가
정상 매치되게 하거나, (b) `yobi.css` 선택자를 현재 마크업(`.comments` 없이)에 맞게 수정하는
두 방법 중 legacy와 더 동치인 쪽을 택한다. 어느 쪽이든 `yona.SubComment.js`를 로드해
hover/click 토글이 실제로 동작하게 만들어야 완결된다. TDD+Playwright 재검증 필요.

**6번**은 여전히 단순 정리 대상 — 삭제로 끝난다.

## 완료 로그 (2026-09-11, 2번·6번)

### 2번 — 대댓글 입력창 상시노출 수정

- **legacy 대조**: `origin/v1.6` 확인 결과 `app/views/issue/partial_comments.scala.html`·
  `app/views/board/partial_comments.scala.html`은 모두 `<ul class="comments"><li class="comment">
  ...</li></ul>` 구조였다 — `_page.less`의 `.board-comment-wrap .comments .comment
  .add-a-comment { display: none; ... }`(그리고 `.subcomment-media-body .child-comment-input-form
  { display: none; }`)가 요구하는 `.comments` 조상이 legacy에는 실제로 존재했다. 이식된
  `issue/view.html`/`board/view.html`은 `#comments.board-comment-wrap` → `#timeline` →
  `.timeline-list`(→ `.comment`) 구조라 이 조상 클래스만 빠져 있었던 것 — 제안하던 두 방법(a/b)
  중 **(a) 현재 마크업에 `.comments` 클래스를 추가**하는 쪽이 legacy와 정확히 동치이고,
  코드베이스 다른 곳(`pullrequest/view.html`의 `<ul class="comments" id="comments">`,
  `code/diff.html`·`code/compare.html`·`code/partial_nonrange_codecomment_thread.html`의
  `<ul class="comments">`)도 이미 같은 관례를 쓰고 있어 채택.
- **수정**: `issue/view.html`의 `.timeline-list`(th:fragment="timelineItems")와
  `board/view.html`의 `.timeline-list`에 `comments` 클래스를 추가(`class="timeline-list
  comments"`). 부수효과로 `.board-comment-wrap .comments .event`/`.reply`/`.hasReply`/`.last`/
  `.mentioned` 등 같은 조상 요구 때문에 매치되지 않고 있던 다른 기존 규칙들도 함께 정상
  매치되기 시작한다(이번 티켓 범위인 대댓글 입력창 외의 부수적 개선).
- **`yona.SubComment.js` 로드**: `issue/view.html`·`board/view.html` 양쪽에
  `<script src="/javascripts/common/yona.SubComment.js">`를 `yona.CommentAttachmentsUpdate.js`
  다음 줄에 추가. 파일 내용은 legacy와 동일(포팅 시 이미 그대로 복사돼 있었음) — 현재
  마크업의 `.comment`/`.add-a-comment`/`.child-comment-input-form`/`.subcomment-author` 셀렉터와
  100% 일치해 코드 수정은 불필요했다. `.new-issue-by`/`.share-link` 셀렉터는 현재 마크업에
  없어 해당 부분은 no-op(빈 jQuery 셋에 대한 fadeIn/fadeOut, 에러 없음). 충돌 확인: 대댓글
  폼(`.child-comment-form`) 제출은 이미 `issue/view.html:647`·`board/view.html:326`의
  delegated AJAX `submit` 핸들러(P3-48/P3-52에서 배선)가 처리하고 있고, `yona.SubComment.js`는
  그 폼의 표시/숨김 토글만 담당(자체 submit 핸들러 없음, Cmd/Ctrl+Enter는 기존 폼의
  `.submit()`을 호출할 뿐)해 충돌 없음을 코드 읽기로 확인.
- **TDD**: `src/test/kotlin/com/github/yonaprojects/yona/web/SubCommentToggleMarkupTemplateEquivalenceSpec.kt`
  신규 작성. RED(수정 전) 3건 모두 실패 확인(`.comments` 조상 부재로 `doc.select("#comments
  .comments .comment .add-a-comment")`가 빈 결과, `yona.SubComment.js` 미로드로 문자열 포함
  단언 실패) → 마크업/스크립트 로드 수정 후 GREEN 확인. 관련 스펙
  (`SubCommentToggleMarkupTemplateEquivalenceSpec`, `IssueViewControllerSpec`,
  `BoardViewControllerSpec`, `CommentUpdateFormAttachmentTemplateEquivalenceSpec`,
  `NewCommentFormAjaxWiringTemplateEquivalenceSpec`, `CommentFormGuardsWiringTemplateRenderingSpec`,
  `BoardCommentFormGuardsWiringTemplateRenderingSpec`, `TimelineTemplateRenderingSpec`,
  `CommentNotiReceiverListWiringTemplateRenderingSpec`, `IssueDetectChangePollingTemplateRenderingSpec`)
  전부 그린.
- **Playwright 실브라우저 재검증(2026-09-11)**: h2 프로파일로 앱을 기동(`SPRING_PROFILES_ACTIVE=h2`,
  격리된 `yona.data`), 관리자 부트스트랩 → 프로젝트/이슈/게시글 생성 → 부모 댓글 작성까지
  실제 UI(로그인 폼, EasyMDE 에디터, 댓글 등록 버튼)로 수행한 뒤 확인:
  - 페이지 최초 로드 시 `.add-a-comment`/`.child-comment-input-form` 모두 `display: none`
    (이슈/게시글 양쪽) — 상시노출 회귀 재현되지 않음.
  - `.comment`에 마우스 hover 시 `.add-a-comment`가 `display: block`으로 페이드인.
  - `.add-a-comment` 클릭 시 `.child-comment-input-form`이 `display: block`으로 토글.
  - 실제로 대댓글 텍스트를 입력해 제출 → `.one-line-comment .contents`에 새 대댓글이 정상
    렌더링됨(이슈: "실제 대댓글 작성 테스트", 게시글: "게시판 실제 대댓글 작성 테스트").
  - 리로드 후에도 다시 초기 숨김 상태로 돌아옴(상태가 남아 항상 열려 보이는 회귀가 아님).
  - 콘솔 JS 에러 없음(`yona.SubComment.js` 로드로 인한 신규 에러 없음).
  - 검증용 임시 스크립트/H2 데이터는 세션 종료 전 정리, 앱 프로세스 종료. 공유 테스트
    DB 컨테이너(`jovial_cori`, mariadb:10.11, testcontainers reuse)는 건드리지 않음.

### 6번 — `.nav-tabs.small` 고아 CSS 삭제

- 전체 템플릿에 `grep -rln "nav-tabs"` 결과 나온 모든 파일을 대상으로 `nav-tabs`와 `small`이
  함께 쓰인 곳이 없음을 재확인(`class="nav-tabs small"` 또는 `nav nav-tabs ... small` 형태
  전무). `.editor-notice-label`(이 규칙 안에서만 스타일링되던 자손 셀렉터)은 별도로
  `[data-toggle="markdown-editor"] .editor-notice-label`이 이미 스타일을 담당하고 있어
  살아있는 기능에는 영향 없음을 확인.
- `src/main/resources/static/stylesheets/yobi.css`에서 `.nav-tabs.small`/`.nav-tabs.small li a`/
  `.nav-tabs.small .editor-notice-label`/`.editor-notice-label .unsaved`/`.editor-notice-label
  .saved`(구 10233~10253행) 블록을 통째로 삭제. 참조하는 템플릿이 없어 별도 회귀 테스트는
  추가하지 않음(티켓에서도 "삭제로 끝난다"로 명시).
