---
id: p2-51
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:252 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-51: **(2026-08-26 골든 체크 버킷C 사람 검토에서 발견, 2026-08-26 재검증 후 CLOSED)** 메일 처리 실패 사유별 커스텀 예외 5종이 yona에서는 단일 타입으로 통합됨

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/mailbox/exceptions/{IllegalDetailException,IssueNotFound,MailHandlerException,PermissionDenied,PostingNotFound}.java`
- **yona 대상**: `domain/mail/IncomingMailOutcome.Rejected(reason: String)`
- **비고(원문)**: **공백 아님, CLOSED** — `IncomingMailProcessingService.kt`의 모든 거부 경로(`Rejected("이슈를 찾을 수 없습니다: ...")`, `Rejected("댓글 작성 권한이 없습니다: ...")` 등)가 legacy의 5개 예외 타입(IssueNotFound/PostingNotFound/PermissionDenied 등)과 동일한 사유를 사람이 읽을 수 있는 문자열로 이미 구분해 담고 있고, yona 어디에도 이 사유별 `catch`/`instanceof` 타입 분기가 없음(legacy도 마찬가지로 타입 기반 분기가 존재한 적이 없음, 호출부는 항상 예외 발생 여부만 사용). 5개 checked exception 서브클래스를 그대로 이식하면 오히려 Kotlin sealed class 패턴과 중복되는 과설계 — 구조 차이는 있으나 기능 동등, CLOSED

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
