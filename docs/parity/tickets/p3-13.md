---
id: p3-13
type: ticket
status: open
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:283 (표, P3 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P3-13: 프런트엔드 분리 (React/Vue3/Angular 등 SPA) (2026-08-31 사용자 제안, 계획서만 작성 — 코드 0줄)

## 원본 표 정보

- **우선순위 구분**: P3
- **상태(원본 체크박스)**: `[ ]`
- **비고(원문)**: [원본에 있던 추가 열(섹션 헤더는 3열인데 이 행은 4열)]
  - (원본 열 3) yona 동치성과 무관
  
  계획서: [`docs/yona-wiki/plans/p3-13-decoupled-spa-frontend.md`](yona-wiki/plans/p3-13-decoupled-spa-frontend.md). 현재 `web/` 패키지에 `@RestController`(JSON) 31개, `@Controller`(Thymeleaf HTML) 31개, 템플릿 159개 존재(코드로 확인) — JSON API 자체는 있지만 대부분 개별 기능의 폼 제출/액션 엔드포인트라 "화면을 SPA로 그리는 데 필요한 데이터를 한 번에 주는 read API"로는 설계돼 있지 않음. [[tickets/p3-02|P3-02]](CLI+REST API)가 만들고 있는 `/api/v1/projects/{owner}/{project}/{resource}` 네임스페이스+Fine-grained PAT 인증이 가장 직접적인 토대이나 현재 이슈/PR/프로젝트 세 리소스만 커버. 인증은 `SecurityConfig.kt`가 CSRF를 아예 비활성화해두고 폼 로그인(세션 쿠키)+HTTP Basic을 쓰는 상태 — SPA가 이 세션 쿠키 방식을 그대로 쓸지 [[tickets/p3-02|P3-02]] 토큰 체계로 통일할지 미결정. 프레임워크(React/Vue3/Angular)도 미결정. 진행 방향: 화면 단위 점진 전환(파일럿 화면 1개로 시작) 권장, 159개 템플릿 전체 동시 전환은 리스크가 커 비권장

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
