---
id: p1-95
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:148 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-95: **(2026-08-20 [[tickets/p2-15|P2-15]] 확정 조사에서 발견)** `MilestoneController.isProjectManager`가 마일스톤 생성/수정/삭제(3곳)를 매니저 전용으로 제한 — yona는 프로젝트 멤버 전원 허용. **`config/security/AccessControl.kt`에 이미 정확한 `isAllowedToUpdateMilestone()`(멤버 전원 허용)가 존재하는데 이 컨트롤러가 쓰지 않고 있음**

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/controllers/MilestoneApp.java`(전 액션 `@IsAllowed(UPDATE/DELETE, MILESTONE)`) → `app/utils/AccessControl.java:280-282`
- **yona 대상**: `web/MilestoneController.kt`(`isProjectManager`) vs `config/security/AccessControl.kt`(`isAllowedToUpdateMilestone()`, 미사용)
- **비고(원문)**: 2026-08-20 [[tickets/p2-15|P2-15]] 조사에서 확정. **완료** — 아래 완료 로그 참고. **범위 정정**: legacy를 다시 대조해보니 생성(`newMilestone`)은 `@IsCreatable`이라 `isProjectResourceCreatable()`로 분리했고, 기존에 존재하던 `isAllowedToUpdateMilestone()`은 `isAllowedIfGroupMember` 분기가 빠져 있어(legacy UPDATE는 `isMemberOf || isAllowedIfGroupMember`) 이 항목이 권장한 그 헬퍼 대신 [[tickets/p1-85|P1-85]] 1b의 더 정확한 `isAllowed(user, project, milestone, Operation)`을 사용

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
