---
id: p2-58
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:259 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-58: **(2026-08-28 legacy `-_-api/v1` Open API 전수 대조에서 발견)** 마일스톤 생성 Open API가 legacy `owner/projectName` 기반 경로 대신 `projectId` 기반으로만 존재

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/controllers/api/MilestoneApi.java`(`newMilestone`, `POST /-_-api/v1/owners/:owner/projects/:projectName/milestones`)
- **yona 대상**: `web/MilestoneController.kt`(`POST /api/projects/{projectId}/milestones`)
- **비고(원문)**: **완료** — `createMilestoneNode`/`parseDueOn`을 `MilestoneController.kt`의 top-level `internal fun`으로 추출(로직 중복 제거) 후 신규 `web/MilestoneApiController.kt`에서 재사용(`MilestoneApiControllerSpec.kt`). 클래스 레벨 `@RequestMapping`이 있는 컨트롤러에 legacy 절대경로를 직접 추가하면 경로가 연결돼 버리는 함정을 이 항목에서 발견하고 정정

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
