---
id: p2-56
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:257 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-56: **(2026-08-28 legacy `-_-api/v1` Open API 전수 대조에서 발견)** 이슈 Open API 전체가 legacy `owner/projectName` 기반 경로(`/-_-api/v1/owners/:owner/projects/:projectName/issues/...`) 대신 내부 `projectId` 기반 `/api/projects/{projectId}/issues/...`로만 존재 — 외부 연동 클라이언트가 legacy 경로로 호출하면 전부 404

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **비고(원문)**: [원본에 있던 추가 열(섹션 헤더는 3열인데 이 행은 6열)]
  - (원본 열 3) `app/controllers/api/IssueApi.java`(`newIssues`/`getIssue`/`updateIssue`/`updateIssueState`/`updateIssueContent`/`newIssueComment`/`updateIssueComment`/`updateIssueLabel`/`upvoteWeight`/`downvoteWeight`, 총 10개 라우트)
  - (원본 열 4) `web/IssueApiController.kt`(신규, 8개 라우트), `web/CommentController.kt`(신규 별칭 2개)
  - (원본 열 5) **완료** — `IssueController`/`CommentController`가 이슈 조회·상태변경·본문수정·투표·라벨 로직을 이미 서비스 레이어(`IssueService`)로 갖고 있어 그대로 재사용, 요청/응답 DTO만 legacy JSON 필드명(title/body/milestoneTitle/assignees[].loginId/labels[].labelName/content·sha1/comment 등)에 맞춰 신규 작성. **구현 중 발견한 중요한 버그**: `sha1` 필드 비교에 yona 내부용 `isModifiedByOthers()`(원문 대 원문, 양쪽 재해시)를 그대로 재사용하면 legacy 클라이언트(이미 SHA-1로 해시된 값을 `sha1` 필드로 보냄)와 호환되지 않아 항상 409가 남 — legacy와 동일하게 서버측 현재값만 해싱해 클라이언트 값과 그대로 비교하는 `isModifiedByOthersLegacyChecksum()`(`domain/support/ChecksumUtils.kt` 신규)을 만들어 대체. **2026-08-28 추가 복원(사용자 지시)**: 최초 구현에서 단순화했던 두 항목을 마저 이식 — (1) `IssueService.createIssue()`에 `explicitNumber` 파라미터 추가, legacy `AbstractPosting.saveWithNumber()`와 동일하게 `project.lastIssueNumber` 카운터를 건드리지 않고 지정된 번호를 그대로 씀(마이그레이션으로 과거 이슈 번호 보존) (2) `sendNotification` 파라미터 추가, `false`면 `isDraft` 여부와 무관하게 신규 이슈 알림을 발행하지 않음. 기존 호출부(`IssueController`/`IssueViewController` 등)는 두 파라미터 모두 기본값(`null`/`true`)을 그대로 쓰므로 회귀 없음. `IssueServiceSpec.kt`(실제 DB 통합 테스트)에 두 시나리오 모두 추가 검증
  
  `IssueApiControllerSpec.kt`/`CommentControllerSpec.kt`/`IssueServiceSpec.kt`에 테스트 추가

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
