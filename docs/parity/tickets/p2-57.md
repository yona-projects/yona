---
id: p2-57
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:258 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-57: **(2026-08-28 legacy `-_-api/v1` Open API 전수 대조에서 발견)** 게시글(Board) Open API가 legacy `owner/projectName` 기반 경로 대신 `projectId` 기반으로만 존재

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **비고(원문)**: [원본에 있던 추가 열(섹션 헤더는 3열인데 이 행은 6열)]
  - (원본 열 3) `app/controllers/api/BoardApi.java`(`newPostings`/`updatePostingContent`/`newPostingComment`/`updatePostingComment`/`updatePostLabel`, 총 5개 라우트)
  - (원본 열 4) `web/BoardApiController.kt`(신규, 3개 라우트), `web/CommentController.kt`(신규 별칭 2개)
  - (원본 열 5) **완료** — [[tickets/p2-56|P2-56]]과 동일 패턴(`PostingService`/리포지토리 재사용 + legacy 필드명 DTO, `isModifiedByOthersLegacyChecksum()` 재사용). **2026-08-28 추가 복원(사용자 지시)**: `PostingService.createPosting()`에 `explicitNumber` 파라미터 추가, legacy `saveWithNumber()`와 동일하게 `project.lastPostingNumber` 카운터를 건드리지 않고 지정된 번호를 그대로 씀. legacy `BoardApi.newPostings()`에는 `sendNotification` 플래그 자체가 없어(이슈 쪽에만 존재) 이 항목은 해당 없음. 기존 호출부는 기본값(`null`)을 그대로 쓰므로 회귀 없음. `PostingServiceSpec.kt`(실제 DB 통합 테스트)에 시나리오 추가 검증
  
  `BoardApiControllerSpec.kt`/`CommentControllerSpec.kt`/`PostingServiceSpec.kt`에 테스트 추가

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
