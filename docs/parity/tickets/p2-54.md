---
id: p2-54
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:255 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-54: **(2026-08-28 legacy `-_-api/v1` Open API 전수 대조에서 발견)** `GlobalApi.hello()` 헬스체크 엔드포인트 미이식

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **비고(원문)**: [원본에 있던 추가 열(섹션 헤더는 3열인데 이 행은 4열)]
  - (원본 열 3) `app/controllers/api/GlobalApi.java`(`GET /-_-api/v1/hello`) — yona에 대응 엔드포인트 0건(`web/` 전수 검색 확인). `/actuator/health`로 대체 가능하나 legacy 클라이언트가 이 경로로 직접 호출하면 404
  
  **완료** — `web/GlobalApiController.kt` 신규(`GlobalApiControllerSpec.kt`), legacy와 동일한 `{message:"I'm alive!", ok:true}` 응답

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
