---
id: p2-55
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:256 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-55: **(2026-08-28 legacy `-_-api/v1` Open API 전수 대조에서 발견, 2026-08-28 구현 중 실제 의미 정정)** `IssueApi.imports()` 미이식 — **최초 등록 시 "이슈 벌크 생성 API"로 잘못 서술함, 실제로는 게시글(Posting) 1건을 이슈로 전환하는 기능**(`postNumber` 쿼리파라미터로 지정한 게시글을 댓글/첨부파일까지 옮겨 이슈로 만들고 원본 게시글은 삭제)

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **비고(원문)**: [원본에 있던 추가 열(섹션 헤더는 3열인데 이 행은 4열)]
  - (원본 열 3) `app/controllers/api/IssueApi.java`(`POST /-_-api/v1/owners/:owner/projects/:projectName/issues/imports`, `imports()` — `Issue.from(posting)`/`IssueComment.from(postingComment, issue)`로 필드 복사, 댓글 2단계(최상위→답글) 이관, 첨부파일 컨테이너 재배정, `removePosting()`)
  
  **완료** — `web/IssueApiController.kt` 신규(`IssueApiControllerSpec.kt`). `AttachmentService.moveAll()`(캐시 evict 포함)로 첨부파일 이관, 쓰기 권한 없으면 403, 대상 게시글 없으면 400

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
