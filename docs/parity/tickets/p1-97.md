---
id: p1-97
type: ticket
status: done
created: 2026-08-20
updated: 2026-08-20
relates_to: []
source: docs/PARITY_BACKLOG.md:150 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-97: **(2026-08-20 [[tickets/p2-16|P2-16]] 확정 조사에서 발견)** 브랜치 삭제가 프로젝트 멤버 전원에게 허용됨(과잉 허용) — yona는 PROJECT 리소스의 DELETE를 매니저 또는 조직관리자로 제한

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/controllers/BranchApp.java:71-72 @IsAllowed(Operation.DELETE)`(resourceType 기본값 PROJECT) → `app/utils/AccessControl.java:188-197`(PROJECT DELETE: `ProjectUser.isManager || OrganizationUser.isAdmin`만)
- **yona 대상**: `web/BranchApiController.kt`(`deleteBranch` — 로그인+프로젝트 멤버십만 확인, 매니저 제한 없음)
- **비고(원문)**: 2026-08-20 [[tickets/p2-16|P2-16]] 조사에서 발견. yona 대비 느슨한(과잉 허용) 방향의 결손 — 일반 멤버가 임의로 브랜치 삭제 가능. **완료** — 아래 완료 로그 참고

## 완료 로그 (원문, `PARITY_BACKLOG.md`의 "완료 로그" 섹션에서 이관)

- **2026-08-20 — P1-97**: `BranchApiController.deleteBranch`가 로그인+프로젝트 멤버십만 확인해 일반 멤버도 브랜치를 삭제할 수 있던 과잉 허용 버그 수정. legacy `BranchApp.java:71-72`의 `@IsAllowed(Operation.DELETE)`(resourceType 기본값 PROJECT)는 [[tickets/p1-87|P1-87]]에서 이미 확인한 것과 동일하게 `isGlobalResourceAllowed()`의 PROJECT 케이스(매니저/조직관리자 전용)를 타므로, `accessControl.isAllowed(loginUser, project, Operation.DELETE)`([[tickets/p1-85|P1-85]] 1b)로 교체.
  - 테스트: `BranchApiControllerSpec.kt`의 기존 "성공 시 302" 테스트를 매니저 계정으로 고치고(기존엔 멤버십 스텁만 있고 역할이 없었음), 매니저가 아닌 일반 멤버는 403(뷰 이름 `error/403`이라 HTTP 상태 자체는 200 OK로 렌더링됨)이 되는 신규 테스트 추가 — 기존 3건 포함 총 4 tests.
  - 검증: `./gradlew test --tests "BranchApiControllerSpec"` 4 tests 전체 통과, `./gradlew compileKotlin compileTestKotlin` 전체 컴파일 성공.
