---
id: p1-07
type: ticket
status: done
created: 2026-08-19
updated: 2026-08-19
relates_to: []
source: docs/PARITY_BACKLOG.md:60 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-07: 이슈 타임라인(IssueEvent) 부재

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `models/IssueEvent.java`
- **yona 대상**: `domain/issue/IssueServiceImpl.kt`, `web/IssueController.kt`
- **비고(원문)**: **완료** — 당시 제외했던 라벨/본문/이동/공유자 변경 기록과 draft-time 병합 최적화는 [[tickets/p1-37|P1-37]]/[[tickets/p1-38|P1-38]]에서 완료(아래 참고)

## 완료 로그 (원문, `PARITY_BACKLOG.md`의 "완료 로그" 섹션에서 이관)

- **2026-08-19 — P1-07**: 이슈 상태/담당자/마일스톤이 바뀌어도 변경 이력이 어디에도 남지 않던 문제 해결(엔티티 자체는 [[tickets/p1-06|P1-06]]에서 신설). `IssueServiceImpl.changeState/changeAssignee/changeMilestone`이 이미 만들고 있던 `NotificationEvent`(알림용, oldValue/newValue 포함)와 같은 데이터로 `IssueEvent`(이력용)도 함께 저장하도록 `recordIssueEvent()` 공통 헬퍼 추가. `IssueController`에 `GET /api/projects/{projectId}/issues/{number}/timeline` 조회 API 추가(yona `Issue.getTimeline()` 대응).
  - **발견 후 별도 등록**: 라벨/본문/이슈이동/공유자 변경 기록과 yona의 30초 draft-time 병합/취소 최적화(연속 변경 시 잡음 감소)는 이번 패스에서 다루지 않음 — **[[tickets/p1-37|P1-37]]/[[tickets/p1-38|P1-38]]로 별도 등록**, 이후 둘 다 완료됨(`[x]`).
  - 테스트: `IssueServiceSpec.kt`(실제 MariaDB 통합테스트) 기존 1건 확장 + 신규 2건(담당자/마일스톤 변경 시 IssueEvent 생성) = 총 5 tests. `IssueControllerSpec.kt` +2(타임라인 조회 성공/404). 전체 컴파일 확인.
