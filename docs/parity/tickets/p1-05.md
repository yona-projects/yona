---
id: p1-05
type: ticket
status: done
created: 2026-08-19
updated: 2026-08-19
relates_to: []
source: docs/PARITY_BACKLOG.md:58 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-05: Related-PR 재병합 로직 스텁

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `RelatedPullRequestMergingActor.java`
- **yona 대상**: `domain/event/PullRequestMergeEventListener.kt`
- **비고(원문)**: **완료** — 당시 제외했던 PR 타임라인 커밋 이벤트 기록 등 5가지 결손은 [[tickets/p1-52|P1-52]]에서 재검증돼 완료(아래 참고)

## 완료 로그 (원문, `PARITY_BACKLOG.md`의 "완료 로그" 섹션에서 이관)

- **2026-08-19 — P1-05**: `handleRelatedPullRequestMergeEvent`가 `isMerging=true`만 세팅하고 실제 재병합/충돌 재검사를 전혀 하지 않아, 관련 브랜치에 push가 일어나도 PR 상태가 "병합중"에 영구히 멈춰있던 문제 수정.
  - 이제 관련 PR마다 이미 존재하던 `PullRequestService.attemptMerge()`(dry-run 병합, `isConflict` 갱신)를 실제로 호출해 재검사하고, 처리 후 `isMerging`을 다시 `false`로 되돌린다(예외가 나도 마찬가지 — `attemptMerge` 실패가 PR을 "병합중" 상태로 영구 고정시키지 않도록).
  - 충돌 상태가 실제로 바뀐 경우(없음→발생, 발생→해소)에만 `NotificationEvent`를 발행 — [[tickets/p0-01|P0-01]](알림 메일)·[[tickets/p0-03|P0-03]](웹훅)이 이미 이 이벤트를 구독하고 있어 별도 배선 없이 자동으로 메일/웹훅까지 나간다.
  - **발견 후 별도 등록**: yona `PullRequestActor.processPullRequestMerging`의 "새 커밋이 추가되면 `PullRequestEvent` 타임라인에 커밋 이벤트 추가" 부분은 `PullRequestEvent`([[tickets/p1-08|P1-08]]) 부재로 이번 패스에서 다루지 않고 충돌 상태 변화 알림까지만 구현 — 이후 [[tickets/p1-08|P1-08]] 완료 후 **[[tickets/p1-52|P1-52]]에서 재검증돼 완료됨**(`[x]`, 아래 [[tickets/p1-52|P1-52]] 완료 로그 참고).
  - 테스트: `PullRequestMergeEventListenerSpec.kt`(신규) 5 tests(재검사 후 isMerging 복구, 충돌 발생/해소 시 알림 발행, 상태 변화 없으면 미발행, 예외 발생해도 isMerging 복구). 기존 `PullRequestServiceSpec.kt`(실제 MariaDB+git) 재실행으로 회귀 없음 확인. 커버리지 INSTRUCTION 86%(449/520, 파일 전체 — 기존 메서드 포함).
