---
id: p0-13
type: ticket
status: done
created: 2026-08-19
updated: 2026-08-19
relates_to: []
source: docs/PARITY_BACKLOG.md:35 (표, P0 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P0-13: LOCKED/DELETED 계정 로그인 차단 안 됨

## 원본 표 정보

- **우선순위 구분**: P0
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `UserApp.java:207-233`
- **yona 대상**: `config/YonaAuthenticationProvider.kt`
- **비고(원문)**: **완료** — `YonaAuthenticationProviderSpec.kt`, 94%/100% (명령어/분기) 커버리지

## 완료 로그 (원문, `PARITY_BACKLOG.md`의 "완료 로그" 섹션에서 이관)

- **2026-08-19 — P0-13**: `YonaAuthenticationProvider`가 계정 상태(LOCKED/DELETED)를 확인하지 않고 비밀번호만 검증하던 문제 수정. `YonaUserDetails`에 `state` 필드 추가(`isAccountNonLocked`/`isEnabled`가 실제 상태 반영), `UserDetailsServiceImpl`이 `user.state` 전달, `YonaAuthenticationProvider.authenticate()`가 사전 검사로 `LockedException`/`DisabledException` 발생. 테스트: `config/YonaAuthenticationProviderSpec.kt` (5 tests, all pass — 이후 [[tickets/p1-01|P1-01]]에서 LDAP 분기 테스트 5개가 같은 파일에 추가돼 현재는 총 10 tests). 커버리지: `YonaAuthenticationProvider` INSTRUCTION 94%(111/118) · BRANCH 100%(8/8), `YonaUserDetails` BRANCH 100%(4/4).
