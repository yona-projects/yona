---
id: p1-107
type: ticket
status: done
created: 2026-08-21
updated: 2026-08-21
relates_to: []
source: docs/PARITY_BACKLOG.md:160 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-107: **(2026-08-21 [[tickets/p1-102|P1-102]] 구현 중 부수 발견)** 게시글(Posting)에도 이슈와 동일한 동시편집 충돌 감지(`isModifiedByOthers`/`conflicted`) 패턴이 원본에 있으나 yona에는 이식돼 있지 않음 — ①게시글 본문 인라인 수정용 경량 PATCH 엔드포인트 자체가 없음(`BoardController.updatePosting`은 title+body+notice+readme 전체를 갈아치우는 별도의 PUT 풀업데이트라 대응 아님), ②게시글 댓글 수정(`CommentController.updatePostingComment`)은 존재하지만 충돌검사가 전혀 없음(이슈 댓글은 [[tickets/p1-102|P1-102]]에서 이미 추가)

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/controllers/api/BoardApi.java:128-159 updatePostingContent()`(`PATCH .../posts/:number/content`, `isModifiedByOthers(posting.body, original)` 불일치 시 `conflicted(posting.body)`→409), `:198-238 updatePostingComment()`(`PATCH .../post/:number/comment/:commentId`, `isModifiedByOthers(postingComment.contents, original)` 동일 패턴)
- **yona 대상**: `web/BoardController.kt`(`updatePostingContent` 신규), `web/CommentController.kt`(`updatePostingComment`)
- **비고(원문)**: **완료** — 아래 완료 로그 참고

## 완료 로그 (원문, `PARITY_BACKLOG.md`의 "완료 로그" 섹션에서 이관)

- **2026-08-21 — P1-107**: 게시글(Posting) 동시편집 충돌 감지 이식(`updatePostingContent` 신규, `updatePostingComment` 충돌검사 추가).
  - Serena LSP로 yona `BoardApi.java:128-159 updatePostingContent()`/`:198-238 updatePostingComment()`를 재확인 — 두 곳 모두 **권한 확인(`AccessControl.isAllowed(..., UPDATE)`)이 충돌 검사(`isModifiedByOthers`)보다 먼저** 실행됨을 확인([[tickets/p1-102|P1-102]]의 이슈 쪽 `updateIssueContent`는 반대 순서였는데, 이번엔 게시글 원본을 정확히 따라 권한 확인을 먼저 배치).
  - `BoardController.kt`에 `PATCH /{postId}/content` 신규(`updatePostingContent`) — `postingRepository`로 본문만 직접 수정·저장(기존 `updatePosting`처럼 title/notice/readme까지 갈아치우지 않음), `isModifiedByOthers`([[tickets/p1-102|P1-102]]에서 만든 `domain/support/ChecksumUtils.kt` 공용 함수 재사용) 불일치 시 409.
  - `CommentController.kt`의 기존 `updatePostingComment`(`PUT .../posts/{number}/comments/{commentId}`)에 `updateIssueComment`([[tickets/p1-102|P1-102]])와 동일한 패턴으로 충돌검사 추가 — `CommentRequest.original`(이미 [[tickets/p1-102|P1-102]]에서 이슈/게시글 댓글 공용 DTO에 추가돼 있어 별도 필드 추가 불필요)이 있을 때만 검사, 반환 타입을 `ResponseEntity<PostingComment>`→`ResponseEntity<Any>`로 변경.
  - 테스트: `BoardControllerSpec.kt` +2(원문 일치 시 정상 갱신/불일치 시 409+storedContent+미저장 검증 — 두 테스트 모두 공유 `posting` fixture를 직접 mutate하는 컨트롤러 특성상 격리된 별도 인스턴스 사용, [[tickets/p1-102|P1-102]]에서 겪은 fixture 공유 버그를 사전에 회피), `CommentControllerSpec.kt` +2(original 일치/불일치).
  - 검증: `./gradlew test --tests "...BoardControllerSpec" --tests "...CommentControllerSpec"` 전체 통과(24 tests), 이어서 `./gradlew test` 전체 1016 tests 통과(회귀 없음 확인).
