---
id: p3-55
type: ticket
status: done
created: 2026-09-11
completed: 2026-09-11
relates_to: [p3-46]
source: P3-46 CM6-7단계(16개 markdownEditor 화면 전면 회귀) 중 `common/reviewForm.html`
  (PR "Changes" 탭)을 Playwright로 검증하다가 발견 — 화면 자체는 P3-46 범위지만 원인은
  마크다운 에디터와 무관한 훨씬 오래된 UI 배선 문제라 별도 티켓으로 분리
---

# P3-55: PR "Changes" 탭의 코드리뷰 댓글 UX가 두 갈래로 쪼개져 있고, 그중 라인 댓글 쪽은
마크다운 에디터가 아예 안 붙어있음

## 배경

`pullrequest/view.html`의 "Changes" 탭(`/{owner}/{project}/pull/{number}/changes`)에는
코드리뷰 댓글을 작성하는 경로가 실질적으로 두 갈래가 있다.

1. **`common/reviewForm.html`(`#review-form`)** — 프로젝트당 하나, "범위 없는(코드 전체 대상)"
   리뷰 댓글을 시작하는 폼. `<yona-markdown-editor>`가 정상적으로 붙어있고 CM6
   렌더링/타이핑/툴바 9개/멘션 3트리거/미리보기 전부 정상 동작함을 이번에 Playwright로 직접
   확인했다. **다만 이 폼은 기본적으로 `display:none`이고, 이걸 보이게 만드는 트리거
   (`yobi.CodeCommentBox.js`의 `.show()`)를 호출하는 코드가 이 페이지 어디에도 없다** —
   `yobi.code.Diff.js`(원래 이 트리거를 담당하는 스크립트)가 어느 템플릿에서도
   `<script src>`로 로드되지 않는다(이 세션에서 반복 확인된 "소스는 있지만 로드가 안 되는"
   패턴과 동일 종류). 즉 `#review-form`은 현재 UI 어디서도 사용자가 열 수 없는 죽은 화면
   요소다.
2. **라인별(ranged) 댓글** — `pullrequest/view.html`의 인라인 `<script>`가 diff 라인의
   `.add-comment-btn-cell` 클릭을 자체적으로 가로채, `common/reviewForm.html`/
   `<yona-markdown-editor>`를 전혀 쓰지 않고 순수 문자열 결합으로 `<textarea name="contents">`
   폼을 즉석에서 DOM에 삽입한다(`pullrequest/view.html` "Changes" 탭 인라인 스크립트,
   `formHtml = '<tr>...<textarea name="contents" ...></textarea>...'` 부분). 서버 제출
   자체(`ReviewViewController.newPullRequestComment`)는 정상 동작하지만, 사용자는 이 경로로는
   툴바/멘션(`@`/`:`/`#`)/서버 렌더링 미리보기를 전혀 못 쓴다 — P3-46이 다른 15개 화면에서
   구축한 CM6 에디터 경험이 이 경로에만 빠져 있다.

두 경로 모두 최종적으로 `ReviewViewController`의 같은 두 엔드포인트
(`POST /{owner}/{project}/pullRequest/{id}/comments`,
`POST /{owner}/{project}/commit/{commitId}/comments`)로 제출되므로 서버 측 로직은
문제없다 — 순전히 화면(뷰) 배선의 문제다.

## 확인된 사실 (이번 세션에서 직접 재현)

- `#review-form`을 Playwright로 강제로 `style.display='block'`시켜서 확인한 결과, 안의
  `<yona-markdown-editor>`는 렌더링/타이핑/light DOM textarea 동기화/툴바 9개 전부/미리보기/
  멘션(`@`/`:`/`#`) 3트리거 전부 정상 동작한다 — **컴포넌트 자체는 문제 없음**, 순수하게
  "아무도 이 폼을 보여주지 않는다"는 배선 문제.
- `grep -rl "yobi.code.Diff.js" src/main/resources/templates`가 0건 — 로드하는 템플릿이
  아예 없다.
- 라인별 댓글 삽입 폼(`pullrequest/view.html` 인라인 스크립트)은 실제로 diff 라인을 클릭하면
  뜨고, 제출도 서버에 정상 도달한다(302) — 다만 정말 순수 `<textarea>`만 있고
  `<yona-markdown-editor>`/툴바/멘션/미리보기가 전혀 없다.

## 제안하는 다음 단계 (설계 판단 필요 — 착수 전 코디네이터 확인)

세 가지 방향이 있고 어느 쪽이 맞는지는 제품 의도에 대한 판단이 필요해 보여 백로그로만
등록한다:

1. **`#review-form`을 완전히 제거**하고, 라인별 댓글 경로만 유지하되 그 경로에도
   `<yona-markdown-editor>`를 붙인다(현재 죽어있는 "범위 없는 리뷰 댓글 시작" 기능 자체를
   포기하는 방향 — GitHub 스타일과는 다름).
2. **`#review-form`을 실제로 살린다** — `.add-comment-btn-cell` 클릭 시(또는 별도의
   "일반 댓글 추가" 버튼을 새로 만들어) `yobi.CodeCommentBox.show()`를 호출하도록 배선하고,
   라인별 댓글은 계속 지금의 인라인 삽입 방식을 쓰되 그 안의 `<textarea>`도
   `<yona-markdown-editor>`로 교체한다(두 경로 모두 CM6 에디터를 쓰게 통일).
3. 라인별 댓글 폼을 서버 렌더링(Thymeleaf fragment 재사용, `pullrequest/partial_comment_form_on_thread.html`처럼)으로 바꿔 애초에 클라이언트 문자열 조립을 없애고, 그 김에
   `<yona-markdown-editor>`를 자연스럽게 붙인다(가장 아키텍처적으로 깔끔하지만 작업량 최대).

착수 시 TDD로 "라인 댓글 폼에 CM6 에디터가 있어야 한다"는 회귀 스펙을 먼저 세우고 진행할 것.

## 완료 로그 (2026-09-11)

**1차 시도(방향 2 선택 후 되돌림)**: 사용자가 처음에는 "제안하는 다음 단계"의 2번 방향
(`#review-form`을 실제로 살리기)을 승인해 착수했다. `.add-comment-btn-cell` 옆에 별도
"일반 댓글 추가" 트리거 버튼(`#btn-add-review-comment`)을 만들고, `yobi.CodeCommentBox.js`를
처음으로 실제 로드해 `.show()`를 인자 없이 호출하는 새 fallback 분기(welTarget/thread-id가
없으면 다른 곳으로 옮기지 않고 원래 자리에 표시)를 추가해 구현을 끝내고 Playwright로 동작까지
확인했다. 그 과정에서 순수 버그도 하나 발견/수정: `Node.appendChild()`가 "이미 같은 부모의
마지막 자식"이어도 내부적으로 disconnect 후 reconnect로 처리되어, `<yona-markdown-editor>`의
`connectedCallback`이 `if(this.shadowRoot)return`으로 재초기화를 막는 바람에 CM6 뷰가
destroy된 채 재마운트되지 않는 회귀를 재현해 고쳤다(`_returnToInitialParent()`를 실제로 이동이
필요할 때만 동작하도록 수정).

**방향 전환**: 코디네이터가 legacy(v1.6, `/home/jiho/yona-convert/legacy-yona`) 원본
(`code/diff.scala.html`, `git/viewChanges.scala.html`, `common/reviewForm.scala.html`,
`common/commentForm.scala.html`)을 직접 대조 조사한 결과, `#review-form`은 legacy에서도
"범위 없는 일반 댓글 작성" 폼이 아니라 `yobi.CodeCommentBox.js`(legacy `yobi.code.Diff.js`)가
diff 라인 선택/스레드 답글 시 동적으로 다른 위치로 옮겨 재사용하던 별개의 팝오버 템플릿이었고,
legacy에서 "범위 없는 일반 댓글 작성"을 실제로 담당한 것은 `board-comment-wrap` 안
`non-ranged-threads-wrap` 바로 다음에 위치한 **완전히 별도 파일** `common.commentForm(container,
resourceType, action)`(`id="comment-form"`, legacy CSS에 `display:none` 없음 - 항상 보임)이었다.
그런데 yona 포팅 과정에서 `commentForm`에 대응하는 파일 자체가 통째로 누락되고 그 빈자리를
`reviewForm`이 대신 차지하고 있었던 것으로 드러났다. 사용자 결정: "레거시 동치 우선" — 1차
시도(방향 2)를 되돌리고 legacy와 동일한 구조로 다시 구현.

**최종 구현**:
- `pullrequest/view.html`에 추가했던 `#btn-add-review-comment` 트리거/배선 스크립트, `#review-form`
  포함 블록을 되돌렸다.
- `common/reviewForm.html`, `src/main/resources/static/javascripts/common/yobi.CodeCommentBox.js`를
  완전히 삭제했다(`grep -rln "common/reviewForm" src/main/resources/templates/`로 `pullrequest/
  view.html` 외 다른 호출부가 없음을 코디네이터가 먼저 확인 — 삭제해도 전체 코드베이스에서
  안 쓰이는 파일이 됨. `code/diff.html`은 이미 P3-52에서 별도 vanilla JS 패턴으로 완전히 대체돼
  이 파일/스크립트를 안 씀).
- **신규**: `common/commentForm.html`(legacy `commentForm.scala.html` 대응) 작성 — legacy와
  동일하게 `non-ranged-threads-wrap` 바로 다음, `board-comment-wrap` 안에 배치. `common.editor(...)`
  자리는 `site/layout::markdownEditor` 프래그먼트(`<yona-markdown-editor>`, editor-mode는 같은
  화면의 다른 REVIEW_COMMENT 폼들과 일관되게 `code-review-body`로 통일 - legacy 원문
  "comment-body"에서 의도적으로 변경, 순수 CSS/속성 스코프 문자열이라 기능 차이 없음),
  `common.fileUploader(...)` 자리는 `common/uploadForm::uploadForm` 프래그먼트로 재현. 작성 권한
  없음일 때의 로그인 필요 placeholder는 `common/child_commentForm.html`과 동일한 기존 패턴을
  재사용. 제출 액션은 기존 `ReviewViewController.newPullRequestComment`
  (`POST /{owner}/{projectName}/pullRequest/{pullRequestId}/comments`)를 그대로 재사용 —
  `codeRangeReq` 없이 제출하면 `CodeReviewServiceImpl.createReviewComment()`가 `codeRange`/
  `startLine`이 null일 때 `NonRangedCodeCommentThread`를 만드는 것을 코드 검토와 Playwright
  실측으로 확인했다.
  - 구현 중 발견한 순수 버그: `commitId`를 액션 URL에 `@{...(commitId=${commitId})}`로 직접
    구웠더니, `commitId`가 null(변경사항 탭 "전체" 보기 기본값)이어도 Thymeleaf가 `?commitId=`를
    그대로 렌더링해 서버가 빈 문자열로 바인딩하는 바람에 `CodeReviewServiceImpl`의 "commitId
    없으면 PR 병합 커밋으로 대체" 폴백이 깨지는 것을 신규 스펙 실행 중 재현 — 라인별 댓글 폼이
    이미 쓰던 방식대로 commitId가 있을 때만 hidden 필드를 추가하는 방식으로 고쳤다.
- 라인별 댓글 삽입 폼(`pullrequest/view.html` 인라인 `formHtml` 문자열 삽입)의 순수
  `<textarea>`는 계획대로 `<yona-markdown-editor>`로 교체했다(이견 없음, 그대로 유지).

**테스트**: TDD RED→GREEN 확인. `PullRequestReviewFormTriggerWiringSpec.kt`(1차 시도의 트리거
검증용, 기능 자체가 되돌려져 삭제)를 걷어내고 `PullRequestCommentFormWiringSpec.kt`(신규, 2
케이스 - 권한 있을 때 legacy와 동일 위치에 CM6 에디터가 붙은 `#comment-form` 렌더링 + reviewForm/
CodeCommentBox 잔재 없음, 권한 없을 때 placeholder만 노출)를 새로 추가했다.
`PullRequestInlineReviewCommentWiringTemplateRenderingSpec.kt`에 라인별 폼의 `<yona-markdown-
editor>` 마크업 계약 검증을 추가했다. 관련 스펙(`PullRequestCommentFormWiringSpec`(2),
`PullRequestInlineReviewCommentWiringTemplateRenderingSpec`(2), `PullRequestViewInlineScriptSyntaxSpec`
(1), `PullRequestViewControllerSpec`(87), `PullRequestViewControllerMoreSpec`(9),
`PullRequestReviewTemplateRenderingSpec`(3), `PullRequestDiffLineCommentUiFragmentRenderingSpec`
(1)) 전부 개별 재실행 GREEN. 전체 회귀(`./gradlew test`) 6605케이스 전부 GREEN(실패/에러 0).

Playwright로 실서버(h2 프로파일, 전용 포트 19470/데이터·git 디렉터리, 최초 관리자 계정 생성 →
GIT 프로젝트 생성 → main/feature 브랜치 실제 push → PR#1 생성)에서 완료 시점에 1회 종합 검증:
- `#comment-form-wrap`이 `.non-ranged-threads-wrap` 바로 다음에 항상 보이는(`display:none`
  아님) 상태로 렌더링됨을 확인.
- 그 안의 `<yona-markdown-editor>`가 Shadow DOM/CM6 뷰까지 정상 마운트(10개 버튼 - 툴바
  9개+미리보기 토글), `@` 멘션 트리거로 드롭다운 노출, light DOM textarea 실시간 동기화,
  미리보기 토글 시 서버 렌더링 마크다운(`<p>...</p>`) 표시까지 실측.
  - 이 과정에서 Playwright 스크립트 자체의 버그도 하나 발견/수정: 수동 좌표 계산
    (`page.mouse.click(x,y)`)이 화면 아래로 스크롤이 필요한 요소에서 포커스를 제대로 못 잡아
    `document.activeElement`가 `<body>`로 남는 바람에, 타이핑한 "h"가 CM6 에디터가 아니라
    페이지 전역으로 전달돼 `yona.KeyControl.js`의 전역 단축키("H" → 프로젝트 홈)가 오발동해
    페이지가 이동해버린 것 — Shadow DOM을 뚫는 Playwright Locator API(`.locator(...).locator(...)
    .click()`, 자동 스크롤+포커스)로 바꿔 해결. 실제 제품 버그는 아니었다(P3-46에서 이미
    `composedPath()`/`isContentEditable` 수정이 들어가 있었고 정상 동작 확인).
  - 폼 제출 → 302 리다이렉트 → 응답 본문의 `.non-ranged-threads-wrap` 구간 안에 방금 작성한
    댓글 텍스트가 실제로 나타남을 확인(`NonRangedCodeCommentThread`로 정상 저장/렌더링).
- 라인별 댓글 폼(`.add-comment-btn-cell` 클릭 → `<yona-markdown-editor>` 삽입)도 동일하게
  Shadow DOM 마운트/타이핑 동기화/제출 후 스레드 반영까지 실측.
- `#review-form`/`#btn-add-review-comment`/`window.yobi.CodeCommentBox`가 페이지 어디에도
  더 이상 존재하지 않음을 확인(삭제 회귀 가드).

커밋은 하지 않았다 — 코디네이터가 diff 검토 + 스펙 재실행으로 독립 검증한 뒤 커밋/푸시할 것.
