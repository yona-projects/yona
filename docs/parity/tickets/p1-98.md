---
id: p1-98
type: ticket
status: done
created: 2026-08-20
updated: 2026-08-20
relates_to: []
source: docs/PARITY_BACKLOG.md:151 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-98: **(2026-08-20 [[tickets/p2-15|P2-15]] 조사 중 부수 발견)** 프로젝트 멤버 제거 시 "프로젝트 오너는 제거할 수 없다"는 가드가 없음

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/controllers/ProjectApp.java:1015`(`deleteMember`에서 `project.isOwner(deleteMember)`면 forbidden)
- **yona 대상**: `web/ProjectMemberController.kt`(`removeMember` — 이 가드 없이 오너도 그대로 제거 가능해 보임, 재확인 필요)
- **비고(원문)**: 2026-08-20 [[tickets/p2-15|P2-15]] 조사 중 그룹 D(`isProjectManager`) 판정 과정에서 부수적으로 발견, 범위 밖이라 별도 등록만 함. **재확인 결과 이미 정확히 구현돼 있었음** — 아래 완료 로그 참고

## 완료 로그 (원문, `PARITY_BACKLOG.md`의 "완료 로그" 섹션에서 이관)

- **2026-08-20 — P1-98**: `ProjectMemberController.removeMember`(오너 제거 방지 가드 부재 의혹)를 재확인한 결과, **실제로는 이미 `ProjectUserServiceImpl.removeMember()`(210-222행)에 정확한 가드가 구현돼 있었다**(`project.owner == user.loginId`면 `IllegalArgumentException("Owner cannot leave or be removed from the project")`를 던지고 컨트롤러가 이를 잡아 400으로 응답) — 그리고 `ProjectUserServiceSpec.kt`의 기존 테스트 4번("멤버 삭제 및 소유자 탈퇴 금지 검증")이 이미 이 가드를 검증하고 있었다. 이 항목은 [[tickets/p1-15|P1-15]] 조사 중 "재확인 필요"로 잠정 등록됐던 것이 그 뒤 다른 작업([[tickets/p1-16|P1-16]] 근처)에서 이미 해결됐는데 백로그에 반영되지 않았던 것으로 보인다 — 코드/테스트 변경 없이 재검증만 하고 완료 처리.
  - 검증: `./gradlew test --tests "com.github.yonaprojects.yona.domain.project.ProjectUserServiceSpec"` 전체 통과(오너 제거 시도 시 `IllegalArgumentException` 발생 확인 포함), 전체 스위트 `./gradlew test` 946 tests 전체 통과(실패 0 — [[tickets/p1-96|P1-96]]/97/98 전부 반영 후).
