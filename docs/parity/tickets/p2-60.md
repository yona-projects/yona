---
id: p2-60
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:261 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-60: **(2026-08-28 legacy `-_-api/v1` Open API 전수 대조에서 발견)** 기본 로그인 페이지 설정 API 경로가 legacy와 다름

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/controllers/api/UserApp.java`(`setDefaultLoginPage`, `POST /-_-api/v1/user/defultLoginPage` — legacy 자체 오타 `defult`도 그대로 경로에 포함돼 있음)
- **yona 대상**: `web/UserController.kt`(`POST /user/setDefaultLoginPage`, [[tickets/p2-11|P2-11]] 기존 이식)
- **비고(원문)**: **완료** — `@PostMapping(value = [...])` 배열에 legacy 경로(오타 포함) 추가, 기존 경로 유지

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
