---
id: p2-59
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:260 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-59: **(2026-08-28 legacy `-_-api/v1` Open API 전수 대조에서 발견)** 프로젝트 생성/라벨생성/exports Open API 경로가 legacy `-_-api/v1/owners/...` 네임스페이스와 불일치(prefix 자체가 다름)

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/controllers/api/ProjectApi.java`(`newProject`/`newLabel`/`exports`) — `newProject`는 `POST /-_-api/v1/owners/:owner/projects`, `newLabel`은 `POST /-_-api/v1/owners/:owner/projects/:projectName/labels`, `exports`는 `GET /-_-api/v1/owners/:owner/projects/:projectName/exports`
- **yona 대상**: `web/ProjectApiController.kt`(`POST /api/projects/{owner}`, `GET /api/projects/{owner}/{projectName}/exports`), `web/ProjectController.kt`(`POST /api/{owner}/{projectName}/labels` — titleHeads/pushedBranches도 동일하게 `/api/{owner}/{projectName}/...` prefix라 legacy `/-_-api/v1/owners/{owner}/projects/{projectName}/...`와 세그먼트가 다름)
- **비고(원문)**: **완료** — 3개 컨트롤러 모두 클래스 레벨 `@RequestMapping`이 없어(`ProjectApiController`/`ProjectController`) `@PostMapping`/`@GetMapping`의 `value = [...]` 배열에 legacy 경로를 그대로 추가하는 것만으로 해결(핸들러 로직 재작성 불필요 — owner/projectName 경로변수 구조가 이미 legacy와 동일했음). titleHeads/pushedBranches는 Open API 목록에 없던 yona 자체 엔드포인트라 범위 제외 유지

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
