---
id: p2-47
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:248 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-47: **(2026-08-26 골든 체크 버킷C 사람 검토에서 발견, 2026-08-26 TDD로 완료)** README 파일 탐색 전용 헬퍼(README 후보 파일명 순회, 첫 발견 objectId 반환)에 대응하는 전용 구현이 yona에 없음 — `web/BoardViewController.kt`가 정확한 "README.md" 한 파일명만 조회해 `readme.md`/`README.markdown`/`readme.markdown`인 프로젝트는 폼이 빈 채로 열리는 실제 차이 확인

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/playRepository/BareRepository.java`(`readREADME`/`getFirstFoundREADMEfileObjectId`/`READMEFileNameFilter`)
- **yona 대상**: `web/BoardViewController.kt`(`readReadmeCandidate` 신규)
- **비고(원문)**: **완료** — legacy와 동일한 4개 후보(`README.md`→`readme.md`→`README.markdown`→`readme.markdown`)를 순서대로 시도하는 `readReadmeCandidate()` 추출, TDD로 "두 번째/세 번째 후보로 폴백" 테스트 2건 신규 추가 + 기존 "전부 실패" 테스트를 4개 후보 모두 예외 스텁으로 갱신, `BoardViewControllerSpec.kt` 전체 GREEN

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
