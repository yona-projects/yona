---
id: p1-08
type: ticket
status: done
created: 2026-08-19
updated: 2026-08-19
relates_to: []
source: docs/PARITY_BACKLOG.md:61 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-08: PR 타임라인(PullRequestEvent) 부재

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `models/PullRequestEvent.java`
- **yona 대상**: `domain/pullrequest/PullRequestEvent.kt`(신규), `PullRequestServiceImpl.kt`, `domain/event/PullRequestMergeEventListener.kt`, `web/PullRequestController.kt`
- **비고(원문)**: **완료** — 당시 제외했던 PR 생성/리뷰 상태변경 기록과 draft-time 병합 최적화는 [[tickets/p1-39|P1-39]]/[[tickets/p1-40|P1-40]]에서 완료(아래 참고)

## 완료 로그 (원문, `PARITY_BACKLOG.md`의 "완료 로그" 섹션에서 이관)

- **2026-08-19 — P1-08**: PR 상태 변경/병합/충돌 상태 전환이 일어나도 이력이 전혀 남지 않던 문제 해결(yona `models/PullRequestEvent.java` 대응). `IssueEvent`([[tickets/p1-07|P1-07]])와 동일한 패턴으로 신설.
  - `PullRequestEvent`(신규 엔티티+리포지토리) 추가.
  - `PullRequestServiceImpl.changeState()`가 그동안 **NotificationEvent조차 만들지 않던** 것을 확인 — `IssueServiceImpl.changeState`와 동일한 패턴으로 NotificationEvent 생성도 함께 추가(발견된 별도 격차를 자연스럽게 해소), 그 위에 PullRequestEvent 기록. 인터페이스에 `updaterLoginId` 파라미터 추가(컨트롤러 호출부도 함께 갱신).
  - `PullRequestMergeEventListener.handlePullRequestMergeEvent`(병합 완료 시 상태→MERGED)와 `notifyConflictStateChanged`([[tickets/p1-05|P1-05]]에서 만든 충돌/해소 알림)에도 PullRequestEvent 기록 추가.
  - `PullRequestController`에 `GET /api/projects/{projectId}/pullrequests/{number}/timeline` 조회 API 추가.
  - **발견 후 별도 등록**: PR 생성 시점(NEW_PULL_REQUEST) 알림과 코드리뷰 승인/반려의 PullRequestEvent 기록, yona의 30초 draft-time 병합/취소 최적화는 이번 패스에서 미포함 — **[[tickets/p1-39|P1-39]]/[[tickets/p1-40|P1-40]]으로 별도 등록**, 이후 둘 다 완료됨(`[x]`).
  - 테스트: `PullRequestServiceSpec.kt`(실제 MariaDB 통합테스트) +2(상태변경 시 이벤트 생성, 동일상태 변경시 미생성) = 총 11 tests. `PullRequestMergeEventListenerSpec.kt` +1(병합시 이벤트 생성), 기존 4건도 함께 재통과 = 총 6 tests. `PullRequestControllerSpec.kt` +1(타임라인 조회). 전체 컨텍스트 로딩 확인.
