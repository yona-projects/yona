---
id: p3-30
type: ticket
status: done
created: 2026-09-09
updated: 2026-09-09
relates_to: [p3-26, p3-28]
source: 사용자 지시로 코틀린 코드 전체를 P3-26/28과 같은 관점으로 재점검하다 코디네이터가 발견 (2026-09-09)
---

# P3-30: `IssueController`/`IssueApiController`/`BoardController`/`CommentController`/`MilestoneController`(24개 엔드포인트)에도 P3-26/28과 동일한 순환참조/비밀번호 노출

## 배경

P3-26(`pushedBranches`)/P3-28(`PullRequestController`)에서 고친 "raw 엔티티를 그대로
직렬화하면 `User.projectUsers`↔`ProjectUser.user` 양방향 관계가 순환 직렬화되며
`User.password`/`passwordSalt`까지 노출된다"는 정확히 같은 문제가 이슈/게시글/댓글/
마일스톤 관련 컨트롤러에도 광범위하게 남아있음을 확인했다.

`grep -rln "): ResponseEntity<Issue>\|): ResponseEntity<Posting>\|): ResponseEntity<Milestone>\|
): ResponseEntity<Comment>\|..."` 로 전수 검색한 결과:

| 파일 | raw 엔티티 반환 엔드포인트 수 |
|---|---|
| `IssueController.kt` | 8 |
| `IssueApiController.kt`(legacy Open API, P2-55/56) | 5 |
| `BoardController.kt` | 4 |
| `CommentController.kt` | 4 |
| `MilestoneController.kt` | 3 |

실제 재현: `GET /-_-api/v1/owners/{owner}/projects/{projectName}/issues/{number}`
(`IssueApiController.getIssueLegacyPath()`)를 실제 앱으로 호출 — 응답 60690바이트,
`password` 해시값이 그대로 노출됨을 확인.

`RestApiResponseDto.kt`에 이미 `Issue.toResponse(): IssueResponse`/
`IssueComment.toResponse(): IssueCommentResponse`가 존재한다(다른 v1 API 컨트롤러가 이미
쓰고 있는 것으로 보임) — `IssueController`/`IssueApiController`가 이걸 안 쓰고 있었을
뿐이다. `Posting`/`Milestone`/`Comment`(리뷰 댓글이 아닌 게시글/이슈 공용 댓글)용 DTO는
아직 없어 신규 작성이 필요하다.

## 범위

1. `IssueController.kt`/`IssueApiController.kt`의 raw `Issue`/`List<Issue>` 반환 13곳을
   기존 `Issue.toResponse()`로 교체(legacy 쪽은 필드명 계약이 다를 수 있으니 응답 스키마가
   실제로 legacy 계약과 맞는지 재확인 — 다르면 legacy 전용 DTO를 새로 만들 것, 기존
   `IssueResponse`를 함부로 변형해 다른 v1 API 소비자를 깨지 않을 것).
2. `BoardController.kt`의 `Posting` 4곳 — `PostingResponse` DTO 신설.
3. `CommentController.kt`의 `Comment`/`IssueComment`/`PostingComment` 4곳 — 관련 DTO 신설
   (이미 있는 `IssueCommentResponse`를 재사용할 수 있는 부분은 재사용).
4. `MilestoneController.kt`의 `Milestone` 3곳 — `MilestoneResponse` DTO 신설.
5. 각 컨트롤러를 호출하는 다른 어댑터(v1 REST API, MCP 도구 등 — PR 때
   `PullRequestApiController`/`PullRequestMcpTools`에서 발견한 것과 동일한 이중 변환
   패턴이 있는지)도 함께 확인·정리.
6. 컴파일 확인 → 관련 테스트 전체 재실행 → 실제 앱 기동으로 재검증(이슈/게시글/댓글/
   마일스톤 각각 최소 1개 엔드포인트를 실제로 호출해 `password` 문자열이 응답에 없는지
   확인).

## 완료 기준

- 위 5개 파일의 어떤 엔드포인트도 raw 엔티티를 직접 반환하지 않는다.
- 실제 이슈/게시글/댓글/마일스톤 API 응답에 비밀번호 해시가 노출되지 않는다(실사용 검증).
- 기존 테스트 전부 GREEN.

## 완료 로그

### 2026-09-09 — 구현 완료(커밋 `d1023909b`)

`fix(P3-30): 이슈/게시글/댓글/마일스톤 API 5개 컨트롤러의 순환참조/비밀번호 노출 수정` —
`RestApiResponseDto.kt`에 `IssueEventResponse`/`PostingResponse`/`PostingCommentResponse`/
`MilestoneResponse` DTO 신설, `IssueController`/`IssueApiController`(legacy)/`BoardController`/
`CommentController`/`MilestoneController`의 raw 엔티티 반환 지점 전부 DTO로 교체.
`IssueRestApiController`/`IssueMcpTools`의 이중 변환도 함께 정리. 범위 1~5 전부 반영됨.

### 2026-09-09 — 별도 세션에서 검증(코드 검토 + 테스트)

다른 세션(코디네이터)이 구현한 위 커밋을 이어받아 검증:
- 5개 컨트롤러 파일 전수 grep으로 `ResponseEntity<Issue|Posting|Milestone|Comment...>` 형태의
  raw 엔티티 반환이 하나도 남지 않았음을 확인.
- 관련 스펙 7개(`IssueControllerSpec`/`IssueApiControllerSpec`/`BoardControllerSpec`/
  `CommentControllerSpec`/`MilestoneControllerSpec`/`IssueRestApiControllerSpec`/
  `IssueMcpToolsSpec`)를 `-Dyona.it.db=h2`로 재실행 — 총 253개 테스트 전부 통과(실패/에러 0건).

**완료 기준 중 미충족 항목**: "실제 이슈/게시글/댓글/마일스톤 API 응답에 비밀번호 해시가
노출되지 않는다(실사용 검증)"은 이번 세션에서 수행하지 못했다 — 격리된 포트(18099)에 새
H2 인스턴스를 띄우고 `/bootstrap-setup`으로 최초 관리자를 만든 뒤 `/users/login`으로
로그인을 시도하자, 이 티켓과 무관한 **별도의 기존 버그**로 500 에러가 발생했다:

```
java.lang.IllegalArgumentException: Given that there is no default password encoder
configured, each password must have a password encoding prefix. Please either prefix
this password with '{noop}' or set a default password encoder in `DelegatingPasswordEncoder`.
	at org.springframework.security.crypto.password.DelegatingPasswordEncoder$UnmappedIdPasswordEncoder.matchesNonNull
	at org.springframework.security.authentication.dao.DaoAuthenticationProvider.additionalAuthenticationChecks
```

`SecurityConfig.kt`의 캐치올 체인(@Order 4)은 `.authenticationProvider(deployKeyAuthenticationProvider)
.authenticationProvider(yonaAuthenticationProvider)` 둘만 명시 등록하는데(과거 회귀 방지 코멘트
참고), 실제로는 `DaoAuthenticationProvider`가 개입해 `BootstrapSetupController`가 저장한
SHA-256 반복 해시(프리픽스 없음)를 `DelegatingPasswordEncoder`로 매칭하려다 실패한다.
`git log --oneline --all | grep -i saml`로 확인한 SSO 통합 커밋(`6a94ea831` "SAML2/OIDC 인증
흐름을 기존 필터체인에 통합")이 유력한 원인으로 보이나, 근본원인 확정과 수정은 이 티켓 범위
밖이라 별도 이슈로 남긴다(다음 담당자 확인 필요 — 로컬 로그인이 완전히 막혀있는 심각도 높은
회귀일 가능성).

**결론**: 순환참조/비밀번호 노출 수정 자체(코드+테스트)는 완료로 판단한다. 위에서 발견한
로그인 버그는 이 티켓과 무관한 별개 결함이라 [[tickets/p3-31|P3-31]]로 분리해 근본원인 규명 +
수정 + 검증까지 완료했다(상세는 P3-31 참고).

### 2026-09-09 — P3-31 수정 이후 실사용 검증 완료(당초 완료 기준 100% 충족)

같은 격리 인스턴스(포트 18099)에서 로그인 성공 → 프로젝트(`admin/p330proj`) 생성 →
레거시 API로 이슈 1건 생성(`POST /-_-api/v1/owners/admin/projects/p330proj/issues`, 201) →
원래 버그 재현에 썼던 바로 그 요청(`GET /-_-api/v1/owners/admin/projects/p330proj/issues/1`)을
재실행:

- 응답 200, 크기 **426바이트**(수정 전 재현 당시 60690바이트였던 것과 대비).
- 응답 본문에 `password` 문자열 0건 — `id`/`number`/`title`/`body`/`authorId`/`authorLoginId`
  등 평탄한 DTO 필드만 노출.

완료 기준 3항목(raw 엔티티 미반환/비밀번호 미노출 실사용 검증/기존 테스트 GREEN) 전부 충족.
