---
id: p2-16
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:217 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-16: **조사 완료, 종결**: 그룹 E 잔여(`checkCodeAccessibility`, `isOrgAdmin`) + 나머지 리소스 타입(CODE/ORGANIZATION/WEBHOOK/ATTACHMENT/PROJECT_TRANSFER) 전체를 yona와 대조 완료

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/utils/AccessControl.java:255-259`(ATTACHMENT), `:198-199`(ORGANIZATION), `:224-238`(PROJECT_TRANSFER)
- **yona 대상**: `web/ReviewThreadController.kt`(`checkCodeAccessibility`), `web/OrganizationController.kt`(`isOrgAdmin`), `domain/project/ProjectServiceImpl.kt:271-280 isAuthorizedToAcceptTransfer()`
- **비고(원문)**: 2026-08-20 확정 조사 결과: **CODE(코드 브라우저 접근), ORGANIZATION, PROJECT_TRANSFER는 yona와 일치해 문제없음**(CODE의 "DELETE 항상 false" 규칙은 yona에서도 실제로 트리거하는 코드가 없는 죽은 분기라 이식 불필요). WEBHOOK은 [[tickets/p1-87|P1-87]] 기존 항목의 수정 범위를 구체화(조회 포함 3곳 전부 멤버십 요구)하는 것으로 흡수, 별도 이동 아님. 조사 중 범위 밖에서 신규 발견한 **첨부파일 다운로드/목록 무인가([[tickets/p1-96|P1-96]], 보안), 브랜치 삭제 매니저 제한 누락([[tickets/p1-97|P1-97]], 과잉허용)**은 P1로 별도 등록. 이 항목은 조사 완료로 종결. **[2026-08-21 상충 해소, [[tickets/p0-21|P0-21]] 완료]** 위 "ORGANIZATION은 yona와 일치해 문제없음" 판정은 웹 화면(`OrganizationViewController.kt`)의 ORGANIZATION 리소스 권한 체크만 대조한 것이었고, REST API(`web/OrganizationController.kt`)의 `isOrgAdmin()`은 대조 대상에서 누락돼 있었다(실제로는 사이트매니저 우회가 빠져 있었음) — [[tickets/p0-21|P0-21]]에서 `isOrgAdmin()`에 `user.isSiteManager` 우회를 추가해 수정 완료. 이 종결 판정은 "웹 화면 한정"으로 정정.

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
