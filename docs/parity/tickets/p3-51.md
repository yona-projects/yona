---
id: p3-51
type: ticket
status: done
created: 2026-09-11
relates_to: [p3-49]
source: Twirl-vs-Thymeleaf 렌더링 감사 세션이 index/partial_notifications.html의 XSS 회귀를
  검증하는 회귀 테스트를 쓰던 중, 로케일을 명시적으로 바꿔가며 렌더링을 검증하다가 발견
---

# P3-51: `spring.messages.fallback-to-system-locale`가 켜져 있어(Spring Boot 기본값) 서버
배포 환경의 JVM 기본 로케일에 따라 i18n 메시지가 요청 로케일과 무관하게 뒤바뀔 수 있음

## 배경

Java `ResourceBundle`(그리고 이를 감싸는 Spring `ResourceBundleMessageSource`)의 기본 동작은,
요청 로케일 전용 번들 파일(`messages_en.properties`)이 없으면 **root 기본 번들
(`messages.properties`)을 시도하기 전에 먼저 "JVM 시스템 기본 로케일" 번들을 시도한다**
(`fallbackToSystemLocale=true`가 Spring의 기본값). 이 저장소는 `messages.properties`(root,
영어 - "Click to post" 등), `messages_ko.properties`/`messages_ko_KR.properties`(한국어),
`messages_ja_JP.properties` 등을 갖추고 있는데, `application.yml`에 `spring.messages.
fallback-to-system-locale`를 명시하지 않아 Spring Boot 기본값(`true`)을 그대로 쓴다.

**실제 재현**: 이 개발 샌드박스의 JVM 기본 로케일은 `ko_KR`이다. `MessageSource.getMessage(
"common.attach.clickToPost", null, Locale.ENGLISH)`를 직접 호출하면(요청 로케일을 명시적으로
영어로 지정했음에도) **"Click to post"가 아니라 "본문에 넣기"(한국어)가 반환된다** — "en"
전용 파일이 없으니 root보다 먼저 JVM 기본 로케일(ko_KR) 파일을 찾아 써버리기 때문이다.

**영향**: 실제 배포 환경(서버 컨테이너)의 시스템 로케일이 무엇이냐에 따라, 사용자가 브라우저
`Accept-Language`로 무슨 언어를 요청하든 **그 언어 전용 번들 파일이 없는 모든 케이스**가
서버의 시스템 로케일로 조용히 뒤바뀐다 — 배포 환경마다 다르게 동작하는 비결정적 버그다.
legacy(Play)는 `application.langs="en-US, ko-KR, ja-JP, ru-RU, uz-UZ"`(conf/
application.conf.default)로 결정적 로케일 폴백 순서를 직접 관리했고, JVM/OS 시스템 로케일과
전혀 무관하게 동작했다 — 이 비결정성 자체가 포팅 과정에서 새로 생긴 회귀다.

## 착수했다가 되돌린 이력 (2026-09-11)

이 세션에서 `spring.messages.fallback-to-system-locale: false`로 끄는 수정을 실제로
적용했었다(root 기본값으로만 폴백하게 함 — legacy의 결정적 동작과 동등). 그런데 이 변경
하나로 **`./gradlew test -Dyona.it.db=h2` 전체 스위트에서 53건이 새로 실패**했다 — 이
개발/CI 환경의 JVM 기본 로케일이 ko_KR이라, 그동안 "Accept-Language 미지정 요청은 한국어로
보인다"는 이 버그의 부작용에 암묵적으로 의존해 작성된 테스트가 최소 14개 스펙 파일에
걸쳐 있었다(`IndexControllerIntegrationSpec` 등 — 로케일을 명시하지 않은 채 한국어 UI
문구를 기대하는 assertion들).

**이번 세션 범위에서는 되돌렸다.** 이유: (1) 이 버그 자체는 Twirl-vs-Thymeleaf 템플릿
렌더링 차이가 아니라 Spring Boot MessageSource 설정 문제라 이번 감사의 본래 범위(A~E) 밖이고,
(2) 영향 범위(53개 테스트, 최소 14개 스펙 파일)가 이번 세션에서 이미 진행 중이던 보안
회귀(P3-51 발견의 계기가 된 알림 XSS, 아래 [[tickets/p3-49|관련 문서]] 참고) 수정과 뒤섞어
처리하기엔 너무 크고, (3) "시작 시점 기준 실패 3건 외에 실패를 늘리지 않는다"는 이 세션의
원칙을 지키려면 53건 전부를 이번 라운드에서 개별 검토해 로케일을 명시하도록 고쳐야 하는데,
그 각각이 "정말 한국어 UI를 검증하려는 의도인지 vs 그냥 기본 동작을 무심코 테스트한 것인지"
판단이 필요해 안전하게 한 번에 끝내기 어렵다고 판단했다.

## 제안하는 다음 단계

1. `application.yml`에 `spring.messages.fallback-to-system-locale: false` 추가.
2. 그 결과 실패하는 모든 테스트(대략 53건, 14개 스펙 파일 — 이번 세션 조사로 최소
   `IndexControllerIntegrationSpec`은 확인됨, 전체 목록은 재실행해서 재확인 필요)를 개별
   검토 — 각 테스트가 실제로 "한국어 UI 검증"이 의도라면 `.locale(Locale.KOREAN)`을 명시,
   단순히 기본 동작에 우연히 의존한 것이라면 그 페이지의 실제 올바른 기본 로케일이 무엇이어야
   하는지(legacy `application.langs`의 첫 번째 값인 en-US가 기본이어야 하는지, 아니면 사이트
   운영 관행상 한국어가 기본이어야 하는지 — 이건 제품 판단이 필요할 수 있음) 확인 후 수정.
3. 실서버로 실제 `Accept-Language: en`/`ko`/헤더 없음 세 케이스를 모두 curl로 재검증해
   결정적으로 동작하는지 확인.

## 완료 로그 (2026-09-11, 재착수)

이전 라운드에서 "영향 범위가 너무 커서 되돌렸다"고 기록했던 수정을 이번 라운드에서 끝까지
완료했다. `fallback-to-system-locale: false`만으로는 "Accept-Language 헤더 자체가 없는"
요청까지 결정적으로 만들지 못한다는 것도 이번에 추가로 발견해 함께 고쳤다(아래 2번).

### 1. 근본 수정 (2곳)

- `src/main/resources/application.yml` — `spring.messages.fallback-to-system-locale: false`
  추가. 요청 로케일 전용 번들이 없을 때 "서버 JVM/OS 시스템 로케일" 파일을 먼저 시도하던
  Spring Boot 기본 동작을 끄고, root(`messages.properties`, 영어)로 결정적으로 폴백하게 함.
- `src/main/kotlin/com/github/yonaprojects/yona/config/WebMvcConfig.kt` — 신규
  `@Bean fun localeResolver(): LocaleResolver`. Spring Boot 기본 `AcceptHeaderLocaleResolver`는
  `defaultLocale`을 지정하지 않으면 `Accept-Language` 헤더가 아예 없는 요청의 로케일을
  `HttpServletRequest.getLocale()`(역시 서버 JVM/OS 시스템 로케일)로 되돌린다 — 위 1번과는
  별개의 비결정성 원인이라 `setDefaultLocale(Locale.ENGLISH)`로 명시적으로 고정해야 "헤더
  없음 → 영어" 케이스까지 완전히 결정적으로 만들 수 있었다.

### 2. 회귀 테스트 (신규)

- `src/test/kotlin/com/github/yonaprojects/yona/config/MessageSourceLocaleFallbackSpec.kt`
  - `MessageSource.getMessage(..., Locale.ENGLISH)` / `Locale.KOREAN)` 직접 호출 검증(1번 수정
    대상).
  - `GET /`을 Accept-Language 헤더 없이/`.locale(Locale.KOREAN)`로 요청해 실제 렌더링된
    페이지가 각각 영어/한국어인지 검증(2번 수정 대상). **주의**: 이 테스트는 반드시 익명
    요청이어야 한다 — `title.login`/`button.login`은 `sec:authorize="isAnonymous()"` 블록
    안에 있어 로그인한 사용자의 대시보드에는 애초에 렌더링되지 않는다(처음에 로그인 사용자로
    작성했다가 이 이유로 RED가 되는 것을 확인하고 익명 요청으로 고쳤다). 또한
    `BootstrapSetupInterceptor`가 회원 0명이면 "/"를 무조건 `/bootstrap-setup`으로
    리다이렉트하므로, 로그인은 하지 않되 최소 1명의 회원은 DB에 있어야 한다.

### 3. 기존 테스트 53건(16개 스펙 파일) 개별 재검토 — 코디네이터가 이미 확정한 기준대로 처리

기준: (a) 정말 한국어 UI 문구 자체를 legacy와 동치 검증하려는 의도인 테스트 →
`.locale(Locale.KOREAN)`/`Accept-Language: ko`를 명시적으로 추가. (b) 단순히 "이 개발
샌드박스의 JVM 기본 로케일이 ko_KR"이라는 버그의 부작용에 우연히 얹혀 있던 테스트 → 기대
문자열을 root(영어) 번들 값으로 수정.

**(b) 영어로 수정한 파일** — 새로 추가된(legacy에 대응물이 없는) GitHub 컨벤션 화면 위주:
- `ApiTokenEditFormTemplateRenderingSpec.kt` — "발급된 토큰"→"Your tokens", "새 토큰
  발급"→"Generate new token", "토큰이 발급되었습니다"→"Token generated".
- `DeployKeyEditFormTemplateRenderingSpec.kt` — "새 Deploy Key 추가"→"Add new deploy key".
- `GpgKeyEditFormTemplateRenderingSpec.kt` — "새 GPG 키 추가"→"Add new GPG key", "GPG 키가
  추가되었습니다"→"GPG key added.".
- `SshKeyEditFormTemplateRenderingSpec.kt` — "새 SSH 키 추가"→"Add new SSH key", "SSH 키가
  추가되었습니다"→"SSH key added.".
- `OAuthAuthorizedAppsControllerIntegrationSpec.kt` — "권한이 없습니다"→"You are not
  authorized", "인가한 OAuth 애플리케이션이 없습니다"→"You have not authorized any OAuth
  applications yet.".
- `CommentDeleteModalInlineScriptSyntaxSpec.kt` — "댓글 삭제"→"Delete comment".
- `IndexControllerIntegrationSpec.kt` — "로그인"→"Log in", "개발팀에게 문의하기"→"Feedback",
  "새 프로젝트 만들기"→"Create new project".
- `PostingHistoryTemplateRenderingSpec.kt` — "변경 이력"→"Change history".
- `TimelineTemplateRenderingSpec.kt` — "이 이슈를 닫았습니다"→"closed this issue", "리뷰를
  완료했습니다"→"completed a pull request review.".
- `TagRestApiControllerIntegrationSpec.kt` — code 브라우저 태그 optgroup 라벨 "태그"→"Tags".
- `PullRequestListTemplateEquivalenceSpec.kt` — "새 코드 보내기"→"pull request"(`pullRequest.new`
  root 값), "저장"→"Save"(`button.save` root 값).

**(a) `.locale(Locale.KOREAN)`을 명시해 한국어 동치성 검증을 유지한 파일** — legacy와의
한국어 UI 문구 1:1 동치성 자체가 테스트 목적인 "TemplateEquivalenceSpec" 계열:
- `BootstrapSetupTemplateEquivalenceSpec.kt` — 최초 관리자 생성 화면(legacy
  `welcome/secret.scala.html`) 전 시나리오.
- `ErrorPageTemplateRenderingSpec.kt` — 컨텍스트 인지형 에러 화면(#45/#47/#49/#50/#53 등)
  전체 재현 시나리오(약 13건 `it` 블록 전부에 `.locale(Locale.KOREAN)` 추가).
- `OrganizationTemplateEquivalenceSpec.kt` — 조직 홈 관리자/멤버 h3 라벨("그룹 관리자"/"그룹
  구성원") 검증 1건.
- `SiteAdminTemplateEquivalenceSpec.kt` — `<title>` 메시지 키 정합성 검증(3건) +
  `/lostPassword` 화면 GNB/타이틀·에러 메시지 검증(2건).
- `TemplateEquivalenceSpec.kt` — 삭제 확인 모달 예/아니요 버튼(1건), 전역 검색
  이슈/프로젝트/사용자/마일스톤/댓글 탭 동치성(5건), 도움말 toc.html 문항 수·`<title>`(2건),
  keymap.html section별 단축키 안내(1건) — 총 9건.

### 4. 이번 검토 중 함께 발견해 즉시 고친, 로케일과 무관한 별개의 스테일 테스트 버그 1건

`PullRequestListTemplateEquivalenceSpec.kt`의 "select2/마크다운 에디터/파일 업로더 드롭존이
issue/create.html과 동일한 방식으로 이식되어야 한다" 테스트가 `script[src*='lib/select2/
select2.js']`가 1개 있어야 한다고 단언하고 있었는데, 이미 이전 세션(P3-46 #5,
`common/select2.html` 주석 참고)에서 Select2(v3)를 Tom Select로 완전히 교체해 실제로는
`tom-select.complete.min.js`만 로드된다 — 로케일 변경과 무관하게 원래도 깨져 있었어야 할
스테일 단언인데 이번 재검토 과정에서 함께 발견해, 그 자리에서 로케일 문제와 별개로 즉시
고쳤다(정답이 명확한 순수 버그 수정이라 코디네이터 확인 없이 처리).

### 5. `SiteAdminTemplateEquivalenceSpec`의 이미 문서화된 무관한 기존 결함(수정하지 않음)

`data/massMail/postList의 스트레이 </div> 버그` 테스트가 전체 6,500여 건 스위트를 한 번에
돌릴 때만 실패하는 것이 [[tickets/p3-48]] 완료 로그에 이미 기록돼 있다(공유 H2 파일 DB에
다른 스펙이 먼저 만든 게시글의 authorId가 가리키는 User가 없어 `userRepository.
findByLoginId(post.authorLoginId)` 계열 SpEL이 깨지는 테스트-순서 의존성). 이번 세션에서
`SiteAdminTemplateEquivalenceSpec` 단독/그룹 재실행으로는 100% 통과함을 재확인했고,
P3-48 완료 로그의 결론(이번 변경과 무관, 4개 티켓 모두 끝난 뒤 전체 스위트에서 재확인)이
여전히 유효함을 확인했다 — 지금 라운드에서는 손대지 않았다.

### 6. 실서버 curl 재검증 (보조 수단)

`./gradlew bootRun`으로 재기동한 뒤(설정 변경은 hot-reload 안 됨) 세 경우 모두 확인:

```
$ curl -s http://localhost:8099/ | grep user-item-btn         # 헤더 없음 → 영어
user-item-btn" data-login="required">Log in
$ curl -s -H "Accept-Language: en" http://localhost:8099/ | grep user-item-btn
user-item-btn" data-login="required">Log in
$ curl -s -H "Accept-Language: ko" http://localhost:8099/ | grep user-item-btn
user-item-btn" data-login="required">로그인
```

`/_help` 페이지의 `<title>`로도 동일하게 재확인(헤더 없음/en → "Help - Yona", ko →
"도움말 - Yona").

### 7. Playwright 실브라우저 검증 (티켓당 1회, 완료 시점에 수행)

`/tmp/.../scratchpad/pw/verify_p351.js`(재사용 가능한 `helpers.js`의 `newBrowser()` 활용) —
브라우저 컨텍스트의 `locale` 옵션(Chromium이 이 값으로 실제 Accept-Language 헤더를 구성함)과
`extraHTTPHeaders`로 헤더를 직접 지정하는 두 경로 모두로 확인:

```json
{
  "defaultLocale_loginText": "로그인",
  "koLocale_loginText": "로그인",
  "koLocale_helpTitle": "도움말 - Yona",
  "enLocale_loginText": "Log in",
  "enLocale_helpTitle": "Help - Yona",
  "headerKo_loginText": "로그인"
}
```

`defaultLocale`(컨텍스트에 locale을 안 주면 Chromium이 OS 기본값을 씀, 이 샌드박스는
ko)이 한국어로 나온 것은 정상이다 — 실제 브라우저는 Accept-Language 헤더를 절대 생략하지
않으므로(항상 무언가를 보냄) "헤더 자체가 없는" 케이스는 curl/API 클라이언트에서만
의미있게 재현되고, 이는 6번에서 이미 확인했다. 브라우저 시나리오에서 중요한 것은
"명시적으로 ko/en을 요청하면 그대로 렌더링되는가"이고, 이는 en/ko 두 경우 모두 정확히
동작함을 확인했다.

### 8. 재실행한 스펙 (17개 파일, 티켓이 직접 건드린 파일과 관련된 것만 — 전체 회귀 스위트는
4개 티켓 완료 후 한 번만 실행 예정)

`MessageSourceLocaleFallbackSpec`(신규), `ApiTokenEditFormTemplateRenderingSpec`,
`BootstrapSetupTemplateEquivalenceSpec`, `CommentDeleteModalInlineScriptSyntaxSpec`,
`DeployKeyEditFormTemplateRenderingSpec`, `ErrorPageTemplateRenderingSpec`,
`GpgKeyEditFormTemplateRenderingSpec`, `IndexControllerIntegrationSpec`,
`OAuthAuthorizedAppsControllerIntegrationSpec`, `OrganizationTemplateEquivalenceSpec`,
`PostingHistoryTemplateRenderingSpec`, `PullRequestListTemplateEquivalenceSpec`,
`SiteAdminTemplateEquivalenceSpec`, `SshKeyEditFormTemplateRenderingSpec`,
`TagRestApiControllerIntegrationSpec`, `TemplateEquivalenceSpec`,
`TimelineTemplateRenderingSpec` — 전부 GREEN.
