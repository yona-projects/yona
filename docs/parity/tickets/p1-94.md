---
id: p1-94
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:147 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-94: **(2026-08-20 [[tickets/p2-15|P2-15]] 확정 조사에서 발견)** `IssueLabelController.isProjectManager`가 라벨/카테고리 CRUD 전체(7곳)를 매니저 전용으로 제한 — yona는 ISSUE_LABEL/ISSUE_LABEL_CATEGORY가 일반 switch 대상이라 프로젝트 멤버 전원 허용

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/controllers/IssueLabelApp.java`(전 액션 `@IsAllowed(UPDATE/DELETE, ISSUE_LABEL[_CATEGORY])`) → `app/utils/AccessControl.java:280-282`
- **yona 대상**: `web/ProjectViewController.kt`(`newLabel`/`updateLabelForm`/`deleteLabelForm`/`updateCategoryForm`/`copyLabelsForm` 등, 라벨 생성/수정/삭제/카테고리 CRUD/copyLabels)
- **비고(원문)**: 2026-08-20 [[tickets/p2-15|P2-15]] 조사에서 확정. **완료** — 아래 완료 로그 참고(단, 완료 로그는 당시 실존했던 `IssueLabelController.kt` 기준 서술). **범위 정정**: 착수 중 Serena LSP로 legacy `IssueLabelApp.java`를 직접 대조해보니 생성 계열 3곳(`newLabel`/`newCategory`/`copyLabels`)은 실제로는 `@IsAllowed`가 아니라 `@IsCreatable`이었음(이 항목의 원 서술은 "전 액션 `@IsAllowed`"라고 뭉뚱그렸으나 부정확) — 결과적으로 두 방식 모두 "프로젝트 멤버 전원 허용"으로 귀결돼 수정 방향 자체는 바뀌지 않았지만, 생성 3곳은 `isProjectResourceCreatable()`로, 수정/삭제 4곳은 `isAllowed(...)`로 서로 다른 함수를 호출하도록 구현. **경로 정정(2026-08-25, stale 감사)**: 이후 TASK-0262가 커스텀 REST `web/IssueLabelController.kt` 자체를 삭제하고 legacy 폼 기반 엔드포인트로 `web/ProjectViewController.kt`에 전면 재구현했다 — 새 코드에서도 이 권한 게이트(`isProjectResourceCreatable`/`isAllowed(...UPDATE/DELETE)`)가 그대로 유지됨을 코드 확인(`newLabel()` 주석에 "P1-94" 명시적 언급 존재)

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
