---
id: p1-66
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:119 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-66: 이슈 수정 폼에 "다른 프로젝트로 이동" UI(대상 프로젝트 선택)가 없음 — 백엔드([[tickets/p1-48|P1-48]])는 이미 완료

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/views/issue/edit.scala.html`(대상 프로젝트 선택 select), `app/controllers/IssueApp.java:736-782 editIssue()`(`targetProjectId` 폼 필드 처리)
- **yona 대상**: 프론트엔드(이슈 수정 화면) — 이미 완료된 `web/IssueController.kt`(`POST /{number}/move`, [[tickets/p1-48|P1-48]])를 호출하는 UI 배선만 필요
- **비고(원문)**: **코드 레벨 완료로 재분류(2026-08-20)** — 사용자 지시("UI는 나중에 작업할 거니까 코드 레벨 기준으로 맞춰야 함")에 따름. 서비스/API 로직(`IssueService.moveIssue`, 라벨 이전·서브태스크 동반 이동·ISSUE_MOVED/NEW_ISSUE 알림 포함)은 [[tickets/p1-48|P1-48]]에서 이미 완전히 구현·테스트 완료돼 있고, 남은 건 순수 프론트엔드(대상 프로젝트 선택 드롭다운)뿐이라 이 백로그(코드 레벨 동치성 추적)에서는 열린 항목으로 두지 않는다. **2026-08-23 정정 및 실버그 수정**: "UI는 별도 트랙에서 진행 예정"이라던 서술이 stale — `issue/edit.html`의 `targetProjectId` select는 `docs/TEMPLATE_BACKLOG.md` #114(TASK-0238)에서 이미 마크업까지 이식돼 있었으나, 재검토 중 `IssueViewController.editIssue()`가 바인딩하는 `IssueForm`에 `targetProjectId` 필드 자체가 없어 폼을 제출해도 이슈가 실제로 이동하지 않는 **죽은 UI**였음을 발견 — `IssueForm`에 필드 추가, `editIssue()`에 legacy `hasTargetProject()`/`isRequestedToOtherProject()`/`moveIssueToOtherProject()` 대응 분기(대상 프로젝트 권한 확인 후 이미 구현돼 있던 `IssueService.moveIssue()` 호출, 이동 후 새 프로젝트로 리다이렉트)를 추가해 완료. `IssueEditMoveProjectSpec.kt`(신규, 2 tests) 추가 — 정상 이동 1건, 권한 없는 프로젝트로의 이동 거부 1건

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
