---
id: p1-145
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:198 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-145: **(2026-08-26 골든 체크 버킷C 사람 검토에서 발견)** 프로젝트명에 예약 패턴(`.`/`..`/`.git` 등)을 막는 검증이 yona에 전혀 없음 — 현재는 중복명 체크만 존재. Git 저장소 디렉터리명으로 그대로 쓰이는 값이라 파일시스템 경로 문제(예: `.git` 접미사로 저장소 디렉터리 자체와 충돌) 소지가 있음

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/validation/ExConstraints.java`(`@Restricted` 어노테이션, `Project.name`에 적용)
- **yona 대상**: `domain/project/ProjectNameValidator.kt`(신규), `ProjectServiceImpl.createProject()`, `web/ProjectApiController.newProject()`
- **비고(원문)**: **완료** — `RestrictedValidator.isValid()`가 `ignoreCase=false`(실사용 기본값)일 때 오히려 `equalsIgnoreCase`로 비교하는 legacy 버그를 그대로 재현(대소문자 무관 일치 시 거부). 서비스 레이어(`createProject`, 웹폼+Import 경로가 전부 경유)와 REST API(`ProjectApiController.newProject`, 서비스 레이어를 안 거치고 직접 저장하는 별도 경로)에 각각 추가. TDD로 RED 확인 후 구현, `ProjectServiceImplSpec`/`ProjectApiControllerSpec` 그린

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
