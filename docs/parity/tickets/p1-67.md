---
id: p1-67
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:120 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-67: 최근 방문 이슈/게시글을 보여주는 사이드바 UI가 없음 — 백엔드([[tickets/p1-41|P1-41]])는 이미 완료

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/views/sidebar.scala.html:54-72`(`myRecentIssueList` 탭), `app/views/index/{myRecentIssueList,myRecentIssueList_partial}.scala.html`
- **yona 대상**: 프론트엔드 — 이미 완료된 `web/UserController.kt`(`GET /api/users/me/recent-issues`, [[tickets/p1-41|P1-41]])를 호출하는 UI 배선만 필요
- **비고(원문)**: **코드 레벨 완료로 재분류(2026-08-20), 이후 UI도 실제로 완료됨(정정, 2026-08-23)** — 당시엔 사용자 지시("UI는 나중에 작업할 거니까 코드 레벨 기준으로 맞춰야 함")에 따라 열린 항목으로 두지 않았으나, `docs/TEMPLATE_BACKLOG.md` TASK-0225(#14 `common/usermenu_tab_content_list.html`)가 별도 트랙에서 이 UI를 실제로 완성했다 — 단, [[tickets/p1-41|P1-41]]이 예상한 `/api/users/me/recent-issues` JSON API+프론트엔드 fetch 방식이 아니라 `UserViewController.usermenuTabContentList()`가 `recentIssueService.getRecentIssues()`를 서버 렌더링으로 직접 소비하는 방식(legacy `myRecentIssueList.scala.html`의 서버사이드 렌더링 구조에 더 충실). 백로그 재감사 중 발견해 정정

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
