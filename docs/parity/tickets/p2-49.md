---
id: p2-49
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:250 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-49: **(2026-08-26 골든 체크 버킷C 사람 검토에서 발견, 2026-08-26 TDD로 완료)** 첨부파일 목록 인메모리 캐싱이 yona에 없음(`@Cacheable` 등 캐시 계층 부재)

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/utils/AttachmentCache.java`(24시간 TTL, `save`/`update`/`delete` 시 무효화)
- **yona 대상**: `config/CacheConfig.kt`(신규), `domain/attachment/{AttachmentRepository,AttachmentServiceImpl}.kt`
- **비고(원문)**: **완료** — `spring-boot-starter-cache`+Caffeine 의존성 추가(`build.gradle.kts`), `YonaApplication`에 `@EnableCaching`, `CacheConfig`에 1일 TTL `CaffeineCacheManager` 빈 신규. `AttachmentRepository.findByContainerTypeAndContainerId()`에 `@Cacheable`, 호출부 15곳 이상을 건드리지 않고 이 Repository 메서드 한 곳만 캐싱해 legacy와 동등 효과. `store`/`delete`/`deleteAll`(self-invocation이라 별도 명시 필요)/`moveAll`/`moveOnlySelected`에 `@CacheEvict`(`moveAll`류는 출발지·도착지 둘 다 `@Caching`으로 무효화). TDD로 캐시 히트(DB 직접 삽입에도 stale 값 유지)와 5개 무효화 지점 전부를 통합 테스트로 검증(`AttachmentServiceSpec.kt`), 전체 회귀 확인

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
