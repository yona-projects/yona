---
id: p2-53
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:254 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-53: **(2026-08-26 골든 체크 버킷C 사람 검토에서 발견, 2026-08-26 재검증 후 CLOSED)** legacy `GitBranch`의 `pullRequest` 필드(브랜치-PR 연결) 대응이 yona `GitBranch.kt`에서 확인되지 않음

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/playRepository/GitBranch.java`(`pullRequest`/`getPullRequest`/`setPullRequest`)
- **yona 대상**: `domain/vcs/GitBranch.kt` + `web/BranchViewController.kt:83`
- **비고(원문)**: **공백 아님, CLOSED** — legacy `setPullRequest()`는 `GitRepository.java:892`(`PullRequest.findTheLatestOneFrom(project, branchName)`) 단 한 곳에서만 호출되고, `getPullRequest()`는 `partial_branchrow.scala.html:45`의 브랜치 목록 "Pull Request" 컬럼 렌더링에만 쓰인다. yona는 엔티티 필드 대신 `BranchViewController.kt:83`에서 `pullRequestRepository.findFirstByFromProjectAndFromBranchAndToProjectOrderByNumberDesc(...)`로 조회 시점에 역방향 조인해 동일 기능을 이미 제공(`PullRequestRepository.kt:104`에 "yona GitRepository.setTheLatestPullRequest() 대응" 명시 주석 존재) — 구조만 다를 뿐 기능 동등

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
