---
id: p0-26
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:48 (표, P0 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P0-26: **(2026-08-31 사용자가 웹 폼으로 프로젝트 생성 후 README 커밋 실패를 재현하는 중 발견)** 웹 폼(`/projectform`, `/projects`)으로 프로젝트를 생성하면 DB 행만 만들어지고 물리 bare git/svn 저장소가 생성되지 않음 — 이후 게시판 "README로 만들기" 등 저장소에 쓰는 모든 작업이 `BareCommit`/JGit 예외로 조용히 실패함(`BoardViewController.kt`의 `catch (e: Exception) { e.printStackTrace() }`가 예외를 삼켜 화면상으로는 정상 리다이렉트로 보임). REST API 경로(`ProjectApiController.kt:163`)는 `repositoryService.getRepository(savedProject).create()`를 호출해 정상이었으나, 웹 폼이 거치는 `ProjectServiceImpl.createProject()`에는 이 호출이 아예 없었음(H2 프로파일로 실행 중 DB `PROJECT`/`POSTING` 테이블과 `/tmp/yona/git` 파일시스템을 직접 대조해 확인)

## 원본 표 정보

- **우선순위 구분**: P0
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `ProjectApp.java:191 "RepositoryService.createRepository(project)"`
- **yona 대상**: `domain/project/ProjectServiceImpl.kt createProject()`
- **비고(원문)**: **완료** — TDD로 수정. `ProjectServiceImplSpec.kt`에 "새 프로젝트를 생성하면 물리 저장소도 함께 생성돼야 한다" 테스트 추가(RED 확인 후) → `createProject()` 마지막에 `repositoryService.getRepository(savedProject).create()` 호출 추가로 GREEN. 기존 `createProject` 성공 경로 테스트 4건에도 `repositoryService.getRepository` 스텁 추가(회귀 방지). `ProjectServiceImplSpec`(87) / `ProjectViewControllerSpec`(187) / `ProjectApiControllerSpec`(24) 전부 GREEN

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
