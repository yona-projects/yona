---
id: p2-61
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:262 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-61: **(2026-08-28 legacy `-_-api/v1` Open API 전수 대조에서 발견)** `StatisticsController`의 `GET /-_-api/v1/users/{loginId}/statistics`가 legacy `-_-api/v1` 네임스페이스에 **존재한 적 없는 경로** — legacy master `conf/routes` 전수 확인 결과 통계는 `GET /:user/:project/statistics`(HTML) 뿐, `controllers/api/` 패키지에 통계 API 자체가 없음. yona가 신규 기능을 legacy Open API 네임스페이스에 잘못 끼워 넣은 것으로, "legacy와 동일한 Open API 목록"을 기대하는 외부 클라이언트에게 혼란을 줄 수 있음

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **비고(원문)**: [원본에 있던 추가 열(섹션 헤더는 3열인데 이 행은 4열)]
  - (원본 열 3) `web/StatisticsController.kt:33`
  
  **완료** — 실사용처(템플릿/정적 JS) 확인 결과 0건이라 안전하게 `/api/users/{loginId}/statistics`로 이동, `-_-api/v1` 네임스페이스에서 제거

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
