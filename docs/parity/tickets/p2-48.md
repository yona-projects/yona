---
id: p2-48
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:249 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-48: **(2026-08-26 골든 체크 버킷C 사람 검토에서 발견, 2026-08-26 TDD로 완료)** Apache Combined Log Format 방식 HTTP 접근 로그(사용자/referer/UA/응답시간)가 yona 전체에 없음

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/utils/AccessLogger.java`(`Global.onRequest/onError/onBadRequest/onHandlerNotFound`에서 예외 없이 항상 호출)
- **yona 대상**: `config/{AccessLogFilter,SecurityConfig}.kt`(신규)
- **비고(원문)**: **완료** — `AccessLogFilter`(`OncePerRequestFilter`) 신규 추가, `SecurityConfig`에 기존 필터들과 동일하게 `addFilterAfter(BasicAuthenticationFilter)`로 등록해 경로 제외 없이 모든 요청에 적용(legacy와 동일). legacy가 요청 경로별로 동적 Logger("access." + uri)를 만들던 것은 Spring/Logback 관례에 맞춰 단일 "access" 로거로 이식(관찰 가능한 로그 포맷 자체는 legacy와 동일). TDD로 logback `ListAppender`를 붙여 실제 로그 이벤트(Combined Log Format 필드, 인증 사용자명, Referer/User-Agent 하이픈 처리, 다운스트림 예외 전파)를 캡처해 검증(`AccessLogFilterSpec.kt`, 100%/100%/100%). TASK-0361에 코드는 포함됐으나 이 문서 상태 갱신을 누락했다가 2026-08-26 뒤늦게 바로잡음

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
