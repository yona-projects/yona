---
id: p3-12
type: ticket
status: in-progress
created: unknown
updated: 2026-09-07
relates_to: []
source: docs/PARITY_BACKLOG.md:282 (표, P3 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P3-12: Mercurial(hg4j) 저장소 지원 추가 (2026-08-31 사용자 제안, 계획서만 작성 — 코드 0줄)

## 원본 표 정보

- **우선순위 구분**: P3
- **상태(원본 체크박스)**: `[ ]`
- **비고(원문)**: [원본에 있던 추가 열(섹션 헤더는 3열인데 이 행은 4열)]
  - (원본 열 3) yona 동치성과 무관 — legacy는 GIT/SUBVERSION 두 VCS만 지원, Mercurial 관련 코드 0건(전수 확인)
  
  계획서: [`docs/yona-wiki/plans/p3-12-mercurial-hg4j.md`](yona-wiki/plans/p3-12-mercurial-hg4j.md). 사용자가 지정한 라이브러리는 Maven Central의 구버전 TMate `org.tmatesoft.hg4j`가 아니라 **자체 개발 중인 [`github.com/search5/hg4j`](https://github.com/search5/hg4j)**이며, **인터페이스를 JGit과 동일하게 맞출 예정**이라고 확인됨 — 그렇다면 `domain/vcs/GitRepository.kt`의 구현 패턴을 hg4j API로 옮겨 적는 방식으로 `HgRepository : PlayRepository`를 구현 가능할 것으로 기대(실제 API 확정 전까지는 가정). `RepositoryService.getRepository()`의 GIT/SVN 이분기에 세 번째 분기 추가, 프로젝트 생성 폼에 VCS 옵션 추가, `GitAuthorizationFilter`/`SvnAuthorizationFilter`와 대칭인 `HgAuthorizationFilter` 신설까지가 yona 쪽 통합 범위. **가장 큰 미확정 사항**: `search5/hg4j`가 JGit의 `GitServlet`에 해당하는 프로토콜 서빙 컴포넌트(smart HTTP)까지 제공하는지, 아니면 저장소 읽기/쓰기 API만 제공하고 Mercurial wire protocol 자체는 yona가 직접 구현해야 하는지 — hg4j 저장소 진행 상황을 먼저 확인해야 착수 가능(사실상의 외부 블로커)

## 완료 로그

- **2026-09-07 1라운드**: 사용자 지시("gh:search5/hg4j에서 가져와서 등록하는 것으로 작업 진행해줘")로
  착수. "가장 큰 미확정 사항"(프로토콜 서빙 컴포넌트 유무)을 hg4j 저장소를 직접 읽어 해소 —
  `HgHttpWireServer`/`HgSshWireServer`가 이미 JGit `GitServlet`에 대응하는 형태로 존재.
  `includeBuild("../hg4j")` 컴포짓 빌드로 연결, `domain/vcs/HgRepository.kt`/`HgCommit.kt` 신설(저장소
  생성/isEmpty/커밋 히스토리·조회/파일·폴더 트리 조회/move·rename, 14 tests GREEN),
  `RepositoryService`/프로젝트 생성 폼/VCS 전환 토글에 3번째 옵션 노출. 브랜치/태그 CRUD·diff·archive는
  의도적으로 2라운드로 미룸. 상세: `docs/yona-wiki/plans/p3-12-mercurial-hg4j.md` 완료 로그 참고.
- **2라운드 착수 전 결정사항(2026-09-07 사용자 확정)**: 범위는 HTTP/SSH 프로토콜 서빙 +
  브랜치/태그 CRUD까지 전부, 1라운드에서 발견한 기존 버그(프로젝트 이전/포크 물리 디렉터리
  이동의 `.git` 접미사 하드코딩)도 함께 수정. Mercurial의 SSH 접근은 Git과 동일한 아키텍처
  (공유 계정 + `authorized_keys` 강제 명령 — Mercurial 공식 `hg-ssh`가 이 방식)이므로 [[p3-03|P3-03]]이
  만든 `SshInternalController`/`SshAuthService` 인가 파이프라인을 그대로 재사용하기로 확정(별도
  파이프라인 신규 개발 안 함). SVN은 관례상 공유 계정이 아니라 실제 시스템 계정 단위 접속이라
  이 아키텍처와 안 맞아 이번 범위에서 제외 — 지금처럼 HTTP(WebDAV)만 유지. 상세: 계획서 완료
  로그 참고.
- **2026-09-08 2라운드 완료**: SSH 프로토콜 서빙(`HgSshProtocolHandler`+`authorizeHgCommand()`)은
  직전 작업(P3-18)에서 이미 완료돼 있었음을 확인 — 이번 라운드는 HTTP 서빙 + 브랜치/태그 CRUD +
  `.git` 접미사 버그 수정 + SSH 쪽 남은 테스트 갭 메우기에 집중.
  - **브랜치/태그 CRUD (`domain/vcs/HgRepository.kt`)**: yona의 git 모양 "브랜치" 개념을
    Mercurial **bookmark**에 매핑(사용자가 이미 확정한 설계 — Mercurial의 진짜 named
    branch(`hg branch`, 커밋에 영구히 새겨짐)와는 별개 개념이라 재론의하지 않음. Bitbucket이 과거
    Mercurial을 git 모양 UI로 노출할 때 쓰던 것과 동일한 매핑, "모호하면 주류 forge 관례" 원칙에
    부합). hg4j의 `BookmarkCommand`(getBranches/createBranch/deleteBranch/getHeadBranch)와
    `TagCommand`/`TagsCommand`(getTagNames/getTags/createTag/deleteTag)로 실제 구현, `getRefNames()`도
    bookmark 이름 + "tip"을 반환하도록 갱신. `GitBranch`/`GitTag`는 기존 git 전용 값 객체를 그대로
    재사용(`refs/heads/`/`refs/tags/` 접두어 계약 동일하게 준수) — Hg 태그는 git의 annotated
    태그처럼 별도 태거 신원/GPG 서명이 없으므로 `tagger`/`message`는 항상 null, `annotated=false`로
    고정(지어내지 않음, `GitTag`의 기존 invariant와 일치). hg4j가 항상 끼워 넣는 pseudo-tag
    "tip"은 목록에서 걸러낸다. "tip"이라는 이름으로 브랜치/태그를 만들려는 시도, 이미 존재하는
    이름 재사용, 존재하지 않는 시작점은 전부 `IllegalArgumentException`으로 거절(실제 hg CLI의
    "the name 'tip' is reserved"/"already exists (use -f to force)" 동작을 근사). `HgRepositorySpec`
    신규 16 tests + 기존 14 tests = 30 tests GREEN.
  - **HTTP 서빙**: `web/HgController.kt`(신규) — hg4j `HgHttpWireServer`(순수 `HttpServlet`)를
    `SvnController`와 동일한 "프로젝트별 인스턴스 캐시" 패턴으로 감싼다(키는 `$owner/$project` —
    SVN의 owner당 하나와 달리 Hg는 저장소당 인스턴스 하나가 필요, `HgHttpWireServer` 생성자가
    hg4j 자체 `lib.HgRepository`를 직접 받는 구조라서). `web/HgServletRequestWrapper.kt`(신규) —
    `HgHttpWireServer.service()`가 `getRequestURI()` 하나만으로 라우팅을 결정하는데(루트 "/"=v2
    capabilities-discovery, "/api/"=v2 명령) Spring MVC 매핑 밑에서는 항상 원본 전체 경로가
    보이므로, `SvnServletRequestWrapper`와 동일한 이유로 저장소 루트 기준 상대경로를 만들어준다.
    라우트는 지시대로 `/hg/{owner}/{project}/**`(git/svn과 동일한 접두어 관례). OPTIONS 우회는
    불필요함을 `HgHttpWireServer` 소스로 직접 확인 후 의도적으로 생략(hg wire protocol은 순수
    GET/POST `?cmd=` 기반, WebDAV가 아님).
  - **인가**: `config/hg/HgAuthorizationFilter.kt`(신규) — `GitAuthorizationFilter`/
    `SvnAuthorizationFilter`와 대칭, `RepoAccessPolicy`(findProject/requiresAuth/isMember/
    isGuestUser)를 그대로 재사용. 이 필터만의 고유 로직은 (1) vcs 종류 검증, (2) hg wire
    protocol 전용 read/write 판정 — v1은 `cmd=unbundle` 쿼리파라미터, v2는
    `/api/<namespace>/<ro|rw>/<command>` URL 세그먼트. HTTP는 SSH와 달리 요청 하나하나가
    read/write를 URL/쿼리 시점에 명시적으로 드러내므로(SSH처럼 한 커넥션이 pull/push를 다
    처리할 수 있는 구조가 아님), git-receive-pack/PUT을 거르는 것과 동일하게 `HgController` 진입
    전에 필터에서 전부 걸러진다 — SSH의 pre-changegroup 훅 같은 별도 훅이 HTTP 경로에는 필요
    없음(주석으로 명시). `SecurityConfig`에 `/hg/**` permitAll + 필터 체인 등록.
    `HgAuthorizationFilterSpec`(신규, mockk 기반) 10 tests GREEN. `RepoAccessPolicy.kt`의 기존
    "미래에 HgAuthorizationFilter 추가 예정" 주석을 실제 완료 사실로 갱신.
  - **`yona.hg.base-dir`**: P3-18에서 이미 확립된 프로퍼티/기본값(`/tmp/yona/hg`)을 그대로
    재사용(신규 설정 키 추가 안 함).
  - **브랜치 보호 정책 스코프 확인(구현 안 함, 지시대로)**: `ProtectedBranchRepository` 기반
    정책은 Hg 쪽에 전혀 없음을 확인(P3-18이 이미 문서화한 알려진 갭과 동일). `HgController`에
    "거짓 안전감 방지" 주석을 명시적으로 남김 — 멤버십/읽기전용 Deploy Key 수준의 접근 제어만
    강제되고, 브랜치별 세부 보호 정책(require_pull_request 등)은 Hg에 아직 없다.
  - **`.git` 접미사 버그 수정(`domain/project/ProjectServiceImpl.kt`)**: `acceptTransfer()`/
    `forkProject()` 둘 다 물리 디렉터리 경로를 vcs 종류와 무관하게 항상 `.git` 접미사로
    계산하던 것을, `baseDir` 판정 바로 옆에 동일한 `when` 분기로 `dirSuffix`(git→".git",
    SVN/Mercurial→"")를 추가해 수정. 부수 발견: 기존 `ProjectServiceImplSpec`의 SVN 관련 테스트
    2건이 이 버그와 **동일한** `.git` 접미사를 픽스처에 붙여두고 있어, 버그가 있어도 통과하는
    거짓 양성 테스트였다(수정 후 실패로 드러나 픽스처를 접미사 없는 실제 경로로 교정) — 이
    테스트들 자체가 "버그를 검증하는 게 아니라 재현하는" 상태였던 것도 이번에 함께 바로잡음.
    Mercurial 회귀 테스트 2건(이관/포크 각 1건) 신규 추가. `ProjectServiceImplSpec` 전체
    GREEN(91 tests).
  - **실제 `hg` 바이너리로 검증하는 통합 테스트**: `web/HgHttpProtocolIntegrationSpec.kt`(신규,
    `GitSmartHttpProtocolIntegrationSpec`과 동일한 `webEnvironment=RANDOM_PORT` 패턴) — PUBLIC
    프로젝트 익명 clone 1건, Basic 인증 멤버 push(별도 clone으로 서버 반영까지 재검증) 1건, 둘 다
    실제 `hg` 바이너리로 GREEN. `hgAvailable()` 가드로 `hg` 바이너리가 없는 환경에서는 스킵.
  - **SSH 쪽 남은 테스트 갭(P3-18에서 이월)**: `config/ssh/SshRelayServerIntegrationSpec.kt`에
    Hg 전용 거부 테스트 2건 추가 — PRIVATE 저장소 비멤버 clone 거부, 읽기전용 Deploy Key push
    거부(clone은 성공, push만 실패) — git 쪽의 대칭 테스트를 그대로 미러링. 실제 `hg`/`python3`
    바이너리로 GREEN(전체 10 tests).
  - **의도적으로 다루지 않은 것(정직하게 남김)**: push 시 알림/웹훅/`PushedBranch` 추적
    이벤트 발행(`YonaPostReceiveHook` 대응물)은 사용자가 확정한 2라운드 범위(HTTP/SSH 서빙 +
    브랜치/태그 CRUD)에 명시적으로 포함되지 않아 이번에도 손대지 않음 — Hg push는 알림/웹훅
    없이 조용히 반영된다(후속 라운드 과제로 남김). `getDiff`/`getPatch`/`getArchive`는 지시대로
    범위 밖 유지(throw/no-op 그대로).
  - 검증: `./gradlew test --tests "com.github.yonaprojects.yona.domain.vcs.Hg*" --tests
    "com.github.yonaprojects.yona.web.Hg*" --tests "com.github.yonaprojects.yona.config.hg.*"
    --tests "com.github.yonaprojects.yona.domain.project.ProjectServiceImplSpec" --tests
    "com.github.yonaprojects.yona.config.ssh.*" --tests "com.github.yonaprojects.yona.YonaApplicationTests"
    -Dyona.it.db=h2` 전부 GREEN(샌드박스에 Docker 미접근이라 h2 프로파일 사용, 단 실제 `hg`/
    `python3` 바이너리는 이 환경에 설치돼 있어 관련 통합 테스트는 스킵되지 않고 실제로 실행·검증됨).

- **2026-09-08 코디네이터 리뷰에서 실측으로 발견한 실제 보안 결함(push 전 확인·수정)**:
  `HgAuthorizationFilter.isWriteRequest()`가 hg wire protocol v1의 `pushkey` 명령을 읽기로
  잘못 분류하고 있었다 — `pushkey`는 서버 저장소 상태를 실제로 변경하는(북마크 생성/이동/삭제,
  `Wire2Commands.applyPushkey()`가 `.hg/bookmarks` 파일을 직접 덮어씀) push 권한 명령이다
  (실제 hg 소스 `mercurial/hgweb/common.py`의 `checkauthz`가 "push requires POST request"로
  강제하는 대상이 `unbundle`과 `pushkey` 둘 다임을 hg4j의 `HgRemoteClient.java` 기존 주석에서
  확인). 이걸 읽기로 잘못 분류하면 PUBLIC Mercurial 프로젝트에서 **인가 절차 없이**
  `GET /hg/{owner}/{project}?cmd=pushkey&namespace=bookmarks&key=<name>&old=&new=<hash>`
  하나로 익명 사용자가 북마크를 조작할 수 있는 실제 우회로가 있었다(인가 검사가 통째로
  스킵됨 — clone/push 정상 시나리오만으로는 이 경로를 아예 타지 않아 통합테스트로도 못
  잡혔음). `isWriteRequest()`에 `pushkey`도 `unbundle`과 함께 쓰기로 분류하도록 수정하고,
  회귀 테스트(`HgAuthorizationFilterSpec`, PUBLIC 프로젝트의 `cmd=pushkey`가 익명 사용자에게
  401을 응답하는지) 추가 — 전체 11 tests GREEN 재확인.

- **2026-09-08 사용자 요청으로 Hg SSH도 실제 컨테이너(sshd+yona)에 end-to-end 검증 — 심각한
  버그 발견·수정(hg4j 라이브러리 자체)**: git과 마찬가지로 Hg도 실제 sshd 컨테이너를 띄워
  `ssh-auth.sh`/forced command/소켓 릴레이 전체 경로로 실제 `hg` 바이너리 clone/push를
  검증했다. 커밋이 있는 저장소는 바로 정상 동작했지만, **yona 웹 UI로 방금 막 만든(커밋
  0개) 신규 Mercurial 프로젝트를 최초로 clone하면 SSH·HTTP 둘 다 100% 재현되게
  hang/abort**하는 심각한 버그를 발견했다 — yona 쪽 코드가 아니라 `../hg4j`(우리 자체
  라이브러리)의 `HgLocalClient.getBundle()`이 보낼 게 없을 때(빈 changelog, 또는 이미
  최신 상태인 증분 pull) `new byte[0]`을 그대로 반환해 real hg 클라이언트가 정상 응답과
  스트림 절단을 구분 못 하는 무효한 프로토콜 응답이 원인이었다. **모든 신규 Mercurial
  프로젝트는 항상 이 커밋 0개 상태로 시작하므로, 이 버그가 있으면 아무도 새로 만든
  Mercurial 저장소를 처음 clone할 수 없었다** — 실제로 컨테이너를 띄워 진짜로 clone을
  시도해보지 않았다면 발견하지 못했을 버그.

  hg4j 저장소(`../hg4j`, 별도 git 히스토리)에서 TDD로 수정: 세 조기 반환을 모두 제거해
  정상적인 봉투 처리 흐름(빈 changegroup도 올바르게 HG20/HG10UN으로 감싸짐)을 타도록
  고쳤다. 실제 hg CLI로 "빈 저장소 clone", "무변경 pull", "빈 저장소로의 첫 push" 3개
  시나리오 회귀 테스트 추가(전부 GREEN) + 기존 단위테스트 3건도 "빈 바이트 배열"이 아니라
  "올바르게 형식을 갖춘 빈 번들"을 검증하도록 교정. 상세: hg4j의
  `llm-wiki/known-bugs-registry.md` `HgLocalClient.getBundle()` 항목, 커밋 `82f705b`
  (hg4j 저장소, push 완료).

  수정된 hg4j로 yona를 다시 빌드해 **같은 컨테이너 환경에서 원래 hang하던 시나리오를
  그대로 재현 → 정상 clone/push로 바뀌었음을 재확인**(신규 빈 프로젝트 clone + 그 위에
  첫 커밋 push, SSH 경로 둘 다 GREEN). yona 쪽 코드 변경은 불필요(`includeBuild`로 hg4j
  수정이 자동 반영됨).

- **2026-09-08 (같은 날 재검증) — P3-19(hg4j GPG 서명) 변경 이후 회귀 여부를 real 컨테이너로
  재확인하던 중, `ssh-auth.sh`(P3-18에서 만든 forced command 스크립트) 자체의 진짜 버그를
  새로 발견·수정**: 빈 저장소 clone/push는(getBundle 버그 수정 이후) 여전히 정상이었지만,
  **PRIVATE 프로젝트 비멤버가 hg로 clone을 시도하면 15초+ 무한 대기(hang)** 하는 걸 처음
  발견했다(예전엔 이 정확한 시나리오를 실제 `hg` CLI로 검증한 적이 없었다 — 자동화 테스트
  `SshRelayServerIntegrationSpec`은 python3로 시뮬레이션한 클라이언트였어서 이 클래스의
  버그를 못 잡았음).

  근본원인은 yona 코드가 아니라 `ssh-auth.sh`(문서에만 존재하는 시스템 관리자용 스크립트)
  자체에 있었다: `{ printf '<핸드셰이크>'; cat; } | socat - UNIX-CONNECT:$RELAY_SOCKET`처럼
  순수 셸 파이프로 짜여 있었는데, yona가 거부 사유(`ERR ...`)를 쓰고 소켓을 즉시 닫으면
  socat은 곧바로 정상 종료하지만, 그 socat과 파이프로만 연결된 **별개 프로세스인 `cat`은
  socat이 끝난 걸 전혀 모른 채 "클라이언트가 다음에 보낼 stdin"을 계속 blocking read로
  기다린다** — 그 결과 forced command 전체(bash → cat)가 끝나지 않아 sshd가 클라이언트
  쪽에 채널 EOF/close를 못 보내고, 실제 `hg` 클라이언트는 하염없이 응답을 기다리며 완전히
  멈춘다. git이 우연히 이 문제를 피해간 건 git 클라이언트가 `ERR` 줄을 받자마자 스스로 즉시
  포기하고 연결을 닫아버리기 때문(그래서 "지저분하지만 빠른" 실패)이지, 서버 쪽 설계가
  안전해서가 아니었다 — push 거부(브랜치 보호 등, 프로토콜이 정상적으로 끝까지 흐르는 경로)는
  이 문제 자체가 없다.

  실제 컨테이너(`ps auxf`)로 "소켓 반대편 socat은 이미 종료됐는데 `cat`만 좀비 아닌
  살아있는 상태로 blocking read 중"임을 직접 확인해 근본원인을 확정한 뒤, 이름있는 파이프
  (FIFO)로 그 `cat`을 담은 하위 프로세스를 완전히 분리해 PID를 직접 쥐고 있다가 socat이
  끝나자마자 강제 종료하도록 고쳤다. 이 과정에서 **2차 회귀**도 하나 발견·수정: 그 하위
  프로세스를 단순히 `&`로 백그라운드에 두면 bash가 잡 컨트롤이 꺼진 비대화형 셸에서
  백그라운드 명령의 표준입력을 자동으로 `/dev/null`로 바꿔치기해버려(bash 매뉴얼
  "asynchronous commands"), 이번엔 거꾸로 **정상 허용되는 clone/push 케이스가 즉시
  "no suitable response"로 깨졌다** — `set -m`으로 잡 컨트롤을 켜서 해결(`disown`은
  잡 컨트롤의 부작용인 "[1]+ Done ..." 알림이 클라이언트에 새는 걸 막는 화장용 처리).

  수정 후 실제 컨테이너에서 4가지 시나리오 전부 재확인: (1) 신규 빈 프로젝트 clone(0.4초,
  정상), (2) 그 위에 커밋 push(0.4초, 정상), (3) PRIVATE 비멤버 clone 거부(1초 미만, 깔끔한
  실패 — 더 이상 hang 없음), (4) push된 내용이 실제로 정확히 반영됐는지 재-clone으로 확인
  (정상). 상세 원인 설명은 `docs/guide/ssh-system-sshd-setup.md` Step 3의 forced_command
  코드 블록 주석 + 트러블슈팅 절에 반영. yona 애플리케이션 코드 변경은 없음(순수히 시스템
  관리자 배포 스크립트 문서의 버그).

- **2026-09-09 — 1라운드부터 의도적으로 미뤄뒀던 diff/patch 구현 완료(사용자 요청)**:
  `HgRepository.getDiff(commitId)`/`getDiff(revA, revB)`/`getPatch(commitId)`/
  `getPatch(revA, revB)`가 지금까지 전부 `UnsupportedOperationException`이었다 — 원래는
  "커밋 상세 페이지 404"로만 보였지만, 실제로는 같은 인터페이스 메서드를 쓰는
  `CompareViewController`(리비전 범위 비교), `PullRequestServiceImpl`(PR diff — 결과를
  `as List<FileDiff>`로 언체크 캐스트하므로 진짜 `FileDiff` 인스턴스가 아니면 여기서
  `ClassCastException`), `NotificationMessageResolver`(알림 메일 diff 요약)까지 전부 영향권—
  Mercurial 프로젝트는 커밋 상세/PR diff/비교뷰/알림 메일 diff 요약이 죄다 깨져 있었다.

  구현은 `GitRepository.kt`의 `getFileDiffs()`/`getPatch()` 패턴을 그대로 따른다 —
  `FileDiff.kt`가 JGit의 `RawText`/`EditList`/`DiffEntry.ChangeType`을 직접 쓰는(git 오브젝트
  DB에 결합돼 있지 않은 순수 라인단위 diff 자료구조) 덕분에, hg4j에서 얻은 파일 바이트에
  JGit의 diff 엔진(`DiffAlgorithm.HISTOGRAM`)을 그대로 돌려 동일한 `FileDiff` 객체를 만들 수
  있다. 변경 파일 목록/변경 종류(ADD/MODIFY/DELETE)는 hg4j에 이미 있던 `DiffCommand`(매니페스트
  비교 로직 재사용)로, 파일별 전/후 바이트는 `CatCommand`로 가져온다. 구현 중 hg4j
  `DiffCommand`의 실제 결함(2차 발견)도 하나 찾았다 — `oldRevision`의 "리비전 없음" sentinel은
  -1이 정확히 "빈 매니페스트"로 처리되는데 `newRevision`은 자기 생성자 주석대로 -1이 "tip으로
  대체"를 뜻해, `getDiff(revA, revB)`에서 revB가 존재하지 않는 리비전일 때 이걸 그대로 넘기면
  조용히 엉뚱하게 tip과의 diff가 나오는 버그였다. **처음엔 yona 쪽에서 `newRevNum < 0`이면
  `DiffCommand`를 아예 안 거치고 old쪽 매니페스트 전체를 DELETE로 직접 합성하는 우회로
  처리했으나(2026-09-09 오전), 사용자 지시로 우회 대신 hg4j 자체의 근본 원인을 수정**했다 —
  `DiffCommand`의 "값 미지정"(not-set, 호출자가 `setNewRevision()`을 아예 안 부른 경우) sentinel을
  `-1`에서 `Integer.MIN_VALUE`로 분리해 `oldRevision`과 같은 방식(값 미지정 sentinel ≠ "빈
  매니페스트"를 뜻하는 -1)으로 맞췄다(`setNewRevision((NodeId) null)`도 동일하게 새 sentinel을
  쓰도록 함께 수정, `IOException` 폴백은 기존 그대로 -1 유지 — `oldRevision`의 대칭 메서드와 동일
  관례). 그 결과 yona `HgRepository.computeChangedEntries()`의 우회 코드를 전부 제거하고 항상
  `hg.diff().setOldRevision(oldRevNum).setNewRevision(newRevNum).call()`을 그대로 호출하도록
  단순화 — old/new 둘 다(또는 한쪽만) -1이어도 hg4j가 알아서 정확히 처리한다. hg4j에 회귀
  테스트 4개 추가(값 미지정→tip 기본값 유지 확인 포함, `DiffCommandCoverageTest`) — 전부 GREEN.
  덤으로 이 수정은 `getPatch(revA, revB)`도 정확하게 만든다 — 우회 경로는 `diffContent`가 항상
  빈 문자열이라 revB가 없는 patch 뷰가 헤더만 있고 본문이 비어 있었는데, 이제 hg4j가 실제로
  빈 매니페스트 대비 통합 diff 본문을 만들어준다.

  실사용 검증(실제 앱 기동, H2): 새 Mercurial 프로젝트에 add/modify/remove 커밋 4개를 실제
  `hg push`로 만들어 넣고, 각 커밋의 `/{owner}/{project}/commit/{id}` 페이지를 curl로 받아
  렌더링된 HTML에서 실제 +/- 라인 내용을 직접 확인(예: `modify file1` 커밋에서
  `<pre class="diff-partial-codeline">+CHANGED</pre>`가 실제로 나옴) — 4개 커밋 전부 200 OK +
  올바른 diff 렌더링, 더 이상 404 없음. `HgRepositorySpec.kt`에 회귀 테스트 259줄 추가, GREEN.

- **2026-09-09 — hg4j `PushCommand`가 인증 필요한 목적지에 push 못 하던 문제 수정(사용자 요청,
  hg4j 저장소 커밋)**: 실제 hg 자신의 URL 관례(`https://user:pass@host/path`)를 `HgRemoteClient`
  생성자가 전혀 파싱하지 않아, 이 방식으로 인증하려는 모든 HTTP(S) 목적지 push/pull이 조용히
  Authorization 헤더 없이 나가 401/403으로 실패했다(SSH 쪽 `HgSshClient`는 이미 정확했음).
  `HgRemoteClient(String url)` 생성자에서 authority의 `user[:pass]@`를 직접 파싱해
  `username`/`password`를 채우고 userinfo가 빠진 URL을 실제 `baseUrl`로 쓰도록 수정 — 이미
  있던 `executeGet`/`executePost`의 Authorization 헤더 로직은 그대로 재사용(호출부 변경 없음).
  실제 `HttpServer`가 Basic 인증을 강제하는 통합 테스트(`HgRemoteClientTest.
  testHgRemoteClientEmbeddedUrlCredentials` — 올바른/틀린/무자격증명 3가지 케이스)로 검증,
  hg4j 전체 스위트 재실행(2301/2302 GREEN, 나머지 1개는 이 변경 이전에도 실패하던 무관한
  기존 결함 `Wire1CommandsCoverageTest`). 코디네이터가 직접 실제 앱을 기동해 `PushCommand`
  API(단위 `HgRemoteClient` 테스트만이 아니라 `Hg.open(repo).push().setDestination(
  "http://user:pass@localhost:8080/...").call()` 전체 호출 경로)로 URL에 자격증명을 담아
  push해 실제로 인증·반영됨을 재확인(응답: `push successful, imported 1 changesets natively`,
  커밋이 실제로 저장소 히스토리에 나타남). 상세: `hg4j/llm-wiki/known-bugs-registry.md`.
  yona 애플리케이션 코드 변경 없음(순수 hg4j 라이브러리 버그).
