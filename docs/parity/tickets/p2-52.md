---
id: p2-52
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:253 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-52: **(2026-08-26 골든 체크 버킷C 사람 검토에서 발견, 2026-08-26 TDD로 완료)** `IssueFilterType`(assigned/created/mentioned/favorite/all 조합) 기반 "내 이슈 전체 조회" API가 yona에 없음. **재검증으로 범위 축소**: 같은 티켓에 묶여있던 `Matching`(Ebean 동적쿼리 enum)과 `Resource`(리소스 직렬화 추상클래스)는 실사용처가 각각 `Milestone.java`(JPA `findByProjectAndState` 컨벤션으로 대체됨)와 `WebhookNotificationEventListener.resolveResource()`류(타입별 Repository 개별 호출로 대체됨)임을 확인해 CLOSED로 판정, `IssueFilterType`만 구현

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/models/enumeration/IssueFilterType.java`, `app/models/support/IssueSearchCondition.java`, `app/controllers/api/UserApi.java:129-186`(`getIssuesByUser`/`issuesAsJson`), `conf/routes:80`
- **yona 대상**: `domain/issue/{IssueFilterType,IssueService,IssueServiceImpl,IssueRepository}.kt`, `web/FavoriteController.kt`(신규 `GET /-_-api/v1/user/issues`)
- **비고(원문)**: **완료** — `MENTIONED`/`FAVORITE` 필터에 필요한 `MentionService.getMentioningIssueIds()`/`FavoriteIssueRepository.findByUserId()`는 이미 이전 세션에서 포팅되어 있었음(재사용). `ASSIGNED`용 `IssueRepository.findByAssignee_UserId()` 신규 추가, `IssueServiceImpl.getIssuesByFilter()`가 4개 필터를 조합(`ALL`은 합집합+`distinctBy(id)`+`updatedDate desc` 정렬). 컨트롤러는 legacy `issuesAsJson()`과 동일한 JSON 구조(`result` 배열, author/assignee/project/owner/refUrl)로 응답, `page`/`pageNum` 쿼리 파라미터로 메모리 페이지네이션. TDD로 필터 4종+ALL 합집합/중복제거, 컨트롤러 JSON 응답, 페이지네이션, 잘못된 filter 값 400 응답까지 검증(`IssueServiceSpec.kt`/`IssueFilterTypeSpec.kt`/`FavoriteControllerSpec.kt`)

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
