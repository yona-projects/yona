---
id: p1-134
type: ticket
status: done
created: 2026-08-21
updated: 2026-08-21
relates_to: []
source: docs/PARITY_BACKLOG.md:187 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-134: **(2026-08-21 백엔드 전수 감사에서 발견 — 웹훅 도메인)** Hangout Chat 스레드 키가 댓글 이벤트에서 부모 리소스가 아닌 댓글 자신으로 계산돼 스레드 그룹핑 무력화 — 재확인 결과 범위가 이슈/게시글 댓글보다 넓음: yona `Webhook.java:480`은 PR 리뷰 댓글(REVIEW_COMMENT)도 부모 PullRequest를 스레드 키로 쓰는데, yona `WebhookNotificationEventListener.resolveResource()`는 REVIEW_COMMENT/COMMIT_COMMENT 둘 다 댓글 자신을 반환해 동일 패턴이 재발함(단, CommitComment는 yona Webhook.java에 대응 오버로드 자체가 없어 이 부분만은 회귀보다 "yona 신규 기능의 자기 불일치"에 가까움).

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `Webhook.java`(threadJSON)
- **yona 대상**: `WebhookServiceImpl.threadKeyOf()`(신규)
- **비고(원문)**: **완료(아래 완료 로그 참고, [[tickets/p1-143|P1-143]] 직후 처리)**

## 완료 로그 (원문, `PARITY_BACKLOG.md`의 "완료 로그" 섹션에서 이관)

- **2026-08-21 — P1-134**: `WebhookServiceImpl`에 새 private `threadKeyOf(resource)`를 추가해, Hangout Chat 스레드 키를 댓글 자신이 아니라 부모 리소스 기준으로 계산하도록 수정(yona `Webhook.java:346` `eventComment.getParent().asResource()` / `:480` `eventPullRequest.asResource()` 대응). [[tickets/p1-143|P1-143]](스레드 저장 경로) 직후 곧바로 처리 — 저장 경로가 없으면 키를 고쳐도 검증할 방법이 없었기 때문.
  - IssueComment → 부모 Issue(ISSUE_POST), PostingComment → 부모 Posting(BOARD_POST), ReviewComment → `thread.pullRequest`(PULL_REQUEST). CommitComment는 yona `Webhook.java`에 대응 이벤트 자체가 없어([[tickets/p2-18|P2-18]]) 부모 매핑 규칙이 없으므로 자기 자신의 키(COMMIT_COMMENT)를 그대로 사용 — 레거시에 없는 매핑을 새로 만들지 않았다. Issue/Posting/PullRequest 등 비-댓글 리소스는 기존과 동일하게 자기 자신의 키를 쓴다.
  - `buildPayload()`의 DETAIL_HANGOUT_CHAT 조회(기존 `webhookThreadRepository.findByWebhookIdAndResourceTypeAndResourceId`)와 `recordHangoutThreadIfNeeded()`([[tickets/p1-143|P1-143]]에서 신설한 저장)가 동일한 `threadKeyOf()`를 공유해 read/write 키가 항상 일치하도록 함.
  - 테스트: `WebhookServiceSpec.kt` +3(이슈 댓글은 부모 이슈 키로 조회·저장/PR 리뷰 댓글은 부모 PR 키로 조회·저장/CommitComment는 부모 매핑이 없어 자기 자신 키 유지 — 레거시 부재 동작 회귀 방지). 전체 통과(25건).
