---
id: p1-83
type: ticket
status: done
created: 2026-08-21
updated: 2026-08-21
relates_to: []
source: docs/PARITY_BACKLOG.md:136 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-83: 검색 시 "본인이 작성했으면 프로젝트 접근권한과 무관하게 노출"되는 예외가 게시글(Posting)·이슈댓글(IssueComment)·게시글댓글(PostComment)·코드리뷰댓글(ReviewComment) 검색에는 전혀 적용되지 않음(이슈는 [[tickets/p1-81|P1-81]]에서 이미 해결)

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/models/Search.java:251 postsEL()`(`equalsUserTemplate(..., DEFAULT_PATH_TO_AUTHOR, containsKeywordInPosting)`), `:599 issueCommentsEL()`, `:655 postCommentsEL()`, `:712 reviewCommentsEL()`(전부 작성자 본인이면 프로젝트 접근권한과 무관하게 검색 노출 — 이슈만 author+assignee 이중, 나머지는 author만)
- **yona 대상**: `domain/board/PostingRepository.kt`, `domain/issue/IssueCommentRepository.kt`, `domain/board/PostingCommentRepository.kt`, `domain/pullrequest/ReviewCommentRepository.kt`, `domain/support/SearchServiceImpl.kt`
- **비고(원문)**: **완료** — 아래 완료 로그 참고

## 완료 로그 (원문, `PARITY_BACKLOG.md`의 "완료 로그" 섹션에서 이관)

- **2026-08-21 — P1-83**: 검색 시 본인 작성 게시글/이슈댓글/게시글댓글/코드리뷰댓글이 프로젝트 접근권한과 무관하게 노출되도록 이식(이슈는 [[tickets/p1-81|P1-81]]에서 이미 완료).
  - Serena LSP로 yona `Search.java`의 `postsEL()`/`issueCommentsEL()`/`postCommentsEL()`/`reviewsEL()`(각 0번째 오버로드, keyword+user 버전)을 심볼 단위로 직접 대조 — 전부 `equalsUserTemplate(keyword, user, junction, pathToUserId, ...)` 패턴(익명이면 스킵, `pathToUserId==user.id AND keyword`)임을 확인. `ReviewComment`만 `DEFAULT_PATH_TO_AUTHOR`("authorId") 대신 `"author.id"`를 쓴다는 차이를 발견(임베디드 `UserIdent`).
  - `PostingRepository`/`IssueCommentRepository`/`PostingCommentRepository`/`ReviewCommentRepository`의 `searchX`/`countSearchX` 8개 메서드에 `userId: Long?` 파라미터 추가, WHERE절을 [[tickets/p1-81|P1-81]]과 동일한 `(프로젝트 접근권한 AND 키워드) OR (:userId IS NOT NULL AND author(Id) = :userId AND 키워드)` 형태로 재작성(`ReviewComment`는 `rc.author.id`).
  - `SearchServiceImpl`의 `searchInAll`/`searchInAGroup`/`getSearchResultCounts`에 `user?.id`를 4개 리포지토리 호출 전부에 관통 전달.
  - 테스트: `PostingRepositorySpec.kt`에 P1-83 케이스 3건 추가, `IssueCommentRepositorySpec.kt`/`PostingCommentRepositorySpec.kt`/`ReviewCommentRepositorySpec.kt` 신규 각 3건(접근불가 프로젝트+본인작성 노출/무관한 제3자는 비노출/비로그인은 프로젝트 필터링만), 기존 `SearchServiceSpec.kt`의 `countSearchX` 스텁 16곳에 `userId` 인자 추가.
  - 검증: `./gradlew test --tests "...PostingRepositorySpec" --tests "...IssueCommentRepositorySpec" --tests "...PostingCommentRepositorySpec" --tests "...ReviewCommentRepositorySpec" --tests "...SearchServiceSpec"` 전체 통과.
