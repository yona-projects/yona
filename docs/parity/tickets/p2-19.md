---
id: p2-19
type: ticket
status: done
created: 2026-08-21
updated: 2026-09-26
relates_to: []
source: docs/PARITY_BACKLOG.md:220 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-19: **(2026-08-21 백엔드 전수 감사에서 발견 — 조직 도메인)** 조직명 변경 시 `FavoriteOrganization.organizationName` 비정규화 필드 동기화 누락

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `FavoriteOrganization.java`
- **yona 대상**: `OrganizationServiceImpl.updateOrganizationSettings()`
- **비고(원문)**: **완료(아래 완료 로그 참고)**

## 완료 로그 (원문, `PARITY_BACKLOG.md`의 "완료 로그" 섹션에서 이관)

- **2026-08-21 — P2-19**: `OrganizationServiceImpl.updateOrganizationSettings()`에 yona `FavoriteOrganization.java:38-46` `updateFavoriteOrganization()` 대응 — 조직명이 바뀌면 그 조직을 즐겨찾기한 모든 `FavoriteOrganization` 행의 비정규화된 `organizationName`도 함께 갱신(안 하면 즐겨찾기 화면에 옛 조직명이 남는다). `FavoriteOrganizationRepository`(이미 존재하는 `findByOrganizationId`) 주입 추가.
  - 테스트: `OrganizationServiceSpec.kt` +1(조직명 변경 시 즐겨찾기한 사용자의 organizationName도 갱신). 전체 통과.

## 2026-09-26 — 조직 개명 시 하위 프로젝트도 이전

- v1.16.0 `Organization.updateWith/updateProjects`와 대조: 즐겨찾기 이름만 갱신하는 것으로는
  충분하지 않았다. 조직 개명 후 프로젝트 `owner`와 저장소 경로가 옛 이름에 남아 새 URL은 404였다.
- `OrganizationServiceImpl`에서 하위 프로젝트의 코드·위키 저장소, LFS 데이터, 소유자·프로젝트 즐겨찾기를 함께 이전한다.
  기존 프로젝트 이전과 같은 이전 URL 보존 정책을 사용한다. 개명 대상 형식·예약어를 먼저 검증하고,
  기존 목적지 저장소는 덮어쓰지 않는다. 트랜잭션 실패 시 이미 옮긴 저장소를 역순으로 복구한다.
- 검증: `OrganizationRenameSpec`, `OrganizationServiceSpec` **55 tests passed**.
  실제 H2 서버에서 Git 저장소 두 개를 가진 조직을 개명하고 새 프로젝트 API·옛 웹 URL의 200,
  코드·위키 각각의 새 Git HTTP URL에서 `git ls-remote`, 기존 커밋 SHA와 README 바이트 보존,
  LFS 데이터 바이트 보존, 옛 디렉터리 제거를 확인했다. 잘못된 이름은 400, 설명만 변경하는 요청은 200이었다.
- 한계: DB와 파일시스템 사이의 프로세스 중단까지 원자적으로 처리하지는 않는다.
  실행 중 롤백은 보상 이동하며, 보상 이동도 실패하면 데이터를 삭제하지 않고 복구 경로를 로그에 남긴다.
