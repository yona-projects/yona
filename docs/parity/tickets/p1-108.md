---
id: p1-108
type: ticket
status: done
created: 2026-08-21
updated: 2026-08-21
relates_to: []
source: docs/PARITY_BACKLOG.md:161 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-108: **(2026-08-21 [[tickets/p1-104|P1-104]] 구현 중 부수 발견)** 조직(Organization)명에도 로그인ID와 동일한 문자 제약(`LOGIN_ID_PATTERN`)이 원본에 있으나 yona에는 이식돼 있지 않음 — yona 조직 생성은 예약어 체크만 수행하고 형식(정규식) 검증 자체가 없어, 로그인ID라면 거부될 공백·슬래시 등 임의 문자가 포함된 조직명도 생성이 통과될 수 있음(URL 라우팅과 충돌 소지, [[tickets/p1-104|P1-104]]와 동일한 위험)

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/models/Organization.java:42`(`@Constraints.Pattern(User.LOGIN_ID_PATTERN)`로 `User.java`의 동일 상수를 재사용)
- **yona 대상**: `domain/organization/OrganizationServiceImpl.kt`
- **비고(원문)**: **완료** — 아래 완료 로그 참고

## 완료 로그 (원문, `PARITY_BACKLOG.md`의 "완료 로그" 섹션에서 이관)

- **2026-08-21 — P1-108**: 조직명 형식(정규식) 검증을 [[tickets/p1-104|P1-104]]의 `LoginIdFormatValidator` 재사용으로 이식.
  - Serena LSP로 yona `models/Organization.java:42`를 재확인 — `@Constraints.Pattern(User.LOGIN_ID_PATTERN)`으로 `User.java`의 동일 상수를 그대로 재사용함을 확인(별도 정규식이 아님).
  - `OrganizationServiceImpl.createOrganization(name, descr, creatorId)`에 예약어 체크([[tickets/p2-01|P2-01]])보다 앞서 `LoginIdFormatValidator.isValid(name)` 검사 추가, 실패 시 기존 중복/예약어 체크와 동일하게 `IllegalArgumentException`을 던져 컨트롤러의 공용 `catch (e: Exception)` 처리(400 Bad Request)로 자연스럽게 흡수되도록 함(컨트롤러 변경 불필요).
  - 테스트: `OrganizationServiceSpec.kt` +2(공백 포함 조직명 거부/영문·숫자·하이픈 조직명 정상 생성).
  - 검증: `./gradlew test --tests "...OrganizationServiceSpec"` 전체 통과, 이어서 `./gradlew test` 전체 1022 tests 통과(회귀 없음 확인).
