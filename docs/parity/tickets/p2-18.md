---
id: p2-18
type: ticket
status: done
created: 2026-08-21
updated: 2026-08-21
relates_to: []
source: docs/PARITY_BACKLOG.md:219 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-18: **(2026-08-21 백엔드 전수 감사에서 발견 — PR/코드리뷰 도메인)** `getCommitComments()`(SVN 커밋코멘트 ↔ PR 매핑) 대응 부재

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `PullRequest.java`
- **yona 대상**: (대응 없음)
- **비고(원문)**: **완료(조사 결과 이식 불필요로 판정, 아래 완료 로그 참고)**

## 완료 로그 (원문, `PARITY_BACKLOG.md`의 "완료 로그" 섹션에서 이관)

- **2026-08-21 — P2-18(이식 불필요로 판정)**: `PullRequest.java:850-853` `getCommitComments()`(`CommitComment.findByCommits(fromProject, pullRequestCommits)`)를 Serena로 전체 코드베이스(`.java` 및 템플릿 포함, 코드 파일 제한 해제)에서 참조 검색한 결과, 정의부 자기 자신 외에 **호출부가 단 한 곳도 없음**을 확인 — 어떤 컨트롤러도 템플릿도 이 메서드를 쓰지 않는다. `CommitComment.findByCommits()` 자체도 다른 호출자가 없다. 즉 yona 자체에서 이미 죽은 코드(사용되지 않는 헬퍼)라, 실제로 사용자가 접하는 어떤 화면·API도 이 기능에 의존하지 않는다. yona가 노출하지 않는 기능을 yona에 새로 노출시키는 건 "레거시에 없는 동작 추가"에 해당하므로 이식하지 않음.
