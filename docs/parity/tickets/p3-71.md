---
id: p3-71
type: ticket
status: resolved
created: 2026-09-14
updated: 2026-09-15
relates_to: [p3-70]
source: "P3-70 라운드5(jQuery→vanilla 전환, project.New.js/project/importing.html) 작업
  중 TDD 에이전트가 발견 — 전환 자체가 만든 새 버그가 아니라 원본 jQuery 코드에도 이미
  있었을 구조적 결함을 1:1로 정확히 재현한 것임을 소스 레벨로 증명"
---

# P3-71: 프로젝트 가져오기 화면의 "저장소 인증정보" 체크박스가 반전 동작함

## 배경

`project/importing.html`(레포지토리 가져오기 화면)에서 "인증정보 사용" 체크박스를 클릭하면
`#repoAuth` 패널이 슬라이드로 열려야 하는데, 실제로는 **체크하면 닫히고 언체크하면
열리는 반전 동작**을 한다.

## 근본 원인(P3-70 라운드5에서 소스 레벨로 확정)

`project/importing.html`의 `$(function(){...})`(현재는 vanilla로 전환됨)가 같은
`#useRepoAuth`의 `change` 이벤트에 핸들러를 **두 개** 등록한다:

1. 템플릿 자체의 단순 핸들러(먼저 등록) — 체크 시 `#repoAuth`를 즉시 보이는 상태로 만듦.
2. `$yona.loadModule("project.New", ...)`가 붙이는 `project.New.js`의
   `_attachEvent()`(나중에 등록) — `.toggle("slide")`(jQuery UI) 애니메이션으로
   열고/닫는다.

jQuery UI의 `.toggle(effect)`는 `lib/jquery/jquery-ui.custom.js:11151`에서 **호출되는
바로 그 순간의 현재 가시성**을 보고 열지/닫을지를 정한다
(`mode = $(this).is(':visible') ? 'hide' : 'show'`). 두 핸들러가 등록 순서대로 실행되므로:

1. 체크 → 핸들러1이 `#repoAuth`를 먼저 "보임"으로 만듦
2. 곧바로 핸들러2의 `.toggle("slide")`가 "지금 이미 보인다"고 판단해 **반대 방향(닫힘)**
   애니메이션을 실행

언체크도 대칭적으로 반전된다. **이 결함은 vanilla 전환 이전(순수 jQuery 시절)부터
있었다** — git 히스토리상 두 핸들러가 같은 이벤트에 이중으로 걸리는 구조 자체가 원래
설계였고, P3-70은 이 구조를 그대로(반전 동작까지 포함해) 정확히 이식했을 뿐이다.

## 완료 기준

- [x] 두 핸들러 중 하나로 통합하거나(권장: `project.New.js`의 `_toggleSlide`가
      `#useRepoAuth.checked`를 직접 참조해 판단하도록 하고, 템플릿의 단순 핸들러는
      제거) 등가의 방법으로 반전을 해소.
- [x] 체크 시 패널이 열리고 언체크 시 닫히는 것을 Playwright로 확인.
- [x] `disabled` 속성 토글(체크박스 상태와 직접 연동, 이 반전과 무관하게 이미 정상)은
      회귀 없이 유지.
- [x] 관련 스펙(`ProjectControllerSpec`/`ProjectViewControllerSpec`/
      `TemplateEquivalenceSpec`) 회귀 없음 확인.

## 수정 내용(2026-09-15)

권장안 그대로 핸들러를 하나로 통합했다:

- `project/importing.html`: `#useRepoAuth`의 `change`에 걸려있던 템플릿 자체의 단순
  show/hide 핸들러를 완전히 제거.
- `common/yona.project.New.js`: `_toggleSlide(el)` → `_toggleSlide(el, bShow)`로
  바꿔 "지금 보이는지"가 아니라 **목표 상태(bShow)를 직접 인자로 받아** 열지/닫을지
  판단하도록 했다(이미 목표 상태면 애니메이션을 건너뛰는 가드도 추가). 호출부
  `_onChangeRepoAuthCheck()`가 `#useRepoAuth.checked`를 그대로 `bShow`로 넘긴다.
  같은 함수에 템플릿에서 빠진 `authId`/`authPw` 초기화(언체크 시) 로직도 이식했다.

## 검증(2026-09-15)

- Playwright: 초기 상태(unchecked, `display:none`) → 체크(→`display:block`) →
  언체크(→`display:none`, `authId`/`authPw` 값 초기화) → 재체크(→다시 `display:block`,
  하위 input 전부 활성화)까지 전체 사이클 반전 없이 정상 동작 확인.
- 검증 도중 별개의 사전 존재 버그를 발견해 함께 수정: `project/importing.html:83`의
  `th:selected="*{owner == org.name}"`가 Thymeleaf 선택 표현식(`*{}`) 안에서 `th:each`
  지역 변수(`org`)를 폼 객체(`ImportForm`)의 필드로 잘못 해석해(`Property or field
  'org' cannot be found on object of type 'ImportForm'`) 소속 조직이 있는 사용자가
  이 화면에 접근하면 500 에러가 났다 - `${importForm.owner == org.name}`(전역 표현식)로
  수정.
- `ProjectControllerSpec`(54)/`ProjectViewControllerSpec`(191)/`TemplateEquivalenceSpec`(87)
  전부 GREEN.
