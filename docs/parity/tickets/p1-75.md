---
id: p1-75
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:128 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-75: **정정(2026-08-20)**: 재확인 결과 이미 구현돼 있었음(서비스 계층만 확인하고 컨트롤러 계층은 못 본 오탐) — `ProjectViewController.transferProject()`가 `sendTransferRequestMail()`로 확인 링크 메일을 목적지 사용자/조직 관리자에게 발송하는 로직이 이미 존재

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/controllers/ProjectApp.java:transferProject()`(`sendTransferRequestMail(pt)`)
- **yona 대상**: `web/ProjectViewController.kt:671,679-734 sendTransferRequestMail()`
- **비고(원문)**: **완료(검증만, 코드 변경 없음)** — 2026-08-20 구현 착수 전 재확인 과정에서 이미 동치임을 확인. `ProjectServiceImpl.kt`(서비스 계층)에는 이 로직이 없지만 `ProjectViewController.kt`(웹 계층)에 이미 완전히 구현돼 있어 최초 감사 시 계층을 잘못 짚은 오탐이었음

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
