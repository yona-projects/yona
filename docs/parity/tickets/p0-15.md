---
id: p0-15
type: ticket
status: done
created: 2026-08-19
updated: 2026-08-19
relates_to: []
source: docs/PARITY_BACKLOG.md:37 (표, P0 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P0-15: Board 라우트 누락 (postlabel)

## 원본 표 정보

- **우선순위 구분**: P0
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `conf/routes` (BoardApp)
- **yona 대상**: `web/BoardController.kt`
- **비고(원문)**: **완료** — `PUT /api/projects/{id}/posts/{postId}/labels`

## 완료 로그 (원문, `PARITY_BACKLOG.md`의 "완료 로그" 섹션에서 이관)

- **2026-08-19 — P0-15**: yona `api.BoardApi.updatePostLabel`(게시글에 붙은 라벨 집합을 통째로 교체)에 대응하는 라우트가 yona에 없던 문제 수정. `BoardController`에 `PUT /api/projects/{projectId}/posts/{postId}/labels`(JSON body: 라벨 ID 배열) 추가 — `postingService.getPosting`으로 대상 조회, `isManagerOrAuthor`로 권한 검사(기존 `updatePosting`과 동일 기준), `IssueLabelRepository.findAllById`로 라벨 엔티티들을 조회해 `posting.labels`를 교체 후 저장. `Posting.labels`/`posting_issue_label` 조인테이블 자체는 이미 존재했으나([[tickets/p1-19|P1-19]] 감사에서 확인) 이를 갱신하는 API가 없었던 것이 실제 격차였음. 테스트: `BoardControllerSpec.kt` +2 tests(정상 교체, 권한 없음 403).
