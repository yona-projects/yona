---
id: p2-50
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:251 (표, P2 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P2-50: **(2026-08-26 골든 체크 버킷C 사람 검토에서 발견, 2026-08-26 TDD로 완료)** ref 갱신 실패 시 전용 예외 타입 대신 일반 `IOException`을 재사용

## 원본 표 정보

- **우선순위 구분**: P2
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/errors/PullRequestException.java`
- **yona 대상**: `domain/pullrequest/{PullRequestService,PullRequestServiceImpl}.kt`
- **비고(원문)**: **완료** — `PullRequestService.kt`에 `PullRequestException(RuntimeException)` 신규 추가, `createMergeCommitAndUpdateRef()`의 `IOException` 대신 사용. TDD로 merged ref의 `.lock` 파일을 미리 점유해 JGit `RefUpdate`가 실제 `LOCK_FAILURE`를 반환하도록 강제하는 통합 테스트 작성(`PullRequestServiceSpec.kt` "merge - merged ref 갱신이 lock 충돌로 실패하면") — RED 확인 후 GREEN, 해당 throw 라인이 이번 세션 커버리지 작업에서도 도달 불가(mi=7)로 남아있던 것을 실제로 커버(javap 등 추가 확인 없이 jacoco로 직접 검증). legacy 조사 결과 이 예외 타입은 legacy에서도 `catch(PullRequestException)`으로 별도 분기된 적이 없어(`PullRequestActor.java:72`가 `catch(Exception)`으로 뭉뚱그림) 런타임 동작 차이는 없음(순수 타입 이식)

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
