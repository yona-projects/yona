---
id: p1-146
type: ticket
status: done
created: unknown
updated: unknown
relates_to: []
source: docs/PARITY_BACKLOG.md:199 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-146: **(2026-08-26 골든 체크 버킷C 사람 검토에서 발견)** 브랜치 갱신 시 관련 PR을 재검사(변경사항 반영/충돌 재확인)하는 이벤트가 실제 프로덕션 코드 경로에서 한 번도 발행되지 않음 — 핸들러(`PullRequestMergeEventListener.handleRelatedPullRequestMergeEvent`)는 있지만 실제 git push 경로 어디에서도 `RelatedPullRequestMergeEvent`를 `publishEvent`하지 않음(테스트에서만 직접 호출). 브랜치 삭제 시 PR 정리(다른 로직)는 정상 이식됨

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/playRepository/hooks/PullRequestCheck.java`(`onPostReceive`)
- **yona 대상**: `domain/vcs/GitPushHooks.kt`(`YonaPostReceiveHook.notifyRelatedPullRequestsForUpdatedBranches` 신규), `domain/event/PullRequestMergeEventListener.kt`(기존 핸들러 재사용)
- **비고(원문)**: **완료** — legacy `ReceiveCommandUtil.getUpdatedBranches()`(UPDATE/UPDATE_NONFASTFORWARD만, CREATE 제외)와 동일한 필터로 갱신된 브랜치마다 `RelatedPullRequestMergeEvent`를 발행하도록 배선. TDD로 RED 확인 후 구현, `GitPushHooksSpec` 그린(UPDATE/UPDATE_NONFASTFORWARD 발행, CREATE/DELETE 미발행 4개 케이스)

## 완료 로그

(원본에 별도 서술형 완료 로그 없음 — 위 비고가 전부)
