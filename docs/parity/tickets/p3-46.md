---
id: p3-46
type: ticket
status: open
created: 2026-09-10
updated: 2026-09-10
relates_to: [p3-45]
source: 사용자 제안 (2026-09-10, [[tickets/p3-45|P3-45]] 계획서 논의 중 파생)
---

# P3-46: 클라이언트 위젯 라이브러리 현대화 (2026-09-10 사용자 제안)

## 배경

[[tickets/p3-45|P3-45]](Tailwind CSS + htmx 템플릿 리팩터링) 계획서 작성 중, yona가 현재 쓰는
클라이언트 전용 위젯 라이브러리 상당수가 오래되거나 사실상 유지보수가 멈춘 것이 확인됐다. 사용자가
이를 지적하며 최신·활발히 유지되는 라이브러리로 교체할 것을 제안했다.

P3-45는 "화면 재설계 없이 CSS/상호작용 구현 방식만 교체"하는 낮은 리스크의 작업으로 범위를
한정하면서, 클라이언트 위젯 라이브러리 교체는 **비범위로 명시적으로 제외**했다 — 위젯 하나하나가
자체 API/이벤트 모델을 가진 별도 라이브러리라 교체마다 고유한 회귀 리스크가 있고, 182개 템플릿
리팩터링에 얹으면 리스크가 배가되기 때문이다. 이 티켓은 그 제외된 범위를 별도로 추적하기 위해
신설한다.

## 현재 상태(코드로 확인, 2026-09-10 기준)

| 용도 | 현재 라이브러리 | 위치 | 비고 |
|---|---|---|---|
| 드롭다운/멀티셀렉트 | Select2 | `static/javascripts/lib/select2/` | jQuery 플러그인, 오래됨 |
| 날짜 선택 | Pikaday | `static/javascripts/lib/pikaday/` | 유지보수 느림 |
| 이미지 크롭(프로필 사진 등) | Jcrop | `static/javascripts/lib/jquery/jquery.Jcrop.min.js`, `static/stylesheets/jcrop/` | 원 프로젝트 사실상 중단(~2015) |
| `@` 멘션 자동완성 | atjs(`jquery.atwho.js` 등) | `static/javascripts/lib/atjs/` | jQuery 플러그인 |
| 이미지 뷰어/라이트박스 | ViewerJS | `static/javascripts/lib/viewerjs/` | jQuery 래퍼(`jquery-viewer.js`) 포함 |
| 마크다운 렌더링 | marked.js | `static/javascripts/lib/marked.js` | 버전 미상이나 `sanitize`/`sanitizer` deprecation 경고 문자열로 미루어 0.7.0 전후 구세대 API(현재 v12+와 시그니처가 크게 다름) |
| 코드 문법 강조 | highlight.js | `static/javascripts/lib/highlight/highlight.pack.js` | **v9.11.0**(2016) — 현재 v11.x는 `highlightBlock()` 등 주요 API가 폐기/변경됨 |
| "마크다운 에디터" | 자체 구현(라이브러리 아님) | `site/layout.html:359` `th:fragment="markdownEditor"` | **리치 에디터가 아니라 plain `<textarea>` + Bootstrap 탭(Edit/Preview) + `autosize.js`(텍스트영역 자동높이) + 위 marked.js/highlight.js/atjs 조합의 수제 구현**임을 확인 — 교체 시 "라이브러리 스왑"이 아니라 상호작용 자체의 재설계에 가까움(아래 권장안 참고) |

## 권장 교체안(모두 jQuery 불필요, 활발히 유지보수 중 — 2026-09-10 시점 판단)

| 현재 | 권장 대안 | 비고 |
|---|---|---|
| Select2 | **Tom Select** 또는 **Choices.js** | 둘 다 무의존성, API가 Select2와 개념적으로 유사해 마이그레이션 부담이 상대적으로 낮음 |
| Pikaday | **Flatpickr** | 무의존성, 매우 활발한 유지보수 |
| Jcrop | **Cropper.js** | 무의존성, 사실상 업계 표준으로 자리잡음 |
| atjs | **Tribute.js** | 무의존성, textarea/contenteditable 양쪽 지원 |
| ViewerJS | **PhotoSwipe** 또는 (동일 계열 유지 시) **Viewer.js**(fengyuanchen, jQuery 래퍼 없이 순정 버전만 사용) | 현재 쓰는 파일명(`viewer.js`)이 fengyuanchen Viewer.js와 동일 계열로 보이므로, jQuery 래퍼(`jquery-viewer.js`)만 걷어내고 순정 버전으로 올리는 것도 저비용 대안 |
| marked.js(구버전) | **marked v12+**(같은 라이브러리 메이저 업그레이드) 또는 **markdown-it** | API 변경 폭 확인 필요(치환이 아니라 버전업 우선 검토) |
| highlight.js v9 | **highlight.js 최신판**(같은 라이브러리 메이저 업그레이드) | `highlightBlock`→`highlightElement` 등 API 변경 대응 필요, 교체가 아니라 버전업 |
| "마크다운 에디터"(자체 구현) | **보수적 옵션**: textarea 유지 + atjs만 Tribute.js로 교체 + marked/highlight.js만 버전업 / **적극적 옵션**: **EasyMDE** 또는 **CodeMirror 6** 기반 재구성(툴바·탭·체크리스트 버튼을 에디터가 자체 제공 — 현재 수제 UI를 대체하는 것이라 사실상 상호작용 재설계) | 적극적 옵션을 택할 경우 P3-45의 "재설계 금지" 원칙과 별도로 사용자 승인 필요 |

## 결정사항 (2026-09-10, 사용자 확정 — `next` 브랜치에서 TDD로 착수)

| 대상 | 확정 교체안 |
|---|---|
| Select2 | **Tom Select** |
| Pikaday | **Flatpickr**(단일 후보였음, 그대로 확정) |
| Jcrop | **Cropper.js**(단일 후보였음, 그대로 확정) |
| atjs | **Tribute.js**(단일 후보였음, 그대로 확정) |
| ViewerJS | **순정 Viewer.js로 업그레이드**(jQuery 래퍼 `jquery-viewer.js` 제거, PhotoSwipe 전면 교체 아님) |
| marked.js | **메이저 버전업(v12+)**, markdown-it 전환 아님 |
| highlight.js | 메이저 버전업(v11+) — 유일 후보, 확정 |
| "마크다운 에디터" | **적극적 옵션 확정 — EasyMDE 또는 CodeMirror 6로 전면 재구성**. 구체 라이브러리는 **EasyMDE**로 진행(에디터 자체가 툴바/탭/미리보기를 제공해 현재 수제 UI를 그대로 대체할 수 있어 CodeMirror 6로 처음부터 만드는 것보다 저비용 — 이 세부 선택은 "적극적 재구성"이라는 사용자 확정 방향 안에서의 구현 판단, 이견 있으면 언제든 CodeMirror 6로 전환 가능) |

### 8번 항목 세부 결정사항 (2026-09-10, 실제 화면 스크린샷 비교 후 사용자와 확정)

항목1~7 완료 후, Playwright로 **현재 yona 에디터 실제 화면**과 **EasyMDE 기본 모양** 스크린샷을
나란히 찍어 비교한 뒤 다음을 확정했다:

- **편집 엔진**: EasyMDE(CodeMirror 기반) — 확정, 재검토 불필요.
- **편집/미리보기 전환**: EasyMDE 기본값(단일 뷰 토글)을 그대로 쓴다 — yona의 기존 탭(편집/
  미리보기 별도 탭) UI는 유지하지 않는다(사용자 확정: "mde 기본값으로 하자").
- **겉모양**: EasyMDE 기본 스타일(회색 톤, FontAwesome류 아이콘)을 그대로 쓰지 않고, yona
  기존 버튼/아이콘 체계(`.ybtn`, `yobicon-*` 아이콘 폰트)로 재스킨한다.
- **"마크다운 도움말" 패널(`help/markdown.html` 프래그먼트)은 변경 없이 그대로 유지한다.**
  조사 결과 이 패널은 텍스트 삽입 툴바가 아니라 "마크다운 입력→출력" 예시를 보여주는 접이식
  치트시트(Header/Text Style/Link/List/Checklist/Image/Blockquote/Code/Table + yona 고유의
  "Short Link"—`#이슈번호`/`@멘션`/`@커밋해시` 문법 설명)였고, 텍스트에어리어/에디터 엔진과
  완전히 독립된 별도 조각이라 에디터 교체와 무관하게 그대로 둘 수 있다(EasyMDE 자체 도움말로는
  yona 전용 문법을 설명 못 해 대체 불가하기도 함).
- EasyMDE 자체 내장 `?`(도움말) 툴바 버튼은 끈다 — 범용 외부 가이드로 링크되어 위 yona 도움말
  패널과 중복/상충.
- 체크리스트 삽입 버튼("체크리스트 추가"): 직접 구현 대신 EasyMDE 내장 체크리스트 툴바 버튼으로
  대체한다.
- "임시저장 지우기" 버튼, "알림수신자 목록"(notification-receiver-list)은 yona 고유 기능이라
  EasyMDE에 없음 — 새 에디터 기준으로 별도 연동 필요(아래 0단계 조사 대상).
- 미리보기 렌더링: EasyMDE 내장 마크다운 렌더러 대신 `yobi.Markdown`(marked v18 + highlight.js
  v11 + `$yobi.xssClean`, 항목6/7에서 완성)을 그대로 재사용하도록 `previewRender` 옵션을
  연결한다 — 조회 화면 렌더링 결과와 100% 동일 보장.

### 8번 작업 단계 (분할정복 기준, 2026-09-10 합의)

분할 기준 3가지: (1) 각 단계가 독립적으로 TDD 가능하고 자기완결적일 것, (2) 의존 순서대로(셸
교체 → 그 위에 얹는 기능들), (3) 위험도/불확실성이 큰 것부터 — 특히 에디터 UI의 상당 부분
(체크리스트/임시저장/알림수신자 로직)이 읽을 수 있는 소스에 없고 `yona-lib.js` 번들에만
박혀있음을 이미 확인했다(항목3/6·7과 동일한 패턴 — `yona.ReceiverList.js` 소스 파일이 있지만
로드되지 않음).

1. [x] **0단계(조사, 2026-09-10 완료)**: 체크리스트/임시저장/알림수신자 실제 동작 확인.
   **당초 가정("전부 yona-lib.js 번들에만 박혀있다")은 틀렸다** — 실제로는:
   - **"체크리스트 추가" 버튼**: `.js` 파일에는 핸들러가 없었지만(0단계에서 `.js` 파일만
     grep해서 놓침), **1단계 에이전트가 발견 — `site/layout.html`에 직접 박힌 인라인
     `&lt;script&gt;`(`$(".add-task-list-button").on('click', ...)`)에 실제 동작하는 핸들러가
     있었다.** 커서 위치에 체크리스트 템플릿 텍스트를 삽입하는 기능으로 실제 동작 중이었음
     (정정 — "완전히 죽은 버튼"이 아니었음). 다만 EasyMDE 내장 체크리스트 버튼으로 대체하는
     결정 자체는 바뀌지 않음(동등 기능 제공) — 1단계 완료 후 이 인라인 스크립트는 대상 없는
     죽은 코드로 남음(에러 없이 조용히 no-op).
   - **"임시저장 지우기" 버튼**: 클릭 핸들러 없어 죽어있음. 다만 **임시저장(초안 자동저장) 시스템
     자체는 살아있고 정상 작동 중**(`temporarySaveHandler`/`removeCurrentPageTemprarySavedContent`
     전역 함수 — 소스 파일 `service/yona.temporarySaveHandler.js`는 어디서도 로드 안 되고
     `yona-lib.js` 번들의 사본만 실행 중이지만, 내용은 소스와 동일해 항목3/6·7 같은 "구버전 사본"
     문제는 아님. 댓글/이슈/마일스톤/게시글/PR 작성 화면 전부에서 실제 호출되는 걸 확인). **사용자
     결정: 지금 함께 고침** — 버튼을 `removeCurrentPageTemprarySavedContent()`에 연결.
   - **"알림수신자 목록"**: 관련 소스(`common/yona.ReceiverList.js`,
     `findNotiReceiversHandler($textarea, url)` — 입력 내용 기준 1초 디바운스로 서버에 물어
     알림 받을 사람 미리보기)가 **소스·번들 어디에서도 호출된 적 없어 완전히 죽어있음**, 게다가
     대응하는 백엔드 API 자체가 yona에 아직 이식되어 있지 않음(`docs/parity/index.md`에도
     미등록). legacy에 `POST .../issues/:number/commentNotiReceivers` →
     `IssueApi.commentNotiRecivers()`(이슈 댓글 전용, 임시 `IssueComment` 객체를 만들어
     `NotificationEvent.getMandatoryReceivers(comment, EventType.NEW_COMMENT)`로 계산)로 존재 —
     이 로직에 대응하는 `getMandatoryReceivers` 계열 함수가 yona
     `IssueServiceImpl.kt`에 이미 있어 백엔드 이식 자체의 난도는 낮음. **사용자 결정: 이번에
     같이 살림** — 이슈 댓글에 한정(legacy와 동일 범위)해 새 REST 엔드포인트 이식 + JS 연동.
2. [x] **1단계(셸 교체, 2026-09-10 완료)**: textarea+수제 탭 UI → EasyMDE 인스턴스(기본 토글),
   yona 스타일로 재스킨. `name`/`value`/폼 제출 동기화 보장. 상세는 완료 로그 "8-1단계" 참고.
3. [x] **2단계(미리보기 연동, 2026-09-10 완료)**: `previewRender`를 서버 렌더링(`POST
   /markdown/{owner}/{name}`)에 연결. 상세는 완료 로그 "8-2단계" 참고.
4. [ ] **3단계(멘션 연동)**: Tribute(항목3)를 EasyMDE의 실제 입력 DOM(CodeMirror)에 연결.
5. [ ] **4단계(나머지 기능 재구현)**: 체크리스트는 EasyMDE 내장으로 대체. 임시저장 지우기
   버튼을 `removeCurrentPageTemprarySavedContent()`에 연결(승인됨). ~~알림수신자 목록은 이슈
   댓글 한정으로 새 REST 엔드포인트(`IssueApi.commentNotiRecivers` 이식, `IssueServiceImpl`의
   기존 mandatory-receivers 로직 재사용) + `findNotiReceiversHandler` 연동으로 되살림(승인됨).~~
   **→ 이 부분은 [[tickets/p3-52|P3-52]](다른 계기 — Twirl/Thymeleaf 감사 카테고리 C — 로
   먼저 착수됨)에서 이미 구현·완료됨(이슈 댓글 한정, 동일 범위). 4단계 착수 시 남는 건
   체크리스트 대체 확인 + 임시저장 지우기 버튼 연결 + `?` 버튼 끄기뿐.** EasyMDE 자체 `?`
   버튼 끔.
6. [ ] **5단계(전면 회귀 + 브라우저 실사용 검증)**: `markdownEditor` fragment를 쓰는 모든 화면
   개별 확인, 저장/업로드처럼 마크업만으론 못 잡는 부분은 Playwright로 실제 확인(이번 세션의
   CSRF/board-edit 사례처럼 숨은 버그가 나올 가능성 높음).

## 착수 원칙(사용자 지시, 2026-09-10)

- **TDD 에이전트로 라이브러리별 순차 진행.** 각 라이브러리 전환은 별도 작업 단위로 다룬다.
- **기존 라이브러리와 동작이 완전히 일치해야 한다** — TEMPLATE_BACKLOG/P3-45와 동일한 정신의
  "동치성 우선" 원칙. 새 라이브러리가 제공하는 부가 기능이 있어도 켜지 않는다(예: Tom Select의
  태그 생성 기능이 Select2엔 없었다면 끈 채로 유지). 옵션/이벤트/키보드 동작까지 최대한 맞춘다.
- **여러 후보가 남아있는 결정은 착수 전 사용자 확인** — 위 표로 전부 해소됨. 작업 중 새로운
  선택지가 발견되면(예: 특정 옵션의 기본값 차이) 그때그때 확인받는다.
- **범위 밖 발견/기록용 이슈는 메인 세션(대화 에이전트)에게 전달, 직접 처리하지 않는다.** TDD
  에이전트가 라이브러리 전환 중 발견하는 관련 없는 버그/개선점은 즉시 고치지 말고 보고만 하며,
  대화 세션이 사용자에게 확인받아 별도 티켓으로 등록하거나 그 자리에서 처리 여부를 결정한다.

## 작업 순서

블로킹 의존이 거의 없어 순서는 자유롭지만, 위험도가 낮고 범위가 좁은 것부터 진행해 초반에 패턴을
확립한다:

1. [x] Pikaday → Flatpickr (2026-09-10 완료)
2. [x] Jcrop → Cropper.js (2026-09-10 완료)
3. [x] atjs → Tribute.js (2026-09-10 완료)
4. [x] ViewerJS → 순정 Viewer.js 업그레이드 (2026-09-10 완료)
5. [x] Select2 → Tom Select (2026-09-10 완료)
6. [x] marked.js 버전업(v12+) (2026-09-10 완료, v18)
7. [x] highlight.js 버전업(v11+) (2026-09-10 완료, v11.12.0)
8. [ ] 마크다운 에디터 → EasyMDE 전면 재구성 — 위 "8번 항목 세부 결정사항"/"8번 작업 단계"
      참고. 0~5단계로 분할, 착수 중

## 완료 로그

### 1. Pikaday → Flatpickr (2026-09-10)

TDD 에이전트로 진행. Flatpickr **4.6.13** 자체 호스팅(`static/javascripts/lib/flatpickr/`) 도입,
`yobi.ui.Calendar.js`(공개 API `getDate`/`setDate`, 마크업 계약 `data-toggle="calendar"`+
`.btn-calendar` 유지)와 `yobi.milestone.Write.js`(브리핑엔 없었으나 에이전트가 직접 발견 —
milestone create/edit는 래퍼 없이 `new Pikaday(...)`를 직접 써서 `#datepicker`에 항상 펼쳐진
인라인 캘린더로 구현돼 있었음, `{inline:true}`로 동일 아키텍처 재현) 둘 다 교체. Pikaday 파일
전체 삭제. 신규 `CalendarWidgetTemplateEquivalenceSpec`(7케이스, RED→GREEN)+기존 이슈/마일스톤
관련 스펙 전부 회귀 없음(메인 세션이 직접 재실행해 재검증).

**결정 필요 사항 해소**: Flatpickr(`allowInput:true`)는 날짜 필드에 파싱 불가능한 문자열을
타이핑하고 blur하면 필드 값을 지운다(Pikaday는 그대로 둠) — 극단적 엣지케이스이며 사용자가
현재 구현(표준적인 Flatpickr 사용) 유지를 확정.

**범위 밖 발견 2건, 사용자 확인 후 이번 커밋에 함께 포함**:
1. `yobi.issue.View.js`의 `elements.dueDate = $("#issueDueDate")`(대응 요소 자체가 없어 항상 빈
   셀렉션이고 이후 어디서도 읽지 않는 죽은 코드) 제거.
2. issue/view.html에서 마감일이 있는 상태로 페이지를 열 때마다 `yobi.ui.Calendar.js`의 초기화가
   필드 값을 화면에 동기화하려고 `setDate()`를 호출하면서 불필요하게 `change` 이벤트까지 쏴서
   `_onChangeDueDate`(마감일 자동저장 위젯)를 매번 트리거하던 기존 버그(Pikaday 시절부터 있던
   것 — 원본 `pikaday.js`의 `setDate()`가 `preventOnSelect` 인자와 무관하게 항상
   `fireEvent(field, 'change', ...)`를 호출함을 삭제 전 커밋에서 직접 확인) 수정. 초기화 시의
   "표시 동기화" 호출만 `triggerChange=false`로 바꿔 change 이벤트를 억제하고, 공개 `setDate()`
   API는 기존처럼 change를 발생시키도록 유지(다른 코드가 명시적으로 부를 경우의 계약은 보존).

**수동 브라우저 확인 필요(미착수)**: 6개 화면(issue create/edit/list/view, milestone create/edit)의
실제 캘린더 팝업/인라인 캘린더 동작, issue/view 마감일 변경 자동저장이 여전히 정상 동작하는지.

### 2. Jcrop → Cropper.js (2026-09-10)

TDD 에이전트로 진행. Cropper.js **1.6.3**(v1 계열 최신 안정판, `getCroppedCanvas()` 명령형 API
사용 위해 웹컴포넌트 기반으로 전면 재작성된 v2 대신 의도적으로 선택 — 재설계 금지 원칙과 부합)
자체 호스팅(`static/javascripts/lib/cropperjs/`) 도입. `yobi.user.Setting.js`의 Jcrop 관련 함수
전체를 Cropper.js API로 재작성(`aspectRatio:1`/`minCropBoxWidth,Height:32`/`ready` 콜백에서
`setCropBoxData`로 기본 선택영역[0,0,128,128]/`crop` 이벤트로 실시간 미리보기/`getCroppedCanvas
({width:128,height:128})`로 최종 렌더링). Jcrop 파일 전체 삭제. 신규
`AvatarCropWidgetTemplateEquivalenceSpec`(2케이스, RED→GREEN)+`UserViewControllerSpec`(145)+
`CalendarWidgetTemplateEquivalenceSpec`(7) 전부 회귀 없음(메인 세션이 직접 재실행해 재검증).

**결정 필요 사항 해소**: Jcrop은 크롭박스가 완전히 사라질 수 있어 그 경우 기본 영역을 복원하는
안전장치(`onRelease`)가 있었는데, Cropper.js는 최소크기(32px) 미만으로 줄어들지 않아 크롭박스가
사라지는 상태 자체가 구조적으로 불가능하다 — 별도 대응 코드 없이도 "항상 유효한 크롭 영역 존재"
보장이 성립한다는 에이전트 판단을 사용자가 그대로 승인.

**범위 밖 발견 2건**:
1. **사용자 확인 후 함께 수정**: 크롭 결과 업로드 시 `elImage.mimeType`이 표준 `Image` 객체에
   없는 속성이라 항상 `undefined`였고, `canvas.toBlob()`이 타입 인자 없이 호출되어 원본 파일
   형식과 무관하게 항상 PNG로 인코딩되던 기존 버그(Jcrop 시절부터 있던 것). `_showCropper(oRes)`
   에서 `oRes.mimeType`(업로드 응답이 실제로 제공하는 값)을 `htVar.sAvatarMimeType`에 저장해두고
   `toBlob()` 호출 시 그대로 넘기도록 수정 — 원본 형식(JPEG 등)이 보존된다.
2. **이번엔 그대로 둠(기록만)**: Cropper.js 기본 오버레이 색상(반투명 검정)이 Jcrop의
   `bgColor:"#fff"`(반투명 흰색)와 시각적으로 다름. 필요 시 `.cropper-modal` CSS 오버라이드로
   맞출 수 있음.

**수동 브라우저 확인 필요(미착수)**: 아바타 업로드→크롭 모달 동작, 크롭박스 드래그 시 미리보기
동기화, 128x128 결과가 기존과 동일 영역을 담는지(특히 원본이 128px보다 작거나 매우 클 때),
모달 취소 후 재업로드 시 Cropper 재초기화, 크롭 결과가 원본 형식(JPEG 등)으로 정상 업로드되는지.

### 3. atjs → Tribute.js (2026-09-10)

TDD 에이전트로 진행(4개 트리거 `@`/`:`/`#`/`[`, 파일 2개(`yobi.Mention.js`,
`yona.TitleHeadAutoCompletion.js`), 호출부 11곳 — 항목 중 가장 큰 범위). Tribute.js **5.1.3**
자체 호스팅(`static/javascripts/lib/tribute/`) 도입, `collection` 배열로 4개 트리거 매핑,
atwho의 커스텀 `sorter`(이슈 참조/라벨 정렬 알고리즘)와 `highlighter`(검색어 `<strong>` 강조)를
`values(query, cb)` 콜백 안에 그대로 이식. atjs 디렉터리 전체 삭제. 신규
`MentionAutocompleteWidgetTemplateEquivalenceSpec`(9케이스, GREEN) + 기존 관련 스펙 13개
622케이스 전부 GREEN.

**메인 세션이 리뷰 중 에이전트가 놓친 심각한 회귀를 직접 발견해 수정**: `common/yobi.Mention.js`
(에이전트가 Tribute로 재작성한 파일)가 **어느 템플릿에서도 `<script src>`로 로드되지 않고
있었다** — 실제 실행되는 `yobi.Mention`은 전역 레이아웃이 로드하는 구버전 번들
(`static/javascripts/yona-lib.js`)에 박혀있던 atjs 기반 사본뿐이었고, 에이전트가 편집한 소스는
브라우저에 전혀 반영되지 않는 죽은 파일이었다. atjs 삭제로 그 번들 사본이 이제 항상 크래시하게
되어, **지금까지 실제로 동작하던 `:`(이모지) 트리거까지 깨지는 회귀**였다. `yobi.Mention()`을
호출하는 6개 화면(board/milestone/issue의 create·edit, pullrequest 2개는 원래도 로드 안 하던
화면이라 제외)에 `tribute.min.js` 바로 뒤로 `<script src=".../yobi.Mention.js">`를 추가해
해결(스크립트 실행 순서상 번들의 구식 정의를 새 정의로 덮어씀). 테스트에 `assertMentionScriptLoaded`
회귀 방지 단언 추가.

**범위 밖 발견, 사용자 확인 후 함께 처리**:
1. `mentionList`/`titleHeads` 원격검색 URL이 실제 컨트롤러 매핑과 어긋나 `@`/`#`/`[` 트리거가
   전부 404였던 기존 버그 — `/api` 접두사 누락(`mentionList`, 6개 파일)과 경로 구조 자체가
   다른 문제(`titleHeads`, 3개 파일) 둘 다 수정, `MentionController.kt`/`ProjectController.kt`의
   실제 `@GetMapping` 경로와 대조해 확정.
2. `pullrequest/create.html`/`edit.html`은 애초에 atjs/Tribute 어느 쪽도 로드한 적이 없는 기존
   버그 — 이번에도 건드리지 않고 보존(에이전트와 메인 세션 둘 다 독립적으로 확인).
3. `issue/view.html`의 atjs include는 `yobi.Mention()` 호출 자체가 없는 죽은 코드였음 — 제거.

**애초에 "심각한 발견"으로 지목했다가 실제 브라우저 재현으로 반증되어 정정한 것**:
`jQuery.browser` 미로드로 파이어폭스 IME 폴리필이 크래시해 같은 스크립트 블록의 뒤이은 코드
(PR/게시글 저장 핸들러 등)가 실행되지 않을 거라 추정했으나, Playwright로 실제 확인한 결과
`jQuery.browser`는 정의되어 있어(`{chrome:true,...}`) 크래시 자체가 나지 않았다. 대신 그
과정에서 board/edit.html의 완전히 다른 실제 버그(문자열 미인용 `ReferenceError`)를 찾아
[[tickets/p3-47|P3-47]]로, 그걸 고친 뒤 드러난 CSRF 403 문제를 [[tickets/p3-48|P3-48]]로
분리 등록했다(둘 다 P3-46과 무관한 기존 버그).

### 4. ViewerJS → 순정 Viewer.js 업그레이드 (2026-09-10)

TDD 에이전트로 진행(범위 좁음 — `site/layout.html` 전역 레이아웃 한 곳뿐, 다른 파일은 안 씀).
Viewer.js를 **1.6.1 → 1.13.0**으로 업그레이드하고 jQuery 래퍼(`jquery-viewer.js`) 완전 제거,
초기화 코드를 `$(".markdown-wrap").each(...)` 순회는 유지한 채 `$that.viewer()` → `new
Viewer(this, {transition:false})`로 전환(여러 `.markdown-wrap` 컨테이너 각각에 독립 인스턴스
필요 — 이슈/PR 뷰의 본문+댓글 여러 개 등). 신규 `ViewerLightboxWidgetTemplateEquivalenceSpec`
(RED→GREEN)+`TemplateEquivalenceSpec`(jquery-viewer.js 로드 단언을 `shouldBe 0`으로 갱신)+
`IssueViewControllerSpec`/`PullRequestViewControllerSpec`/`PullRequestViewControllerMoreSpec`/
`CalendarWidgetTemplateEquivalenceSpec`/`MentionAutocompleteWidgetTemplateEquivalenceSpec`
(레이아웃 전역 스크립트 블록을 건드린 만큼 추가 확인) 전부 개별 `--tests` 실행으로 GREEN —
메인 세션이 신규 스펙 재실행으로 재검증.

**결정 필요 사항 해소**: mouseover 시 커서를 포인터로 바꾸는 핸들러가 jquery-viewer.js
래퍼 기능인지 확인 — v1.13.0 소스/CSS 전체를 뒤져봐도 그런 처리가 없음을 확인, 애초에
순정 jQuery로 직접 작성된 이 프로젝트 자체 코드였다는 결론. 그대로 보존.

**범위 밖 발견**: 없음.

**수동 브라우저 확인 필요(미착수)**: 라이트박스 열기/닫기/갤러리 넘기기, 여러
`.markdown-wrap` 컨테이너(본문+댓글)가 각각 독립적으로 동작하는지, mouseover 커서 변경.

### 5. Select2 → Tom Select (2026-09-10)

TDD 에이전트로 진행(지금까지 중 가장 넓은 범위 — 템플릿 15개, 커스텀 JS 5개). Tom Select
**2.6.2**(`tom-select.complete.min.js`, remove_button 플러그인 포함) 자체 호스팅 도입.
`yobi.ui.Select2.js`를 전면 재작성 — `data-format`별(projects/issues/user/milestone/
issuelabel/branch) 포맷터를 `render.option`/`render.item`으로, 커스텀 matcher를 `score`
콜백으로 이식. `issuelabel`(이슈 라벨 배타 카테고리 다중선택)이 가장 복잡한 부분이었는데,
select2 v3의 "선점형"(`select2-selecting`+`preventDefault`) 방식 대신 Tom Select의
`onItemAdd`에서 "추가 후 정리"(같은 `categoryId`의 기존 선택을 사후 제거) 방식으로 재현 —
`optgroup`의 `data-category-id`/`data-category-is-exclusive`가 HTML5 dataset이라 항상
문자열로만 와서 `=== "true"`로 비교해야 한다는 점까지 정확히 짚어 구현. 원격 자동완성 2건
(담당자/공유자)은 `load`/`loadThrottle`(300ms)+`valueField`로 이식. `yobi.issue.Write.js`/
`yona.TitleHeadAutoCompletion.js`/`yobi.issue.View.js`의 `.select2("val",...)` 프로그래매틱
호출부(`yobi.issue.View.js`는 브리핑에 없었으나 에이전트가 직접 발견)도 `.tomselect.setValue`/
`.getValue()`로 전환. Select2 라이브러리 파일 전체 삭제. 신규
`SelectWidgetTemplateEquivalenceSpec`(9케이스, RED→GREEN)+기존 스펙 14개(Issue/Milestone/
Project/PullRequest/Board/Organization/Code 등 핵심 컨트롤러·렌더링 스펙 + Calendar/Mention/
AvatarCrop/Viewer 위젯 스펙) 전부 개별 `--tests` 실행으로 GREEN — 메인 세션이 신규 스펙
재실행 + 핵심 로직(`onItemAdd` 배타 처리) 코드 리뷰로 재검증.

**전역 번들 중복 확인(항목3 교훈 적용)**: `yona-lib.js`/`yona-common.js`/`yona-layout.js`
3개 전부 select2/tomselect 관련 코드 0건 — 착수 전·완료 후 에이전트가 확인했고 메인 세션도
독립적으로 재확인함. 이번엔 항목3같은 번들 함정 없음.

**결정 필요 사항(에이전트가 합리적 근거로 판단, 이견 없으면 그대로 확정)**:
1. `select2-without-searchbox`(project/create의 VCS 선택 등) → Tom Select `controlInput:null`
   (입력 자체 제거)로 대응. 원본도 `display:none`이라 포커스/타이핑이 애초에 불가능했으므로
   기능적으로 동일.
2. 로케일(ko/ja) 처리는 `window.YOBI_SELECT2_I18N`(noResults/searching/tooShort)로 이식.
   `formatInputTooLong`/`formatSelectionTooBig`은 이 프로젝트 어디서도 트리거 안 되는 설정이라
   이식 안 함(허용 가능한 gap).

**범위 밖 발견(전부 기존 버그/죽은 코드, 고치지 않고 보고만 함)**:
1. `yona.issue.Sharer.js`(`#issueSharer` 담당자 공유 위젯) 전체가 어느 템플릿에서도 로드된 적
   없는 죽은 파일 — 원본 formatter도 malformed(안 닫힌 `</div>`)였음.
2. `yobi.issue.Write.js:84`, `yobi.issue.View.js`의 `data("forceChange")` 분기 — 어디서도
   set되지 않아 도달 불가능.
3. `yobi.issue.View.js`의 `labelIds`(`evt.added`/`evt.removed`) 분기 — 현재 `.issue-info`
   안에 해당 select2 필드 자체가 없어 도달 불가능.
4. `organization/issueSearch_partial.html`의 `#projects` select에 `data-format` 속성이 빠져
   있어 아바타/owner 커스텀 렌더링이 애초에 적용 안 됨(기존 불일치).
5. `user/edit_tokens_new.html`의 `#scopedProjectIds`는 `data-toggle="select2"`는 있지만 이
   템플릿이 `common/select2` fragment를 include 안 해서 select2/Tom Select 어느 쪽도 초기화된
   적 없음(순정 native select로 동작 — fragment include 누락, 이번 교체와 무관).
6. `code/view.html`/`code/history.html`의 `#branches`(순정 select)를 다루는
   `yobi.code.Browser.js`/`yobi.code.Diff.js`가 select2 전용 프로퍼티(`weEvt.val`)를 읽어
   기존부터 `undefined`로 깨져있던 것으로 보임.
7. `code/view.html`/`code/history.html`/`code/diff.html`이 select2 위젯이 전혀 없는데도
   `common/select2` fragment를 불필요하게 로드 중(리소스 낭비, 기능 문제는 아님).
8. `.select2-container.select2-drop-above`(드롭다운이 위로 펼쳐질 때 테두리 반전) 스타일은
   Tom Select에 대응 클래스가 없어 이식 안 함 — 코너 라운딩 정도의 사소한 시각 차이 가능.

**수동 브라우저 확인 필요(미착수)**: `data-format`별 드롭다운 표시/검색(특히 milestone/issues/
issuelabel), 담당자 원격 자동완성, **이슈 라벨 배타 카테고리 다중선택**(가장 중요), 라벨
카테고리 단일/다중 선택 클릭 전용 동작, `project/importing`/`project/issuelabels`/
`organization/boardList`/`organization/issueList`/`pullrequest/list`/`pullrequest/view`/
`code/view`/`code/history`/`code/diff` 등 자동화 테스트 범위 밖 화면.

### 6+7. marked.js + highlight.js 버전업 (2026-09-10)

**TDD 에이전트가 계획만 세우고 구현 전에 턴이 끝나(항목3과 비슷한 패턴 — "테스트 대기 중"),
메인 세션이 직접 이어받아 완료했다.** 에이전트가 남긴 신규 스펙
(`MarkdownRendererWidgetTemplateEquivalenceSpec`, 매우 꼼꼼하게 설계돼 있었음)을 그대로
살려서 구현했다.

**착수 전 번들 중복 확인(항목3/5 교훈 적용, 이번엔 실제로 걸림)**: `marked(...)`/`hljs.*`를
실제로 호출하는 `common/yobi.Markdown.js`가 **이번에도 어느 템플릿에서도 `<script src>`로
로드되지 않고**, `yona-lib.js` 번들에 박힌 구버전 사본만 실행되고 있었음을 착수 전 확인.
`site/layout.html`의 `markdown(project)` fragment에 `yobi.Markdown.js`를 실제로 로드하는
`<script src>` 태그를 추가해 해결(항목3과 동일 패턴 — 스크립트 실행 순서상 새 정의가 번들의
구식 정의를 덮어씀).

**추가로 발견한 변형 함정**: `pullrequest/view.html`만 유일하게 `markdown(project)`
fragment를 `scripts` fragment(yona-lib.js 포함)**보다 먼저** include하고 있어서, 위 수정만으로는
부족했다 — 나중에 로드되는 `yona-lib.js`가 방금 로드한 새 정의를 다시 구버전으로 덮어써버림.
페이지 구조를 재배치하는 대신 `markdown` fragment의 include 위치를 `scripts` fragment 뒤로
옮겨서(다른 모든 페이지와 동일한 순서로 통일) 해결.

**버전**: marked **v18.0.12**(npm `marked` 패키지의 `lib/marked.umd.js` — 최신 버전은
사전 빌드된 `marked.min.js`를 배포하지 않아 UMD 번들을 직접 사용), highlight.js
**v11.12.0**(`@highlightjs/cdn-assets` 공식 CDN 번들, 전체 언어 포함 — 기존과 동일하게
`hljs.configure({languages:[]})`로 자동감지 비활성 유지).

**API 이식 상세**:
- `marked(text, options)` 함수형 호출 → `marked.parse(text, options)`.
- 구버전 `highlight` 콜백 옵션(marked v5+에서 제거됨) → `marked.use({renderer: {code(token){...}}})`
  커스텀 렌더러로 이식. `hljs.getLanguage(lang)`으로 언어 지원 여부를 먼저 확인(구버전은
  예외로만 판별) 후 `hljs.highlight(code, {language: lang}).value`(신버전 시그니처 — 코드가
  첫 인자) 호출. 원본의 `langPrefix:''`(클래스 접두어 없음, "hljs" 클래스도 원래 안 붙어있었음)
  동작을 그대로 재현 — 언어가 있으면 `<code class="lang">`, 없으면 `<code>`, HTML 이스케이프는
  직접 구현(`marked`가 커스텀 렌더러 사용 시 이스케이프를 대신 안 해줌).
- `hljs.highlightBlock(block)` → `hljs.highlightElement(block)`(서버사이드 렌더링 경로,
  `_render()`의 AJAX 성공 콜백).
- `hljs.initHighlightingOnLoad()`(v11에서 제거) → `hljs.highlightAll()`.
- highlight.js 테마 CSS(`styles/*.css`, 70여 개) 파일은 업데이트하지 않음 — v9→v11 사이
  `.hljs-*` 클래스 네이밍 체계가 안정적으로 유지돼 색상 테마와는 무관하다고 판단(메인 세션 판단,
  변경 없이 그대로 둠).

메인 세션이 npm으로 marked v18/highlight.js v11을 직접 설치해 `renderer.code(token)` 콜백
시그니처(`{type,raw,lang,text}`)와 `hljs.highlight()` 신버전 반환값을 실측 검증한 뒤 이식.
신규 `MarkdownRendererWidgetTemplateEquivalenceSpec`(14케이스, `pullrequest/view`의 fragment
순서 검증 포함) 전부 GREEN(도중 테스트 픽스처 자체 버그 하나 발견·수정 — `pull/new` 테스트가
git 저장소 없이 프로젝트를 만들어서 `PullRequestViewController.createPullRequestForm()`이
`error/badrequest`로 리다이렉트해버리는 바람에 실패했었음, 브랜치가 있는 bare git 저장소를
만들어주는 기존 패턴을 추가해 해결) + `IssueViewControllerSpec`/`PullRequestViewControllerSpec`/
`BoardViewControllerSpec` 회귀 없음, 전부 개별 `--tests` 실행.

**범위 밖 발견**: 서버사이드 마크다운 렌더링 경로(`_render()`, `htVar.sMarkdownRendererUrl`로
POST 후 HTML을 받아오는 방식)는 Kotlin 서버 쪽 렌더러 문제라 이번 작업(클라이언트 JS 라이브러리
버전업) 범위 밖으로 확인만 하고 손대지 않음.

**수동 브라우저 확인 필요(미착수)**: 마크다운 렌더링 결과 HTML이 기존과 동일한지, 코드블록
문법 강조가 정상 동작하는지, XSS 정화가 여전히 잘 되는지.

### 8-1단계. EasyMDE 셸 교체 (2026-09-10)

TDD 에이전트로 진행. EasyMDE **2.21.0** 자체 호스팅 도입, `markdownEditor` fragment를 단순화
(구 탭 UI/체크리스트·임시저장·알림수신자 관련 마크업 제거, `editor-notice-label`/`help/markdown`
include는 유지). 신규 `yobi.ui.MarkdownEditor.js`로 초기화, 툴바를 커스텀 버튼 객체로 구성해
`.ybtn`/`yobicon-*`로 재스킨(Bold·Italic·Heading·Quote·Checklist·List·OrderedList·Link·Image·
Preview — `?`/side-by-side/fullscreen 및 단축키는 제외). `spellChecker`/`autoDownloadFontAwesome`
명시적으로 끔(소스 확인 결과 spellChecker 켜면 외부 CDN에서 사전을 받아오려 해 자체호스팅
원칙 위반 — 이 판단은 사용자 확인 없이 에이전트가 스스로 내림, 합리적이라 추인).

**가장 중요한 기술적 발견**: `CodeMirror.fromTextArea`(EasyMDE 내부 구현)는 `forceSync` 옵션
없이는 폼 제출 시점에 원본 `&lt;textarea&gt;`에 값을 자동으로 되쓰지 않는다는 걸 소스 레벨로
확인 — `codemirror.on("change", ...)`에서 `easyMDE.element.value = easyMDE.value()`로 직접
동기화하고 `input`/`keyup` 네이티브 이벤트를 재발행하도록 구현(이 `keyup` 재발행 덕분에 살아있는
임시저장 시스템도 계속 동작). 신규 `MarkdownEditorShellWidgetTemplateEquivalenceSpec`(RED→GREEN)
+ `IssueFormSpec`/`IssueViewControllerSpec`/`BoardViewControllerSpec`/
`MilestoneTemplateRenderingSpec`/`CalendarWidgetTemplateEquivalenceSpec`/
`ViewerLightboxWidgetTemplateEquivalenceSpec` 전부 GREEN — 메인 세션이 `--rerun-tasks`로
캐시 무시 재실행해 재검증(gradle UP-TO-DATE 오탐 방지, 이 세션의 교훈 적용).

**범위 밖 발견, 메인 세션이 직접 조사해 해소**:
1. **에이전트가 짚은 위험(드래그앤드롭 파일첨부 깨짐 우려)은 EasyMDE 때문은 아님** — 근거로
   든 두 파일(`yona.CommentAttachmentsUpdate.js`, `yona.SubComment.js`) 모두 **애초에 어느
   템플릿·번들에서도 로드된 적 없는 완전한 죽은 코드**임을 메인 세션이 직접 확인(둘 다 로드
   경로 grep 0건). **정정(2026-09-10, 8-2단계 이후)**: 다만 사용자가 "실제로 드롭까지
   해봤냐"고 재차 확인해 Playwright로 텍스트/이미지 파일을 실제로 드롭해보니 **업로드가 전혀
   안 됐다** — 다만 원인은 EasyMDE가 아니라 완전히 별개의, 훨씬 이전부터 있던 두 개의 기존
   버그(`issue/view.html`의 `<script>` 안 `<th:block>` 중첩으로 `yobi.issue.View` 모듈 자체가
   안 뜸 + legacy의 즉시-업로드 담당 파일 `yona.CommentAttachmentsUpdate.js`가 포팅은 됐지만
   `<script src>`로 로드된 적이 없음)이었다. "EasyMDE 탓이 아니다"는 결론은 맞았지만
   "문제 없다"는 결론은 틀렸다 — 상세 원인 분석은 [[tickets/p3-49|P3-49]] 참고(범위 밖이라
   기록만 해두고 착수 보류).
2. `PullRequestListTemplateEquivalenceSpec`의 `select2.js` 스크립트 존재 단언(214번째 줄)이
   실패하는데, `git stash`로 직접 대조해 이번 변경 전부터 있던 stale 단언(항목5에서 이미 제거된
   리소스를 여전히 기대함)임을 확인 — 이번 변경과 무관, 손대지 않음(추후 항목5 후속 정리 대상으로
   기록만).
3. `.nav-tabs.small` CSS 규칙(`yobi.css`)이 이번 변경으로 고아가 됨 — 동작 영향 없어 그대로 둠.
4. `.markdown-preview` div의 `editorMode` 클래스 분기 — 실사용처를 못 찾아 이번에 제거했으나
   100% 확신은 못 함, **2단계(미리보기 연동) 에이전트가 재확인 필요**.

**수동 브라우저 확인 필요(미착수)**: 17개 화면에서 EasyMDE 실제 렌더링/타이핑, **폼 제출 시
입력값이 서버로 정확히 전달되는지**(MockMvc로 검증 불가한 가장 중요한 부분), 재스킨한 툴바
버튼(특히 텍스트 라벨 "H"/"1.") 외관, 임시저장 표시, 여러 댓글 폼이 한 화면에 동시에 있을 때
각 에디터의 독립 동작.

### 8-2단계. 미리보기 서버 연동 (2026-09-10)

TDD 에이전트로 진행. EasyMDE 내장 클라이언트 렌더러를 켜는 대신, 리팩터링 이전 탭 UI의 실제
동작(`yobi.Markdown.js`의 `_render()` — `POST /markdown/{owner}/{name}`으로 서버 렌더링,
성공 시 `hljs.highlightElement()`로 코드블록 하이라이팅)을 그대로 재현하는 방향으로
`previewRender` 옵션을 연결했다.

**핵심 조사 결과 및 설계 판단**:
1. **레거시 미리보기는 클라이언트 marked.js가 아니라 항상 서버 AJAX였다** — `yobi.Markdown.js`의
   `_setEditor()`가 구 탭 UI의 `a[data-mode="preview"]` 클릭을 감시하다 서버 엔드포인트
   (commonmark 기반, 이슈링크 자동변환/referrer체크 포함)를 호출하는 방식. 이 사실을 근거로
   `yobi.ui.MarkdownEditor.js`에 `_previewRenderer()`를 새로 만들어 동일한 계약(`{body, breaks:
   true}` POST, 응답 HTML 삽입 후 `pre code` 하이라이팅)으로 구현 — EasyMDE 내장 렌더러(부가
   기능)는 켜지 않는다는 동치성 원칙 유지.
2. **범위 확장 여부를 사용자에게 확인 후 승인받음**: `yobi.Markdown.js`(따라서 레거시 미리보기)는
   `site/layout.html::markdown(project)` fragment를 포함하는 8개 화면(이슈/PR/마일스톤
   생성·수정)에서만 로드됐다 — 나머지 화면(이슈/게시판 댓글, 코드리뷰 댓글, 위키 편집 등)은
   `yobi.Markdown.js` 자체가 로드되지 않아 미리보기 탭이 원래부터 빈 화면만 보여주는 죽은
   기능이었다. **사용자 결정: 모든 화면에서 살림** — project 컨텍스트가 있는 markdownEditor
   호출부 16곳 전부에서 서버 렌더링 미리보기가 실제로 동작하도록 확장(임시저장 지우기/알림수신자
   목록을 이번에 함께 살리기로 한 것과 동일한 결정 패턴).
3. **`data-markdown-render-url` 노출 + Thymeleaf 컨텍스트 상속 확인**: `markdownEditor(name,
   value, editorMode)` fragment는 `project`를 명시적 파라미터로 선언하지 않지만, Thymeleaf
   프래그먼트는 파라미터 시그니처가 있어도 호출 측 전체 모델 컨텍스트를 그대로 상속한다는 것을
   **직접 렌더링 테스트로 실증**(임시 probe span을 삽입해 2단계 깊이로 중첩 호출되는 경우
   — `board/view`→`commentUpdateForm`→`markdownEditor`, `issue/view`→`commentUpdateForm`→
   `markdownEditor` — 에서도 project가 끊기지 않고 전달됨을 확인, 이후 probe 제거). 16개
   호출부 전부(`issue/view`, `board/view`(신규+기존수정폼), `wiki/edit(_new)`,
   `milestone/create`, `pullrequest/view`(changes 탭, common/reviewForm) 등)에서 project가
   실제로 모델에 있어 `data-markdown-render-url`이 항상 채워짐을 확인 — 속성이 비는 화면은
   현재 실사용처가 없어 이 분기는 방어적 가드로만 존재한다. (부수 발견: `pullrequest/view`의
   `common/reviewForm`은 `/pull/{number}/changes` 전용 라우트에서만 렌더링되며, `/pull/{number}`
   overview 라우트에선 안 됨 — 처음엔 이를 놓쳐 RED가 남았다가 라우트를 바로잡아 GREEN.)
4. **호출 빈도 조사 후 디바운스 결정**: `easymde.min.js` 소스를 직접 분석해, 우리가 구성한
   툴바(단일 Preview 토글, side-by-side 없음)에서는 `previewRender`가 Preview 버튼 클릭(토글)
   때만 호출되고 타이핑마다 반복 호출되지 않음을 확인(side-by-side 모드에서만 CodeMirror
   `"update"` 이벤트로 연속 호출되는 별도 경로가 있음) — 레거시(탭 클릭 시 1회성 렌더링)와
   이미 호출 빈도가 동일했다. 다만 토글을 닫을 때도 EasyMDE가 한 번 더 호출하는 점, 연타 시
   요청이 겹칠 수 있는 점을 고려해 300ms 디바운스 + 요청 순번 비교(오래된 응답이 최신 내용을
   덮어쓰지 않도록 하는 레이스 컨디션 가드)를 추가 — 새 기능이 아니라 같은 동작의 안전한
   구현.
5. **`editorMode` 클래스 분기 최종 결론**: 8-1단계에서 남겨뒀던 미확인 사항 — `yobi.css`/전체
   JS를 다시 grep해 재확인 완료, 이 분기를 읽는 CSS/JS 규칙은 정말 없다(죽은 코드, 재현 불필요).
   대신 사이트 전역 마크다운 콘텐츠 스타일(`.markdown-wrap`)을 `previewRender`가 미리보기 div에
   직접 부여해 시각적으로 조회 화면과 일치시킨다.
6. **엔지니어링 판단 — highlight.js 전역 승격**: `previewRender`가 16개 화면 전부에서 동작해야
   하는데 `hljs`는 기존에 `markdown(project)` fragment(8개 화면 한정)에서만 로드되고 있었다.
   8-1단계의 EasyMDE 전역 승격과 동일한 패턴으로 highlight.js CSS/JS를 head/scripts로 전역
   이동하고, `markdown(project)` fragment의 중복 `<script src="...highlight.pack.js">`는
   제거(`hljs.configure`/`highlightAll` 호출은 그대로 유지). `MarkdownRendererWidgetTemplateEquivalenceSpec`
   의 스크립트 개수 단언(`shouldBe 1`)이 그대로 통과함을 메인 세션이 재검증해 중복 로드가
   없음을 확인.

**테스트**: 신규 `MarkdownEditorPreviewWidgetTemplateEquivalenceSpec`(5케이스, RED→GREEN) +
`MarkdownEditorShellWidgetTemplateEquivalenceSpec`(2)/`MarkdownRendererWidgetTemplateEquivalenceSpec`
(14) 개별 재실행 — 메인 세션이 `--rerun-tasks`로 캐시 무시 재검증(3개 스펙 전부 실제 재실행
확인, `build/test-results/test/*.xml`의 `failures="0" errors="0"` 및 신선한 타임스탬프로 직접
대조), 회귀 없음.

**범위 밖 발견**: 없음(이 단계 자체는). 다만 이 단계 검증 과정에서 사용자가 8-1단계의 "드래그
앤드롭 문제 없음" 결론을 재확인 요청해 [[tickets/p3-49|P3-49]](이슈 댓글 파일 업로드가 전혀
다른 두 이유로 죽어있음)를 새로 발견·기록함 — 위 8-1단계 로그 정정 참고.

**수동 브라우저 확인 필요(미착수)**: 실제 AJAX 호출/CSRF 헤더 자동 전달, `hljs.highlightElement()`
코드블록 하이라이팅 실동작, 디바운스·레이스 컨디션 가드가 연타 시 실제로 의도대로 동작하는지,
`.markdown-wrap` 적용 후 시각적 스타일이 조회 화면과 실제로 일치하는지, 16개 화면 각각에서
Preview 버튼 클릭 시 렌더링 결과가 해당 화면의 조회 모드와 100% 동일한지.

## 8번 항목 방향 전환 — EasyMDE/CodeMirror5+Tribute.js → CodeMirror6 기반 자체 제작 Web Component (2026-09-11)

3단계(멘션 연동) 착수 중 CodeMirror5의 구조적 결함(숨겨진 `<textarea>`가 백스페이스/Delete
직후 순간적으로 비어 Tribute.js가 컨텍스트를 잃는 문제, zurb/tribute#511)에 부딪혔다. 완화책을
시도했으나 "줄 중간 타이핑 시 문서 내용이 반복 중복·붕괴되는" 훨씬 심각한 회귀를 유발해
비활성화(재현/원인/완화책 코드는 워킹 트리에 미커밋 상태로 보존 — 정리는 후속 세션에서 일괄).

이후 대안 조사(CKEditor5/Toast UI Editor/Milkdown/CodeMirror6 자체구축/전면 빌드파이프라인
도입/Web Component 분리 — 각각의 조사 근거는 세션 로그 참고, 요약: CKEditor5는 HTML 왕복변환
손실+빌드도구 재스킨 문제로 반대, Toast UI Editor는 저장소 아카이브(죽은 프로젝트)로 반대,
Milkdown은 빌드도구 없는 자체호스팅 불가+WYSIWYG 왕복변환 문제로 반대, CM6 자체구축은
빌드도구 필요가 걸림돌이었으나 Web Component로 격리하면 프로젝트 전체 "빌드 도구 없이
자체호스팅" 원칙을 안 깨고 도입 가능하다는 결론)를 거쳐, **CM6 기반 Custom Element
(`<yona-markdown-editor>`)로 EasyMDE+Tribute.js를 전부 교체**하기로 확정했다.

**저장소 분리**: 컴포넌트 소스는 yona 본체와 완전히 분리된 새 GitHub 저장소
`yona-projects/components`(모노레포, `editor/` 하위 디렉터리)에 TypeScript+esbuild로 둔다.
yona 저장소엔 이 컴포넌트의 소스/node_modules/npm 설정이 전혀 들어가지 않고, 로컬에서 빌드한
산출물(`yona-markdown-editor.min.js`) 하나만 `static/javascripts/lib/yona-markdown-editor/`에
vendoring한다.

승인된 전체 계획(설계 결정 + 0~7단계 로드맵)은 [[p3-46-cm6-plan|별도 계획 문서]]
(`docs/parity/tickets/p3-46-cm6-plan.md`, git 추적됨 — 로컬 전용이었던 이전 버전에서 이관,
어느 세션/컴퓨터에서든 이어받을 수 있음)를 참고. 이하 완료 로그는 이 새 트랙(CM6 0~7단계)
기준이며, 위 8-1/8-2단계(EasyMDE 시절) 로그는 폐기된 접근의 기록으로 그대로 남겨둔다.

**컴포넌트 저장소 접근**: `yona-projects/components`(별도 GitHub 저장소) — 새 세션/컴퓨터에서
이어받으려면 `git clone git@github.com:yona-projects/components.git`으로 먼저 클론해야 한다
(로컬 경로 `~/yona-convert/components`는 이번 세션 기준일 뿐, 다른 환경에선 임의 위치에 클론
가능 — 위 계획 문서의 "주요 참고 파일" 절 참고).

### CM6-0단계. Shadow DOM 스파이크 (go/no-go 게이트, 2026-09-11 완료)

CM6 `EditorView`를 실제 Shadow DOM에 마운트했을 때 Chromium/Firefox/WebKit 3개 엔진 전부에서
스타일/캐럿/선택/타이핑/구문강조가 정상 동작하는지 `yona-projects/components`의
`editor/spike/`에서 검증. 우려했던 3가지 함정(스타일 미주입, `root` 옵션 없을 때 크래시,
`ShadowRoot.getSelection()` 브라우저 편차) 전부 현재 CM6 버전(`@codemirror/view@6.43.11` 등)
에서는 실측으로 반증됨(각각 `style-mod`의 `adoptedStyleSheets` 처리, `EditorView`의 DOM
탐색 폴백, `getSelection(root)` 헬퍼의 `document.getSelection()` 폴백이 이미 내장돼 있었음).
**결론: Shadow DOM 채택 확정.** 스크린샷 18장+Playwright 실측 데이터를
`yona-projects/components` 저장소에 커밋.

### CM6-1단계. 빌드 인프라 (2026-09-11 완료)

`components/editor/`에 정식 npm 패키지 구조(TypeScript+esbuild) 구축. CM6 패키지 버전은
0단계 스파이크에서 실제 검증된 버전을 그대로 exact-pin(`@codemirror/state` 6.7.4 / `view`
6.43.11 / `commands` 6.11.0 / `lang-markdown` 6.5.2, `esbuild` 0.24.2). 최소 골격
(`customElements.define('yona-markdown-editor', ...)` + `attachShadow` only)까지만 구현,
메인 세션이 빌드/타입체크 직접 재현해 검증 후 커밋.

### CM6-2단계. 셸 (폼 통합 + 기본 편집, 2026-09-11 완료)

`markdownEditor` 프래그먼트가 더 이상 `<textarea>`를 직접 렌더링하지 않고
`<yona-markdown-editor name=... editor-mode=...>`를 렌더링하도록 교체. 이 커스텀 엘리먼트가
`connectedCallback`에서 스스로 light DOM에 실제 `<textarea>`를 만들어(name/id(`editor-`
접두어)/`data-editor-mode`/`markdown="true"` 계약 그대로 유지) 기존 폼 제출 코드와의 호환을
보장하고, Shadow DOM 안에 CM6 `EditorView`를 마운트(0단계 검증된 `root` 옵션 방식). 문서 변경
시 `updateListener`로 textarea.value 동기화 + `input`/`keyup` 재발행(임시저장 시스템 의존
유지). `easymde.min.js`/`yobi.ui.MarkdownEditor.js` 삭제.

**진행 중 사용자 확인이 필요했던 판단 지점 (에이전트가 스스로 결정하지 않고 멈춰서 보고 →
사용자 결정)**:
1. **첨부파일 삽입 데이터손실 재발 위험**: `yobi.Attachments.js`/`yona.CommentAttachmentsUpdate.js`
   가 `$textarea.data("easymde").value(newValue)`를 호출해 P3-50에서 고친 데이터손실 버그의
   재발을 막던 워크어라운드가, EasyMDE 제거로 조용히 재발할 뻔했다. **사용자 결정: 호환 shim
   추가**(같은 jQuery data 키에 `{ value(newValue?) }` 최소 shim 노출, 실제 호출 메서드만
   grep해 과잉 구현 안 함) — 데이터손실 재발 방지 완료.
2. **멘션 일시 먹통**: shim이 `codemirror` 키를 지원하지 않아 `yobi.Mention.js`의 attach 대상을
   못 찾아 멘션이 5단계까지 동작하지 않게 됨. **사용자 결정: 허용**(데이터 손실이 아닌 기능
   일시 중단이라 성격이 다름, 화면별 EasyMDE 잔류 절충안은 채택 안 함).
3. **`MarkdownEditorPreviewWidgetTemplateEquivalenceSpec` 동시 파손**: 지시 범위 밖이었으나
   layout.html 변경으로 즉시 깨지는 걸 발견. **사용자 결정: 지금 같이 갱신**(미루면 알면서 깨진
   스펙을 방치하는 셈). highlight.js는 `markdown(project)` fragment의 SSR 콘텐츠 하이라이팅
   (`hljs.highlightAll()`, 8개 화면)이 여전히 의존함을 재확인하고 그 로드는 유지, EasyMDE 관련
   단언만 교체.

**테스트**: `MarkdownEditorShellWidgetTemplateEquivalenceSpec`(2)/
`MarkdownEditorPreviewWidgetTemplateEquivalenceSpec`(5)을 새 마크업 계약으로 갱신(RED→GREEN).
메인 세션이 두 스펙 개별 `--rerun-tasks` 재실행으로 검증(`BUILD SUCCESSFUL in 9m 44s, 9
actionable tasks: 9 executed`, 캐시 아닌 진짜 재실행). Playwright로 실제 타이핑→폼 제출→
서버 수신 값 일치까지 실증(에이전트의 첫 시도는 이미 떠 있던 낡은 dev 서버 재사용으로 타임아웃
났었으나, 별도 포트로 새로 기동한 서버로 재검증해 성공 — 메인 세션도 이 사실을 인지하고
스펙 재실행으로 별도 검증함).

**범위 밖 발견**: 없음(이번 지시 범위 안에서 전부 처리·기록됨).

### CM6-3단계. 툴바 9개 커맨드 + CSS 테마 계약 (2026-09-11 완료)

bold/italic/heading/quote/checklist/unordered-list/ordered-list/link/image 9개 커맨드를
`EditorState.changeByRange()` 기반으로 구현(EasyMDE 소스의 `_toggleBlock`/`_toggleHeading`/
`_toggleLine`/`_toggleLink` 토글 규칙을 직접 읽고 동치로 포팅). bold/italic의 "이미 서식
적용됨" 판정만 CM5 토크나이저 대신 CM6 lezer 구문 트리(`StrongEmphasis`/`Emphasis`)로
대체(CM6-네이티브 포팅, 새 기능 아님). link/image는 지시 범위(템플릿 삽입+커서 이동)만
구현, 해제(unwrap) 분기는 범위 밖. `::part()`(toolbar/button/button-{command}/separator/
editor) + CSS 커스텀 프로퍼티(`--yona-md-*`) 테마 계약 도입, 옛 EasyMDE 재스킨과 동일한
색상/치수를 컴포넌트 자체 기본값으로 내장(yobi.css 오버라이드 불필요 — `.EasyMDEContainer`
죽은 CSS는 제거).

**부수 발견**: 옛 EasyMDE 재스킨 CSS(`.editor-toolbar button.ybtn[class*="yobicon-"]`)가
실제로는 셀렉터 특이도 문제로 yobicon 아이콘 폰트가 안 먹혀 tofu 박스로 깨져 있었음(2단계
때 이미 삭제된 코드라 손대지 않고 기록만). 새 구현은 같은 색상/치수値이면서 아이콘은 오히려
정확히 렌더링됨.

**테스트**: 컴포넌트 쪽 순수 커맨드 로직 단위테스트(`node:test`) 16개 신규(RED→GREEN).
`MarkdownEditorShellWidgetTemplateEquivalenceSpec` 개별 재실행 — 메인 세션이 재검증
(GREEN 2/2, `BUILD SUCCESSFUL in 10m 39s, 9 actionable tasks: 9 executed`). 빌드 산출물이
컴포넌트 저장소 빌드 결과와 바이트 단위로 동일함을 메인 세션이 `diff`로 직접 확인. Playwright로
실서버에서 9개 버튼 전부 실제 클릭→마크다운 문법 변환 결과 표로 실증(회원가입→로그인→
프로젝트 생성→postform, 콘솔 에러 없음).

**범위 밖 발견**: 없음. 확인이 필요해 멈춘 판단 지점도 없음(bold/italic 판정 방식 등은 "동일
목적의 CM6 네이티브 포팅"이라는 명확한 근거로 에이전트가 직접 결정, 최종 보고에 근거 명시).

**다음**: 4단계(미리보기 서버 렌더링 재연동).

### CM6-4단계. 미리보기 서버 렌더링 재연동 (2026-09-11 완료)

옛 `yobi.ui.MarkdownEditor.js`의 `_previewRenderer()`(EasyMDE 시절 구현, git show로 원본
확인)를 CM6/네이티브 fetch 기반으로 이식: 300ms 디바운스 + 요청 순번 비교 레이스가드 +
`POST /markdown/{owner}/{name}` `{body, breaks:true}` → 응답 HTML 삽입 → `pre code`마다
전역 `window.hljs.highlightElement()` 호출(하이라이팅 라이브러리 자체는 번들링하지 않고
전역 참조 - 자체호스팅 중복 방지). CSRF는 layout.html의 전역 `fetch()` 패치(P3-legal/P3-44)가
모든 요청에 이미 적용되어 컴포넌트가 별도로 다룰 필요 없음을 확인.

**판단 지점(사용자 확인 완료)**: 미리보기 패널의 `.markdown-wrap` 타이포그래피와 highlight.js
코드 하이라이팅 색상은 Shadow DOM 경계 때문에 전역 `yobi.css`/`highlight/styles/default.css`
`<link>`가 닿지 않는 문제가 있었다. 3가지 안(전체 재현/부분 재현/링크 참조) 중 3단계 툴바와
동일한 원칙인 "전체 재현"으로 사용자가 확정 — 두 스타일시트의 실제 규칙(`.markdown-wrap`
11331~11559행대, highlight.js `default.css` 전체)을 `.preview-wrap` 셀렉터로 컴포넌트
Shadow DOM 스타일에 그대로 옮겨 적었다(전역 오버라이드 불필요, 컴포넌트 자체 완결 - 단,
yobi.css/hljs 테마가 나중에 바뀌면 이 블록도 손으로 맞춰야 하는 유지보수 트레이드오프는
감수하기로 함).

**부수 발견 및 즉시 수정(심각도 높음, 사용자 지시 "지금 바로 고쳐")**: Step 4 에이전트가
보고만 하고 고치지 않은 채 넘긴 이슈 — `<yona-markdown-editor>` 안(Shadow DOM 내부
contenteditable)에서 타이핑할 때 H/C/I/M/B/A/U/N 등 `yobi.ShortcutKey.setKeymapLink`에
등록된 전역 단축키 글자가 그대로 발동해 미저장 내용을 잃은 채 페이지가 이동하는 회귀가 있었다.
1차 원인은 `_runEventHandler`가 `weEvt.target.tagName`만 보는데, `window` 레벨 리스너
기준으로 Shadow DOM 안에서 발생한 이벤트는 `target`이 shadow host(`<yona-markdown-editor>`,
INPUT/TEXTAREA 아님)로 리타게팅되어 "폼 입력 아님"으로 오판했기 때문 — `composedPath()[0]`로
실제 origin 엘리먼트를 구하고 `isContentEditable`도 폼 입력으로 인정하도록
`common/yobi.ShortcutKey.js`를 고쳤다. 그런데도 Playwright 재현이 계속 실패해 추가로 조사한
결과, **이 세션에서 반복된 "소스는 고쳤지만 실제로 `<script src>`로 로드된 적이 없어 레거시
번들 사본이 대신 실행되는" 패턴**(`yobi.Markdown.js`/`yobi.Files.js`/`yobi.Attachments.js`와
동일)이 또 있었다 — `common/yobi.ShortcutKey.js`는 어느 템플릿에서도 로드되지 않았고, 실제로
동작하던 건 `yona-lib.js`(Play→Spring 리라이트 최초 커밋부터 있던 통짜 미니파이 번들)에
박제된 구버전 사본이었다. `site/layout.html::scripts`의 `yona-lib.js` 로드 직후에
`common/yobi.ShortcutKey.js` `<script src>`를 추가해 전역을 최신 버전으로 덮어쓰도록 고쳤다
(선례와 동일하게 yona-lib.js보다 반드시 뒤에 와야 함). Playwright로 3가지 케이스(에디터 내
타이핑 시 미이동+내용 보존, 일반 `<input>`은 여전히 예외 처리, 본문 영역 단축키는 정상 동작)
모두 재확인.

**테스트**: 컴포넌트 쪽 `preview.test.ts` 21개(신규 5개 - 시퀀싱/디바운스/레이스가드/하이라이팅
적용) 전부 GREEN. `MarkdownEditorPreviewWidgetTemplateEquivalenceSpec`(5/5)/
`MarkdownEditorShellWidgetTemplateEquivalenceSpec`(2/2) 메인 세션이 개별 재실행해 GREEN
확인(`BUILD SUCCESSFUL`, 실행 시각 일치하는 새 XML로 실제 재실행 확인). 빌드 산출물이 컴포넌트
저장소 빌드 결과와 바이트 단위 동일함을 `diff`로 확인. Playwright로 실서버에서 미리보기 토글→
h1/bold/italic/link/list/quote/코드블록 하이라이팅까지 실제 렌더링 결과를 스크린샷으로 직접
확인(옛 EasyMDE 재스킨판과 시각적으로 동일).

**범위 밖**: 없음.

**다음**: 5~7단계(멘션 재구현/16개 호출부 마이그레이션/전면 회귀)는 사용자 지시에 따라 이번
세션에서 착수하지 않고 다른 세션으로 넘긴다 — `docs/parity/tickets/p3-46-cm6-plan.md`의 5~7단계
절 그대로 이어서 진행하면 된다. `yobi.Mention.js`/`yona.TitleHeadAutoCompletion.js`에 남아있는
CM5+Tribute 완화책 시도의 미커밋 diff는 "나중에 한꺼번에 정리"(사용자 지시)로 여전히 보류
상태이며, 5단계 착수 세션이 멘션을 CM6로 재구현하는 과정에서 함께 정리하는 것이 자연스럽다.

### CM6-5단계. 멘션 자동완성 재구현(@codemirror/autocomplete, 2026-09-11 완료)

착수 전 이 저장소를 새로 이어받아 확인한 결과, 계획서에 적힌 대로 4단계까지 완료된 상태 그대로였고
(`components` 저장소 `9820a0b`), 사용자 지시문에 언급된 "3단계 CM5+Tribute 완화책 미커밋 diff"는
이 워킹 트리(방금 새로 클론된 저장소)엔 존재하지 않았다 — 별도 세션의 로컬 전용 잔재였을 뿐이라
신경 쓸 것이 없었다.

**착수 전 API 조사(`@codemirror/autocomplete@6.20.3`, node_modules 소스 직접 확인)**: 계획서가
묻던 "renderOption 커스텀 렌더링 자유도"에 정확히 대응하는 필드는 없지만, `CompletionConfig
.addToOptions(render(completion, state, view) => Node, position)`가 동등한(사실상 더 넓은) 자유도를
준다 — 아이콘(20)/라벨(50)/detail(80) 좌표 공간에 임의 위치로 커스텀 DOM을 끼워 넣을 수 있다. 다만
기본 라벨(label/displayLabel 텍스트) 렌더는 이 옵션과 무관하게 항상 같이 그려지므로, Tribute의
`menuItemTemplate`처럼 `<li>` 내부를 통째로 대체하려면 CSS로 `.cm-completionLabel`을 숨기고
`addToOptions`로 실제 내용을 대신 그려야 한다(정리해서 `mention.ts` 상단 주석에 남김). 또한
`CompletionResult.filter=false`(커스텀 정렬 재사용에 필수) 상태에서 라이브러리가 "연속된 동일
label+detail+apply 옵션을 하나로 합치는" dedup 로직을 갖고 있음을 소스 레벨에서 발견 — 이모지
배열에 content는 같고 name만 다른 항목(`hooray`/`tada` 둘 다 🎉)이 실제로 있어, label을 전부
비워두면 이 dedup에 걸려 하나가 사라진다는 것을 확인하고 label을 사람이 읽는 고유 키(loginid/emoji
이름/issueNo)로 채우는 방식으로 회피했다(`mention.test.ts`에 회귀 테스트 있음). 마지막으로,
`autocompletion()`의 tooltip DOM은 기본값(`config.parent` 미지정)일 때 `view.dom`의 자식으로
붙는다는 것도 소스로 확인 — 즉 Shadow DOM 밖으로 나가지 않아 `MENTION_STYLES`를 컴포넌트 자체
shadow `<style>`에 얹기만 하면 되고, 3~4단계처럼 전역 CSS를 shadow 안으로 옮겨 적는 별도 작업이
필요 없었다.

**컴포넌트(`components/editor`) 구현**: `@codemirror/autocomplete`를 `package.json`
dependencies에 추가(6.20.3 - `@codemirror/view`(6.43.11)/`state`(6.7.4)/`language`(6.12.4) 모두
이미 이 레지스트리의 최신판이라 peerDependencies 범위(`^6.0.0`/`^6.17.0`)를 그대로 만족, 별도
호환성 조사 불필요). 신규 `src/mention.ts`에 `yobi.Mention.js`의 순수 로직을 그대로 이식했다
(이모지 배열 62개, `MAX_QUERY_LEN=20`, `_highlight`→`highlightMatch`, "@" 커스텀 sorter→
`sortUsersBySearchText`, "#" 커스텀 sorter→`sortIssues` - 정렬 공식은 100% 동일, 원본의 배열
mutate만 순수 함수로 다시 씀). 트리거 감지는 CM `CompletionContext`에 의존하지 않는 순수 함수
`matchMentionTrigger(textBeforeCursor, trigger)`로 분리해(같은 줄에서 공백으로 끊기지 않고
트리거로 시작해 커서까지 이어지는 마지막 구간을 찾고, 직전이 공백/줄시작인지로
requireLeadingSpace를 재현) Node 환경에서 직접 단위 테스트했다. "@" 트리거는 원본의 300ms
debounce(`clearTimeout(searchPending)`)를 `CompletionContext.addEventListener("abort", ...,
{onDocChange:true})`로 재현 — 사용자가 계속 타이핑하는 동안 매 키 입력마다 CM6가 이 abort
핸들러를 불러 예약된 setTimeout을 취소시켜, 실제 네트워크 요청은 300ms 정지 구간 뒤에만
나간다(원본과 동일한 서버 부하 절감 효과, 단순히 "최신 결과만 반영"이 아니라 "낡은 요청 자체가
안 나감"까지 동치 재현). "#"는 원본처럼 debounce 없이 매 키 입력마다 조회(원본 코드 그대로
유지). `YonaMarkdownEditor.ts`는 `render-url`과 동일한 패턴으로 `data-mention-url`을 읽어,
있을 때만(전부 없으면 전부 없음 - all-or-nothing) `createMentionExtension(...)`을 extensions
배열에 추가한다.

**판단 지점 1 — 멘션 스코프를 8개 화면에서 project 컨텍스트가 있는 16개 전부로 확대(코디네이터
재검토 필요)**: 계획서 6단계 절은 "멘션 있는 8곳/없는 8곳"을 구분해 서술하지만, 조사해보니 이
구분은 의도적 설계가 아니라 우연이었다 — `yobi.Mention()`은 8개 화면(board/pullrequest/
milestone/issue의 create+edit)의 inline `<script>`에 하드코딩 호출돼 있었을 뿐이고,
`MentionController`의 `/api/{owner}/{projectName}/mentionList`는 project 스코프이기만 하면
어떤 화면에서도 쓸 수 있는 범용 엔드포인트다. 게다가 pullrequest 두 곳은 `tribute.min.js` 자체를
로드하지 않아 `Tribute is not defined`로 이미 깨져 있었고(범위 밖 발견, 아래), 원래 selector도
`textarea[id^=editor-]`라는 페이지 전체 매칭이라 그 8페이지 중 함께 렌더링되는 댓글 수정 폼에도
부수적으로 멘션이 붙던 반면, 댓글만 있고 본문 편집이 없는 화면(issue/view 등)은 아예 대상이 아니었다
— 사실상 일관성 없는 우연한 커버리지였다. 4단계가 미리보기(`render-url`)를 똑같은 이유로 8개에서
project 컨텍스트가 있는 16개 전부로 넓힌 선례가 있어, 이번에도 `site/layout.html`의 공유
`markdownEditor` 프래그먼트에서 `data-mention-url`을 `render-url`과 동일한 게이트(`project !=
null`)로 노출해 16개 전부로 넓혔다(개별 템플릿을 건드리지 않고 프래그먼트 한 곳만 고치면 됨 —
8개 화면에 흩어진 하드코딩 URL 중 pullrequest 두 곳의 "/api" 접두어 누락 버그도 이 중앙화로
자연히 해소됨). 계획서 문면과 다른 판단이라 커밋 전 재확인 요청.

**판단 지점 2(코디네이터 지적으로 발견한 범위 침범 — 즉시 수정)**: `yobi.Mention.js`를 삭제하면서
`tribute.min.js`/`tribute.min.css`도 8개 화면 전부에서 함께 걷어냈는데, `board/create.html`·
`issue/create.html`·`issue/edit.html` 3곳은 계획서가 명시적으로 "범위 밖(무변경)"이라 못박은
`yona.TitleHeadAutoCompletion.js`(`[` 라벨 트리거 - CodeMirror와 무관한 별도 Tribute 인스턴스)도
같이 로드하는 화면이었다 — 이 스크립트가 전역 `Tribute` 생성자에 직접 의존(`new Tribute({...})`)
하므로, tribute 라이브러리 자체를 걷어내면 이 3곳의 `[` 트리거가 `Tribute is not defined`로
깨지는 회귀였다. 코디네이터가 작업 도중 발견해 지적, 즉시 `git checkout HEAD --
.../lib/tribute/`로 파일을 복원하고 이 3개 템플릿에만 원래 위치(`yona.TitleHeadAutoCompletion.js`
직전)로 `<script src=".../tribute.min.js">`(+ 원래도 있던 2곳엔 `tribute.min.css`)를 다시
추가했다. 나머지 5개 화면(board/edit, milestone 2개, pullrequest 2개)은 TitleHeadAutoCompletion을
쓰지 않으므로 tribute도 완전히 제거된 채로 유지. 수정 후 Playwright로 `[` 트리거가 실제로 여전히
동작하는지(제목 입력창에 `[첫` 입력 → `.tribute-container` 드롭다운 표시, 콘솔에 Tribute 관련
에러 없음) 재확인 완료.

**범위 밖 발견**: `pullrequest/create.html`/`pullrequest/edit.html`은 CM5/Tribute 시절부터 이미
두 가지 이유로 깨져 있었다 — (1) `tribute.min.js` 자체를 로드한 적이 없어 `new Tribute(...)`가
`ReferenceError`, (2) `yobi.Mention()`에 넘긴 URL이 `@{/{owner}/{projectName}/mentionList(...)}`
로 `/api` 접두어가 빠져 있어(실제 매핑은 `/api/{owner}/{projectName}/mentionList`) 설령 (1)이
없었어도 404였을 것. 이번에 `yobi.Mention()` 호출 자체와 그 URL을 완전히 제거하고 위 판단 지점
1의 중앙화된 `data-mention-url`로 대체했으므로 두 버그 모두 자연히 해소됨(별도 수정 불필요 -
지금 바로 고쳐졌다는 의미로 기록).

**테스트**: 컴포넌트 쪽 `node:test` 40개 전부 GREEN(신규 `mention.test.ts` 24개 -
`matchMentionTrigger`/`highlightMatch`/`sortUsersBySearchText`/`filterEmojis`/`sortIssues`
순수 로직, 기존 `commands.test.ts`(16)/`preview.test.ts`(4) 회귀 없음). yona 쪽
`MentionAutocompleteWidgetTemplateEquivalenceSpec`(CM5/Tribute 시절 이후 방치돼 4단계 시점부터
계속 RED였던 것을 확인 - 9개 중 8개 실패, 메인 세션이 직접 재현) 마크업 계약을 `data-mention-url`
기준으로 전면 재작성해 RED→GREEN(9/9, `--tests` 개별 실행으로 실제 재실행 확인 -
`build/test-results/test/*.xml` 새 타임스탬프). `MarkdownEditorShellWidgetTemplateEquivalenceSpec`
(2/2)/`MarkdownEditorPreviewWidgetTemplateEquivalenceSpec`(5/5)/`IssueViewControllerSpec`
(114)/`BoardViewControllerSpec`(87)/`MilestoneViewControllerSpec`(75)/
`PullRequestViewControllerSpec`(87) 전부 개별 재실행해 회귀 없음 확인(총 379케이스, 전부 fresh
timestamp GREEN). 빌드 산출물(`yona-markdown-editor.min.js`, 543.9kb - autocomplete 추가로
4단계 대비 약 26kb 증가)을 yona 저장소에 vendoring(소스맵 파일은 이전 단계들과 동일하게 vendoring
대상에서 제외 - 선례 확인).

Playwright로 실서버(H2 프로파일, 격리 포트/데이터 디렉터리, 최초 관리자 계정 생성→프로젝트
생성→이슈#1 생성→issueform)에서 3트리거 전부 실제 재현: ":smi" 입력→이모지 후보 3개 표시→선택 시
"🙂 " 삽입, "@" 단독 입력(menuShowMinLength:0)→자기 자신이 후보로 표시→선택 시 "@loginid " 삽입
(실제로는 특수 항목 "@owner/project "가 선택됐는데, 이는 테스트용 로그인 아이디를
`MentionController.MENTION_ADMIN_LOGIN_ID` 상수와 우연히 똑같은 "admin"으로 만들 수밖에
없었기 때문 - bootstrap-setup이 최초 관리자 loginId를 "admin"으로 강제하고,
`toMentionMaps`가 그 loginId를 가진 사용자를 후보에서 제외하는 기존 서버 로직 때문에 실제
사람 계정 대신 "@project all:" 특수 항목만 후보로 남은 것 - CM6 어댑터 자체의 결함이 아니라
테스트 픽스처가 우연히 그 상수와 충돌한 것임을 확인), "#1" 입력→이슈#1 후보 표시→선택 시 "#1 "
삽입. 회귀 방지 시나리오도 재현: 백스페이스 직후 Enter(문서 반복/붕괴 없음 - CM5
zurb/tribute#511류 버그 재발 없음 확인), 후보 0개(MAX_QUERY_LEN 초과로 메뉴 자체가 안 뜸)
상태에서 Enter(평범한 개행으로만 처리, 문서 붕괴 없음). 위 "범위 침범 즉시 수정" 이후 `[` 라벨
트리거도 별도로 재확인(전역 `Tribute` 정의 확인 + 실제 드롭다운 표시 + 콘솔 에러 없음).

**코디네이터 독립 검증(2026-09-11)**: 계획서 원칙("메인 세션이 diff 직접 검토 + 스펙
재실행으로 독립 검증한 뒤에만 커밋/푸시")에 따라 직접 검증했다 — 컴포넌트 쪽 `npm test`
40/40 GREEN 재실행, `npm run build` 재실행 후 vendoring된 산출물과 `diff`로 바이트 단위
동일함 확인, yona 쪽 관련 스펙 7종(`MentionAutocompleteWidgetTemplateEquivalenceSpec`(9)/
`MarkdownEditorShellWidgetTemplateEquivalenceSpec`(2)/
`MarkdownEditorPreviewWidgetTemplateEquivalenceSpec`(5)/`IssueViewControllerSpec`(114)/
`BoardViewControllerSpec`(87)/`MilestoneTemplateRenderingSpec`(3)/
`PullRequestViewControllerSpec`(87))을 개별 재실행해 전부 GREEN, `build/test-results/test/*.xml`
타임스탬프로 실제 재실행 확인. `tribute.min.js` 복원 상태(3개 화면 한정 로드)와
`data-mention-url` 게이트(`render-url`과 동일 조건)도 diff로 직접 재확인.

**"판단 지점 1"(멘션 스코프 8→16 화면 확대) 사용자 승인 완료(2026-09-11)** — 계획서 문면과
다른 확대였으나, 4단계 선례와 동일한 논리이고 pullrequest 두 곳의 URL 접두어 누락 버그도
함께 해소된다는 점을 근거로 승인받았다.

**`yona-lib.js` 구버전 사본 재확인**: 번들 안에 구식 `yobi.Mention = function(...)` 정의가
여전히 존재하지만, 이를 호출하는 코드(`yobi.Mention(...)`)가 템플릿 어디에도 남아있지 않음을
`grep`으로 확인 — 죽은 채로 남을 뿐 실행되지 않아 전역 이름 충돌 위험 없음(계획서 6단계
검증 항목 충족).

코디네이터가 컴포넌트 저장소(커밋 `79683ab`, 푸시 완료)와 yona 저장소(이 커밋)를 각각
커밋·푸시한다.

**다음**: 6~7단계(16개 markdownEditor 호출부의 8개 멘션 호출부 마이그레이션 — 이번 5단계에서
이미 대부분 흡수됨/전면 회귀)는 이번 세션에서 착수하지 않는다. 6단계 잔여 작업은 사실상 이번
5단계로 이미 끝났음을 코디네이터가 확인했다(8개 멘션 호출부의 `yobi.Mention(...)` 인라인
스크립트 제거 + `mention-url` 속성 추가가 이번 5단계에서 전부 처리됨 - "판단 지점 1" 참고).
남은 것은 사실상 7단계(전면 회귀 + 16개 화면 개별 Playwright 확인)뿐이다.
