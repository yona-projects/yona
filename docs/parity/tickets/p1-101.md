---
id: p1-101
type: ticket
status: done
created: 2026-08-21
updated: 2026-08-21
relates_to: []
source: docs/PARITY_BACKLOG.md:154 (표, P1 섹션) — 이관 전 버전은 git 히스토리 참고
---

# P1-101: **(2026-08-21 컨트롤러/모델 전수 대조에서 발견)** 이슈 가중치(weight) 투표 API가 전혀 없음 — `Issue.weight: Int` 필드는 이식돼 있으나 이걸 증감시키는 엔드포인트가 없어 죽은 컬럼 상태

## 원본 표 정보

- **우선순위 구분**: P1
- **상태(원본 체크박스)**: `[x]`
- **yona 근거**: `app/controllers/api/IssueApi.java:1176-1210 upvoteWeight()/downvoteWeight()`(`AccessControl.isAllowed(..., UPDATE)` 확인 후 `issue.weight`를 ±1, 저장 후 `{"weight": N}` 응답)
- **yona 대상**: `domain/issue/{IssueService,IssueServiceImpl}.kt`, `web/IssueController.kt`
- **비고(원문)**: **완료** — 아래 완료 로그 참고

## 완료 로그 (원문, `PARITY_BACKLOG.md`의 "완료 로그" 섹션에서 이관)

- **2026-08-21 — P1-101**: 이슈 가중치(weight) 증감 API 신규 이식.
  - `IssueService`/`IssueServiceImpl`에 `upvoteWeight(issueId)`/`downvoteWeight(issueId)` 신규 — yona `IssueApi.java:1176-1210`과 동일하게 `issue.weight`를 ±1 하고 저장 후 갱신된 `Issue`를 반환.
  - `IssueController.kt`에 `POST /{number}/upvoteWeight`/`downvoteWeight` 신규 — `accessControl.isAllowed(user, project, issue, Operation.UPDATE)`([[tickets/p1-85|P1-85]] 1b에서 이미 이식된 중앙 함수, yona `AccessControl.isAllowed(user, issue.asResource(), Operation.UPDATE)`와 정확히 동일한 규칙)로 권한 확인 후 서비스 호출, 응답은 갱신된 `Issue`(JSON에 `weight` 포함) — yona의 `{"weight": N}` 응답과 관찰 가능 동작 동일.
  - 테스트: `IssueServiceSpec.kt` +1(가중치 증감 및 영속화 확인, 실 DB), `IssueControllerSpec.kt` +3(업보트 성공/비멤버 403/다운보트 성공).
  - 검증: `./gradlew test --tests "...IssueServiceSpec" --tests "...IssueControllerSpec"` 전체 통과.
