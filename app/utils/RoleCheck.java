package utils;

import models.Project;
import models.Role;
import models.User;

/**
 * @author "Hwi Ahn"
 *
 */
public class RoleCheck {
    public static boolean roleCheck(String userId, String roleToBeChecked, String projectId) {
//        ProjectUser projectUser = ProjectUser.findById(Long.parseLong(userId), Long.parseLong(projectId));
//        Project project = Project.findById(Long.parseLong(projectId));
//        if(project.share_option) return true;
        
        return false;
    }
}
